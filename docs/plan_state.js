window.PLAN_STATE={"stamp": "2026-08-19 17:25:22", "raw": "STATE\n# RUNNING: full gate on hub linking, then promote and push, started 2026-08-19 17:25\n\n# A0  Groundwork \u2014 the duplication that makes every later area harder\nA0-1: done 2026-08-18  \u2014 this file\nA0-2: done 2026-08-18  \u2014 docs/architecture/ written from three explorations\nA0-3: done 2026-08-18  \u2014 promoted 2026-08-18, QC 1574/0. Also fixed two things found on the\n                way: promt.md was about to be destroyed by promote (now in\n                SKIP_FILES, it is a live log not source), and a hub could forward\n                to itself over HTTP because all of 127.0.0.0/8 is loopback\n                (now short-circuited by is_self, guarded by a check that was\n                proven to discriminate)\nA0-4: done 2026-08-18 09:46  \u2014 promoted 2026-08-18, QC 1607/0. shared/web/mice.css is the only\n                :root in the product; every page links /mice.css and the BOARD\n                serves that same URL from flash (gen_tables.py compiles the file\n                into web/MiceCss.h), so a module looks the same over WiFi and\n                over USB. Radii unified on 10/14/18/999, the four caption\n                classes became one rule, soft tints and --code-bg are now\n                derived with color-mix instead of five hand-picked hexes, and\n                color-scheme:dark fixes the native checkbox/slider/scrollbar.\n                check_design_system.py guards it (broken twice, failed twice).\n                Two things found on the way: promote.py was about to push a QC\n                run's settings_shared.json over the real one (now in SKIP_FILES,\n                guarded by check_no_data_loss), and the three snapshot patchers\n                now carry the shared stylesheet, or an old page would restore\n                against today's tokens\nA0-5: done 2026-08-18 10:31  \u2014 promoted 2026-08-18, QC 1611/0. Deleted. It was a second module\n                page: 295 lines, six cards duplicated from WebUI.h, and 8 joint\n                sliders where a nong has 10, so WAIST and SHRUG were unreachable\n                through it. Nothing linked to it and it already redirected away\n                in JavaScript. /module.html survives as a 302 that carries\n                ?port=&id= across to /mod?dev=usb:PORT:ID, so an old bookmark\n                still lands on the right module; bare /module.html goes to the\n                hub instead of to a page addressed at nothing. The hub's AP-mode\n                message no longer sends people to it. check_hub_api guards both\n                halves (broken twice, failed twice). promote.py does not delete,\n                it warns \u2014 the file had to be removed from the real tree by hand\nA0-6: done 2026-08-18 12:26  \u2014 one logging helper in firmware. src/core/Log.h\n                holds the tag list ONCE (X-macro generates both the enum and the\n                names); mlog::line formats the whole line and issues exactly ONE\n                Serial.write, which cannot be split by another task \u2014 the bench\n                bug where a WiFi event glued its log onto the end of a reply.\n                34 call sites moved off Serial.println/printf across 5 files,\n                including the SSID scan that built one line out of twelve\n                writes. A log can no longer contain a newline (it would read as\n                two replies) and an over-long line is cut with a visible ...\n                The formatting is Arduino-free so the PC tests assert the real\n                bytes: 5 new native tests, 25 total. check_logging.py guards\n                that nothing goes around the helper (broken twice, failed\n                twice). Flash unchanged at 66.5%. COMMANDS.md carries the\n                one-write rule. NOT tested on hardware\nA0-7: done 2026-08-18 17:18  \u2014 RS485Bus::send. MY PREMISE WAS WRONG, and\n                the code said so: I parked this as an RS485 copy of the A0-6\n                logging bug, but Serial2 is written in exactly ONE function,\n                send() serialises on sendMtx_ (created because the async web\n                task and loop() both send), and the driver enable is held across\n                the whole frame. Nothing can get between those two writes. The\n                hole that WAS open is narrower: the lock is applied as\n                `if (sendMtx_)`, so a board that never created it would skip\n                locking in silence and let two tasks interleave halves of two\n                frames onto a bus every module obeys. That is now reported once\n                instead of ignored. check_rs485_frame.py pins the three things\n                that make a frame whole \u2014 one writer, the lock held ACROSS the\n                writes, the driver raised and lowered once \u2014 broken three ways,\n                failed three ways. Builds; not tested on hardware\nA0-8: done 2026-08-18 17:18  \u2014 the reply path (main.cpp emitLine) and the log path now both\n                build one buffer and write once, in two different places. Share\n                one, so the rule cannot be half-kept. Found while doing A0-6\nA0-9: done 2026-08-18 17:18  \u2014 Studio's side panel overflows 32px at its default width: the\n                joint number box sits past the edge and the panel grows a\n                scrollbar. Pre-existing, measured against the unmodified tree\n                while doing A0-4, NOT caused by it\nA0-11: done 2026-08-18 13:50  \u2014 the browser checks FLAKE under full-suite load: check_responsive\n                reported measurements missing inside run_qc.py, then passed 35/0\n                on its own; an earlier full run failed transports/ui which then\n                passed 194/0. A suite that fails randomly is a suite people stop\n                believing, and promote.py is gated on it. Give the browser\n                checks a retry, or more settle time, and prove it by running the\n                full suite several times.\n                FOUND IT: qcMark in qc/lib/browser.py swallowed a failed fetch\n                with a bare catch, so a dropped report vanished and the check\n                failed with measurements missing. It now retries four times.\n                check_qc_reports.py drives it against a network that refuses the\n                first two requests: before the fix R1 and R2 were lost, after it\n                all six arrive. Fixed as a BLOCKER \u2014 it was stopping A0-6 from\n                promoting, not a later cleanup.\n                REOPENED and finished during A1-3: the dropped report was only\n                HALF of it. The other half was drivers measuring on a stopwatch\n                \u2014 a fixed setTimeout, then count \u2014 which under load measured a\n                page that had not finished and reported a bug that did not\n                exist. Three more full runs failed on three more checks. Fixed\n                three ways: check_responsive now AWAITS each report instead of\n                firing 25 at a port that takes one caller at a time; a shared\n                qcWaitFor(condition, deadline) went into both browser preludes\n                and check_network_tab uses it; and the wait deadline went from\n                25s to 150s of grace, which costs nothing because the wait ends\n                the instant a page says done. Two consecutive full runs green\nA0-10: done   2026-08-19 02:31  touches=hub.html,WebUI.h,studio  \u2014 three tab systems, all called showTab, with different class One tab component in mice.css, used by the hub, the module website and Studio. They had three: the hub outlined its active tab while the other two filled theirs, so the hub looked like a different product from the screens it opens. Filled won - two of three already used it, it survives a dark room and an angled screen, and an outline differs from an unselected tab only by colour, which fails a screenshot and a colourblind reader. Every id, data attribute and handler kept; .mtab stays as a NAME beside .tab because scripts and QC select on it. check_tabs.py measures the COMPUTED colours of all three in a real browser. Two weak assertions of mine were exposed by breaking it: all-three-match is satisfied by three apps that are equally wrong, and the report separator was being eaten by the mark sanitiser so the comparison could never fail.\n                names and different active styles: the hub is an outline tab,\n                the module site and Studio are filled. Left alone by A0-4\n                because unifying them changes markup QC drives\n\nA0-12: done   2026-08-18 23:15  touches=qc/,promote.py  \u2014 a change costs two full QC runs: one by hand before promoting and\n                one inside promote.py as the gate. The gate must stay FULL \u2014 three\n                times on 2026-08-18 a change broke a check that looked unrelated\n                (a stylesheet broke Studio's panel, the logging helper broke\n                check_wifi_live, the login gate broke check_modsite_tabs) \u2014 but\n                the SECOND run is pure waste. Make promote reuse a run that just\n                passed on an unchanged tree, or run the fast checks first and\n                stop at the first failure so a broken change fails in seconds\n                instead of nine minutes. ~20 of the 50 checks launch a real\n                browser and that is nearly all of the time.\n                TWO SUITES CANNOT RUN AT ONCE TODAY, which is why waiting hurts:\n                browser.kill() kills every Edge carrying the QC tag, so a second\n                run murders the first one's browser, and every run reports\n                through the same fake serial port into one shared qc_marks list.\n                Fixing that \u2014 a tag per run, and marks owned by the run that\n                produced them \u2014 is what would let two changes be verified in\n                parallel instead of queueing\nA0-13: done   2026-08-19 02:49  touches=qc/,promote.py  \u2014 MORE THAN ONE .staging, so two changes can be verified at the PROVEN, not asserted: two browser-driven suites ran at the same time in two trees, both green - 45/0 in 47.6s and 45/0 in 48.3s, 49s wall clock for what was 96s serial. Each run takes its identity from its own pid: browser.TAG carries it (kill matches on the tag, so a shared one meant one run killed the other's browser mid-measurement - which looks exactly like a flaky check), and _hub_port starts at 8700+(pid%40)*25 (two runs on 8700 could have one tree's hub answer the other tree's request, which does not fail loudly, it silently tests the wrong tree). promote.py takes --staging DIR. Promoting stays SERIAL on purpose: both trees copy into the same real tree and two promotes touching one file is a lost edit - the failure that ate the settings_shared.json fix earlier.\n                same time instead of queueing behind one 10 minute suite. The\n                user's idea, 2026-08-18, and two staging trees are two PROCESSES\n                so they do not share qc_marks or the fake port \u2014 the collision\n                that bit us inside ONE process does not apply. Four things stop\n                it today, all small and all verified:\n                  * qc/lib/browser.py TAG is the constant MICEQCBROWSER and\n                    kill() matches on it, so one run kills the other run's\n                    browser \u2014 needs a per-process suffix;\n                  * qc/lib/qc.py _hub_port starts at 8700 in both, so the two\n                    trees fight over ports and one hub can answer the other's\n                    requests \u2014 base it on the pid or take a free port;\n                  * the QC auth file is named mice_qc_auth_<port>.json, so the\n                    same port means the same file (falls out of the port fix);\n                  * promote.py hardcodes STAGING = MAIN/.staging \u2014 needs to take\n                    the tree as an argument.\n                Then promote them one at a time, since both copy into the same\n                real tree and two promotes touching one file is a lost edit.\n                HOW MANY. Measured on this machine 2026-08-18: 24 cores, 31 GB\n                RAM, 47.8 GB free, and a staging tree is 648 MB of which 624 MB\n                is firmware/.pio \u2014 the SOURCE is about 24 MB. So disk allows\n                ~70 trees and the CPU is not the constraint either. The real\n                ceilings are different:\n                  * LOAD MAKES BROWSER CHECKS FLAKE. Every intermittent failure\n                    chased on 2026-08-18 was load-induced timing. Twenty suites\n                    at once is twenty times that load on the checks that were\n                    just stabilised, so the number must rise only as the flake\n                    causes are proven fixed;\n                  * INDEPENDENT WORK RUNS OUT. Parallel trees only help when the\n                    tasks touch different files, and this plan does not have\n                    twenty such streams \u2014 every A2 task edits hub.html and every\n                    A4 task edits WebUI.h. Three to five is what actually exists;\n                  * and one build cache would save most of the disk anyway:\n                    point PLATFORMIO_CORE_DIR at a shared folder so the trees do\n                    not each carry 624 MB of the same libraries.\n                So: start at THREE, measure wall-clock and how many runs come\n                back green first time, and raise it only if that stays clean.\n                Twenty trees on a suite that flakes under load is twenty times\n                the debugging, not twenty times the work\n\n# A1  Authentication \u2014 there is none today\nA1-1: done   2026-08-19 00:57  touches=WebPortal.cpp  hw=1 board (done on nong id 85, 2026-08-19) \u2014 [hw] runs in WebPortal.cpp on the board; fake_wifi is a Python stand-in, so auth there can be read but never executed. Original task: enforce auth on the board's mutating HTTP routes VERIFIED ON REAL HARDWARE 2026-08-19, nong id 85 lift-test at 192.168.137.72. Before: GET /api/cmd?c=SET NAME hacked returned 200 and ran, from any device on the WiFi. After: 401. Reading stays open (PING, INFO, /api/status, the page, the stylesheet) because the hub's own discovery probes /api/status and gating it would hide every board. STOP stays open on purpose - refusing a stop is the dangerous answer. What is gated comes from config/commands.json (query / safety flags compiled into COMMAND_DOCS), not a list in C++, so allowing a new command is one entry and an unknown command fails CLOSED. Two registry faults found on the way and fixed: CAM was declared query:true while its help said it changes the sensor, and PIN VALID now needs a login because PIN is correctly declared as changing - only PIN? is a query.\nA1-2: done   2026-08-19 01:28  touches=WebUI.h,UserStore.cpp  hw=1 board (done on nong id 85, 2026-08-19) \u2014 [hw] same: the board's login card and first-login change live in WebUI.h + UserStore on the chip. Original task: stop printing the default password; force a change on first login VERIFIED ON HARDWARE 2026-08-19, nong id 85. The card no longer prints manny/12345678 and neither does the served page source - view-source is not a lock. A board still carrying the shipped password answers mustChange:true and the website will not open Setup until it is changed. Checked BY VALUE, not by a flag set when the account is created: this very board had been in service since before the firmware, so a flag would have called it set up and never asked - the boards most likely to still have the shipped password are exactly the ones a flag misses. Caught before flashing. Minimum length went 1 to 8 (what the shipped one already was) and changing it TO 12345678 is refused, because forcing a change is theatre otherwise. Measured on the board: login mustChange:true, 7 chars refused, change-to-itself refused, real change accepted, old password then 401.\nA1-3: done 2026-08-18 13:50  \u2014 hub login for mutating routes. main_python/\n                hub_auth.py holds the gate; nothing that CHANGES anything is\n                reachable without a session, and everything that only reads\n                still is. Three things the obvious design would have got wrong:\n                (1) the rule is not POST \u2014 /api/usb/cmd, /api/robot/cmd and\n                /api/robot/delete are GETs that move servos and delete files, so\n                the gate is a LIST of what changes something; (2) STOPPING is\n                never gated (NEVER_GATED), because a password prompt while an arm\n                is about to hit someone is a safety failure \u2014 starting a show\n                needs a login, ending one never does; (3) a module on another PC\n                is gated by THAT hub, since a session here means nothing there,\n                and the error says to go and log in on that PC. Password is\n                PBKDF2 + random salt, generated on first run and printed only in\n                the hub's own console on the PC, never on a page and never over\n                the network; five wrong tries locks that caller for a minute and\n                no one else. check_hub_auth.py drives it from outside with no\n                session, 57 assertions, and asserts on fake_serial.wire \u2014 a gate\n                that answers 401 AFTER the arm moved has protected nothing.\n                Broken three ways, failed three ways. Gemini designed the login\n                card; its .banner.info/.warn moved into mice.css (a banner is a\n                shared component) and the lockout countdown now reads the real\n                number from the hub instead of a hardcoded 30s\nA1-4: done 2026-08-18 15:19  \u2014 remote flash confirms, naming the PC. The old\n                question was a browser confirm() box naming only a COM port or\n                an IP \u2014 neither tells you WHICH board in the room you are about\n                to overwrite. It is now an in-page panel that names the board\n                (name, id, the type it runs now), what it becomes and whether\n                that is a change, how it is reached, and THIS PC BY NAME \u2014\n                several machines at a venue run the same hub on the same\n                network, so the wrong browser tab is the easy mistake. Cancel is\n                the prominent button and under the thumb; overwrite is styled\n                destructive and has to be reached for; Escape cancels; the\n                dialog takes focus. Being a real panel and not confirm() also\n                means QC can READ it. check_flash_confirm.py clicks Cancel and\n                then asks the HUB whether a write began \u2014 a confirm that closes\n                while the write has started is worse than none. Broken twice\n                (drop the PC name; make cancel answer yes), failed twice.\n                Gemini designed it; .banner.ok moved into mice.css, its\n                id=flashBusy collided with hub.html's flashBusy() function, and\n                it needed Escape and focus handling. check_ota asserted the\n                literal confirm( and now asserts the panel instead\n\n# A2  The hub's screens\nA2-1: done   2026-08-18 23:15  touches=hub.html  \u2014 hub nav: Home Modules Firmware Network Tools Settings Help Seven destinations. Help is a LINK not a tab. Home and Settings are real, not placeholders: Home counts what the scans already found so it cannot disagree with Modules, and repeats any error banner from the other tabs; Settings says who you are on which PC. Firmware is an honest empty state pointing at where flashing lives today. Gemini designed the bar; its placeholder copy named internal task ids on a user-facing page, and its filled-active-tab idea is A0-10 territory. Also fixed a real bug found by looking: .banner is display:flex which BEATS the hidden attribute, so hidden banners showed as empty coloured bars - mice.css now makes [hidden] win everywhere. check_hub_nav.py drives all seven in a browser; broken three ways, and the third break exposed a weak assertion of mine plus a report truncated at the first newline.\nA2-2: done   2026-08-19 04:30  touches=hub.html needs=A2-1  \u2014 a real Firmware section, separate from module management one screen writes firmware; unbuilt types show their build command; a finished write sets the board type; flashBusy restored (the cleanup deleted it and the first Write threw) one Firmware screen writes over cable or WiFi; unbuilt types are listed with the exact pio command; a finished write sets the board type. Two real faults were found by rebuilding the checks rather than renaming their selectors: the cleanup deleted flashBusy so the FIRST click on Write threw before anything was sent, and check_hub_nav measured the panel before it had painted. check_flash_type now drives fwWrite itself with the network stubbed and asserts on the SET TYPE that reached the board - broken four ways, and three of those assertions were too weak on the first try. Gemini reviewed it and claimed the OTA path must poll /api/ota and that want is shared state; both are false, checked against the code. Patch 0006, gate 1917/0\nA2-3: done   2026-08-19 04:59  touches=hub.html,mice.css  \u2014 .danger styling so destructive actions look destructive danger is a component in mice.css now, with hover and disabled states and a compact icon button, not a colour pasted per control. The unlink button never had it; the remove-account control had a red spelled out in an inline style, which is how a design system quietly stops being one - and it is now a button, since a control that ACTS is not a link. check_danger.py holds the line: the rule must be built from --err with no literal colour, every destructive handler in the list must carry it, and no control may hand-roll it. Broken three ways plus a CONTROL edit that had to keep passing\nA2-4: done   2026-08-19 04:59  touches=main.py,hub.html needs=A2-2  \u2014 POST /api/flash/remote, image sent over the network the image travels, not the command. POST /api/flash/send hands this PC's built image to the hub holding the cable; POST /api/flash/remote receives it, unpacks it and runs esptool THERE; GET /api/flash/at proxies that PC's progress back, because the browser cannot ask another hub directly - the session cookie is SameSite=Lax so a cross-origin POST carries nothing. New decision, not in the plan: the operator types a login for the other PC in the confirm panel, used once and never stored, rather than a shared token that would leave every PC holding a credential that overwrites boards on every other PC. Found while writing the check: drain() swallowed whatever Content-Length claimed, so one header saying 900 MB blocked the hub on a REFUSED request, before any route saw it - capped at 4 MB. Eight sabotages, all caught. Gate 1950/0, patch 0007\nA2-5: done   2026-08-19 04:30  touches=hub.html  \u2014 move the pio build command off the first screen the pio run build command is off the first screen. It lives on Firmware now, built from the envs that are actually missing rather than one written into the page\n\n# A3  Discovery and identity\nA3-1: done   2026-08-18 23:15  touches=main.py  \u2014 one discovery layer: Discovery to Registry to ConnectionManager main_python/discovery.py holds the sweep once: Sweep walks one /24 in parallel, Finder is one kind of thing with a shelf life and an optional patient second pass. scan_hubs and scan_modules were the same loop with different probes and TTLs, and the divergence was real - the module scan had learned to be patient with a board that answers late and the hub scan never did. A new kind of thing is now ONE line, driven in the check by adding a fake one and watching it find something. Two design points worth keeping: probes are passed as wrappers so they are looked up WHEN THEY RUN (binding the function at construction silently breaks every monkey-patch, including the QC ones), and Finder.forget() is public so a check does not poke internals. check_discovery.py broken four ways - the fourth was a CONTROL, a harmless edit that must not fail, and it DID, exposing that I had asserted the spelling of a line rather than the object it produces\nA3-2: done   2026-08-19 05:00  touches=main.py  \u2014 one subnet sweep helper (scan_hubs and scan_modules are the same loop) no new work: A3-1 already did it. scan_hubs and scan_modules are each one line over discovery.Finder (main.py:255 and :321), which is exactly what this task asked for. Verified in the code rather than rebuilt\nA3-3: done   2026-08-19 08:56  touches=main.py,hub.html needs=A3-1  \u2014 QR at /api/qr and mDNS mice.local both halves, both stdlib-only because the hub is copied to a venue laptop and run. main_python/qr.py is a QR encoder written from the standard - GF(256) Reed-Solomon, zig-zag placement, all eight masks scored - served as SVG at /api/qr and shown on the Network screen beside the address, redrawn each time the tab opens because a laptop that moves between venue WiFi and its own hotspot gets a new IP. main_python/mdns.py answers mice.local; if Bonjour or avahi already holds 5353 the hub still runs and the page says the name is unavailable rather than printing one that answers to nothing. Verifying a thing that cannot be eyeballed: the check reads the picture BACK and also checks the parity by Reed-Solomon syndromes, which is different arithmetic; both constant tables are then verified by computation, not recall - all eight format strings match the BCH(15,5) code, and every version's capacity matches the free-module count from my own placement. Thirteen sabotages, all caught, one of which found a real fault: the responder sent the multicast copy FIRST, so on a network where multicast fails - the exact case mDNS exists for - the throw meant the unicast answer was never sent. Gate 1994/0, patch 0008\nA3-4: done   2026-08-19 09:47  touches=main.py  \u2014 stable identity, dedupe a board that is on WiFi and cable boards report chip - the ESP32 eFuse MAC - in INFO and /api/status, from the same source already used for the default name and bus id, so it adds no new truth. modules_here() merges cables, RS485 children and WiFi into one list per board with every route; the module screen renders one row saying reachable on COM9 and over WiFi. The dangerous direction is the opposite of the reported bug: two DIFFERENT boards out of one box share a default id, so merging on the id would send a command meant for one arm to the other - without a chip the id counts only with the type, and a board with a chip is never merged with one without. Six sabotages, all caught; the sixth needed a new case, a half-awake board reporting its chip and nothing else, which must not blank the name the cable already knew. Firmware builds mice_nong and mice_blank; COMMANDS.md documents the field. One check was found to be testing nothing: check_ui_states broke /api/scan, which the list no longer calls. Gate 2016/0, patches 0009 and fw 0038 VERIFIED ON HARDWARE 2026-08-19. A board flashed with the current firmware answers INFO with chip 6825DD163688 - the eFuse MAC, twelve hex digits, exactly as designed. Before the flash the same board reported no chip at all (fw 1.0.0), which is what every board on this bench still does\nA3-5: todo touches=firmware  hw=2 boards; RS485 transceivers x2 + A/B wires for the bus half (the hotspot half needs no wiring) \u2014 [hw] RS485 peers and hotspot peers are board-to-board; nothing on a PC carries a real bus. Original task: carry group on RS485 and hotspot peers (they always show not linked) UNBLOCKED 2026-08-19: RS485 is wired and working - broadcast on COM23 answered @67 PONG 67 nong nong through a MAX485. Both halves are now doable The hotspot half is DONE - see A13-7, fixed and measured with three boards. What is left of A3-5 is the RS485 half: carrying the group to modules behind the bus\nA3-6: done   2026-08-19 10:07  touches=hub.html  \u2014 hide IP, port, COM, dBm behind Advanced addresses, cable ports and signal strengths sit behind one switch in Settings, remembered per browser. It is ONE rule in shared/web/mice.css - .tech and body.adv .tech - not a decision repeated per page, and every page in the project already links that sheet, so the board site can join the same switch with one class. The module id stays visible with the switch off even though it shares a span with the address: it is how a board is addressed on the bus and how two identical arms on a shelf are told apart. Six sabotages; the one that got through first was the lazy fix - hiding the whole span, which takes the id with it - because innerText of a display:none element falls back to its raw text, so asking the hidden span reports it as visible. Measured on the rendered row instead. Gate 2031/0, patch 0010\n\n# A4  The module's own website\nA4-1: done   2026-08-18 23:15  touches=WebUI.h  \u2014 board site: what am I, at a glance The board reports fw, group and seq.running in the same status payload it always sent, and none of them reached the screen. The header now shows the firmware version - the first question anyone asks about a board behaving oddly, previously only reachable by sending INFO on a console - plus the group when it is in one, and a line saying what it is DOING and HOW you reached it: playing a named sequence or not running anything, over WiFi or over the cable through the hub. That distinction matters because unplugging a cable is not the same failure as losing WiFi. Also fixed the FAKE: it never reported fw or group though the firmware always has, so a page showing them looked broken here while working on hardware - it now reads FW_VERSION from the firmware header so the two cannot drift. check_modsite_glance.py broken three ways; break 2 survived at first because the check only ever drove the IDLE state, so the branch that names a running sequence was untested - it now drives both\nA4-2: done   2026-08-19 10:42  touches=WebUI.h  \u2014 fix two dead error handlers in WebUI.h the plan said two dead handlers; the code had five empty catches, four of which lied, and rule 1 says the code wins. Fixed: the re-login after a password change was ignored, so Setup opened on a DEAD session with every card visible and every action refused; mustChange was read from a body that might not parse, and swallowing it left false - a board still carrying the shipped password silently stopped asking anyone to change it, the security prompt turning itself off; logout was fired and forgotten directly under a comment promising it reaches the board; and a failed file list left an empty dropdown, which already means nothing on this card. The fifth catch was legitimate and now says catch(e){okCur=false;} so the check can keep an absolute guard - catch(e){} must not appear in that file at all, the pattern that produced all four. Four sabotages, all caught. Firmware builds nong, lift and blank\nA4-3: done   2026-08-19 10:42  touches=WebUI.h  hw=1 board on WiFi, to verify the half that runs on the chip (built and PC-verified) \u2014 a way back to the hub from the board site the trap is not the link, it is guessing the address: several PCs at a venue run this hub, so a wrong link lands someone on a stranger's control panel. Two cases only. Through the hub the page IS the hub, so / is right by construction. Reached directly, the board offers the hub that last identified itself - the hub sends X-Mice-Hub on every request it makes, from one helper so its two callers cannot disagree - and offers NOTHING when none has. The board keeps it in RAM with a five minute TTL and the header is character-checked before it becomes an href, since it arrives from the network. Five sabotages, all caught. The board-side memory runs on the chip and cannot be driven from a PC: that half is held to the source and the check says so. Gate 2055/0, patches fw 0039-0040 and hub 0011-0012\nA4-4: todo touches=WebUI.h  \u2014 both pinout images, chosen by board type and transport\nA4-5: todo touches=WebUI.h  \u2014 the five states everywhere they are missing\nA4-6: todo touches=studio,WebUI.h  \u2014 Studio writes its status into a hidden tab\n\n# A5  Connection state\nA5-1: todo touches=hub.html,main.py,WebUI.h  \u2014 one connection state machine, errors say what to do next\n\n# A6  Tools and help\nA6-1: todo touches=hub.html needs=A2-1  \u2014 Tools and Help reachable from every screen\nA6-2: todo touches=hub.html,help.html needs=A6-1  \u2014 contextual help per tool, regrouped by task\nA6-3: todo touches=hub.html  \u2014 broken registry no longer reads as no tools\n\n# A7  Firmware structure\nA7-1: done   2026-08-18 23:15  touches=firmware  \u2014 one applyJointSetting helper (13 copies of 3 idioms) THE COUNT WAS WRONG: the plan said 13 copies of 3 idioms, the code has 5 of each. GEAR PULSE RANGE SERVO HZ each parsed the <1-10|name|ALL> word themselves, each carried its own copy of the sentence ERR joint 1-10 name or ALL, each wrote its own loop guard, and each built its own all-or-name label for the reply. The error message is the part that matters: it tells a person what they may type instead, so five copies means changing the rule in one command and leaving four telling people something untrue. Now one small struct JointSel - covers(i), label(), and one jointSelHelp() sentence - with selectJoints() parsing it once. No class hierarchy, readable in one screen. check_joint_select.py, broken two ways. It says plainly what it cannot prove: driving those commands over the wire was tried and REMOVED because the fake module is a Python stand-in that never implemented them, so a green run means the rule is written once, not that the five commands ran\nA7-2: todo touches=firmware  \u2014 one joint-field validator table, used by JCFG too\nA7-3: todo touches=firmware  \u2014 one per-joint field descriptor (five hand-kept serialisers)\nA7-4: todo touches=firmware  hw=1 board + RGB strip + speaker + SD card (AudioPlayer reads its files off it) \u2014 [hw] moving RgbStrip and AudioPlayer needs a board with a strip and a speaker to prove nothing regressed. Original task: move RgbStrip and AudioPlayer into modules/lift/ SD CARD ARRIVED and is NOT mounting, measured on the nong 2026-08-19: INFO says sd=false after a clean reboot, FILES / answers ERR bad dir, while FILES /moves and /seq answer with empty arrays - so the board has a filesystem and no card. First suspect is the format: cards above 32 GB ship exFAT and the ESP32 driver reads FAT32 only. Second is seating. Not a firmware fault until the card is FAT32 and still refused SD CARD NOW MOUNTS - it was the wiring, not the format and not a pin clash. After the user rewired it: INFO says sd=true and caps includes sd. Then a full file round trip over RS485, which had never been tested outside fakes: FBEGIN, FDATA with 37 bytes of base64, FEND, the listing showing s=37, FREAD returning the identical bytes, FDEL removing it. So the file transfer, the card and the bus all work together on real hardware. Note for anyone testing by hand: FDATA takes BASE64 and reads only the first word, so plain text silently writes a few bytes rather than failing - my first attempt wrote 3 bytes from hello and looked like a bug in the firmware when it was a bug in the test\nA7-5: todo touches=firmware  hw=1 board + 2 servos minimum (a whole nong is better) + SD card for a saved show \u2014 [hw] SequencePlayer drives real servos; a fake cannot show a move still runs. Original task: SequencePlayer stops hard-coding lift and nong verbs\nA7-6: todo touches=firmware  \u2014 call nongmath::ease, delete dead maxDelta, generate the CFG key list\nA7-7: todo touches=firmware,docs  hw=1 ESP32-CAM (COM17, CH340 shield) + SD card (SNAP writes the photo to it) \u2014 [hw] SNAP on a cam build needs the ESP32-CAM. Original task: SNAP can never work on a cam build: fix it or fix four documents PREMISE DISPROVED ON HARDWARE 2026-08-19: SNAP works on a cam build. The board answered OK captured 14671 bytes (SNAP <name> saves it, /api/cam.jpg shows it). What is left of this task is only the SD half - SNAP <name> writes to a card, and there is no card yet - plus checking whether the four documents describe it correctly. Do not rebuild what already works The SD half is now unblocked too: the card mounts on the nong and files write over the bus. SNAP <name> on the cam board can now be tested, which was the only part of A7-7 still waiting for hardware\n\n# A8  The device API\nA8-1: todo touches=main.py  \u2014 /api/robot/* and /api/usb/cmd become thin shims over /api/dev/*\nA8-2: todo touches=main.py  \u2014 usb_close must not close a handle it failed to lock\nA8-3: done   2026-08-18 23:15  touches=studio  \u2014 Studio cannot parse an @peer dev Studio parsed a dev by cutting off usb: and splitting on colon, so usb:COM7@far-nong came out as a PORT NAME - it offered a serial port called COM7@far-nong in the dropdown and failed to reach a module that was perfectly reachable. wifi:<ip>@peer went the same way, into the IP box. Now the peer is split off first and SENT: with a peer the command goes to the unified endpoint, which the hub already understands and turns into REACH <peer> <command> down the cable, and without one nothing about the ordinary cable path changes. That is what makes one USB cable enough for a whole installation with no venue WiFi. check_studio_peer.py asserts on the WIRE, not the page - break 2 parses the peer correctly and never sends it, which the page reports as connected. Two harness bugs found doing it: the studio prelude still reported through rawCmd so a connection bug silenced the very check meant to find it, and fake_serial.wire is (when, command) pairs which my join treated as strings - the same mistake was sitting in check_hub_auth, only working because nothing had reached the wire\nA1-5: done   2026-08-19 03:24  touches=main.py,hub.html  \u2014 hub LOGINS become USERS, added from The hub has accounts now, added and removed from Settings, with the module's own rules copied so the two cannot disagree: names 1-20 of letters digits _ and -, passwords 8-32 with no spaces, at most 16, and the LAST account can never be removed. Two deliberate differences from the module: the hub HASHES (the module stores in the clear) and every account gets its OWN salt, so two people who pick the same password do not share a hash. The risky part was migration, not the feature - a hub that already had a password must not be locked out by the upgrade, so the old single-password file is read as an account named mice and a login that sends no user name still works. Driven against a real old-format file in check_hub_users. A wrong USER NAME gives exactly the same answer as a wrong password, because different wording is how a stranger learns which names are real.\n                the web, the same shape the module already has. The user asked\n                for this on 2026-08-18 after being locked out: make like\n                authority have the user and pass, make can add user like the\n                module. firmware/src/core/UserStore.h is the model to copy \u2014\n                names 1-20 of letters, digits, _ and -, passwords 1-32 with no\n                spaces, at most 16 users, and the LAST user can never be\n                removed so nobody can lock everyone out. The hub should hash\n                them, which the module does not. Half-started and then parked\n                on the user's instruction to follow the plan first; what DID\n                land is the one-password gate (A1-3) plus a readable\n                hub_password.txt, so nobody is locked out in the meantime\nA9-6: done   2026-08-18 23:15  touches=studio  \u2014 the splitter between the 3D view and the side\n                panel is 6px wide (style.css:71) and cannot be grabbed on a\n                laptop touchpad. Reported from a real machine, 2026-08-18: it\n                is too short and I cannot drag. 6px is under every pointer\n                target guideline and there is no keyboard way to resize either,\n                so a person who cannot hit it cannot make the panel wider at\n                all. Widen the HIT area without widening the visible line \u2014 a\n                transparent grab zone either side is the usual answer \u2014 and\n                give it arrow-key resizing. Belongs with A9 because it is the\n                same failure as the rest: it works on the machine it was built\n                on and not on the machine someone uses\nA3-8: todo    [hw] the PC's internet drops while it is sharing its hotspot,  hw=1 board + a second device (a phone will do) + the laptop that shows the fault\n                seen again 2026-08-18. Three causes were already found and\n                fixed on 2026-08-06 \u2014 a stale legacy ICS pairing, WiFi channel\n                roaming, and Radmin VPN pinned to ifMetric 1 stealing the\n                route \u2014 so this is either a fourth cause or one of those coming\n                back. Needs the real machine and a second device to reproduce;\n                `ping -S 192.168.137.1` is NOT a valid client test. Original\n                task:\n# A9  Every screen \u2014 phone, tablet, laptop, desktop, TV\nA9-1: todo touches=qc/  \u2014 the device list QC measures stops at 1920. Add the ones people\n                really use: a 320px phone, a 1280x800 laptop, a 1440p desktop, a\n                4K desktop, and a TV at 1920 and 3840. Today check_responsive\n                covers 360 / 390 / 768 / 1024 / 1920 only, so nothing above a\n                desktop monitor is measured at all and a TV is nobody's\n                breakpoint\nA9-2: todo touches=mice.css needs=A9-1  hw=a TV or a large screen, to judge three-metre legibility (optional) \u2014 a TV is watched from three metres, not thirty centimetres.\n                Everything is legible there or it is not usable: type, spacing\n                and touch/focus targets need a step up past about 2000px rather\n                than the same 15px body text stretched across two metres of\n                glass. One rule in mice.css, not five copies\nA9-3: todo touches=mice.css,hub.html,WebUI.h  hw=a TV remote or a keyboard-only run, to prove arrow-key operation (optional) \u2014 a TV has no mouse and no touch: it is driven by a remote, which\n                is arrow keys and Enter. Every screen must be reachable and\n                operable in DOM order with a visible focus ring, no hover-only\n                affordance, and no control that can only be reached by dragging.\n                The focus ring exists already (mice.css); the ORDER and the\n                hover-only cases have never been checked\nA9-4: todo touches=mice.css,hub.html,WebUI.h,studio needs=A9-1  \u2014 stop the wide screens wasting themselves. The hub caps at\n                1400px above 1500px wide, Studio's panel grows once at 1600px,\n                and the module site caps at 1400px \u2014 three different answers to\n                the same question. Decide one behaviour for a big screen and\n                apply it everywhere: either a readable column with the space\n                given to the CONTENT that benefits, or a second column\nA9-5: todo touches=qc/ needs=A9-1  \u2014 prove it, do not eyeball it. Extend the responsive check to\n                assert, at every width above, that nothing scrolls sideways,\n                that no PANEL clips its own controls (the check added in A0-9),\n                that body text is at least the size the distance needs, and that\n                every interactive element meets the target size for that input\n                kind. A screenshot at one width proves one width\n# A10  Colour and joy, without losing the dark\nA10-1: done   2026-08-19 13:18  touches=shared/web/themes.css  \u2014 ONE file holds every colour in the project. Asked for themes.css written: Midnight (default), Carnival, Daylight, designed by Gemini Pro and measured here - its Daylight accent failed 4.5:1 and was changed. mice.css now names no colour at all; both are served joined as one stylesheet by the hub and from board flash, so no page changed. Shared mice.js discovers themes from the stylesheet, so adding one is that block and nothing else. IN STAGING, NOT PROMOTED\n                directly, 2026-08-19: a file to change only the theme or colour,\n                so a new one can be added later without reading anything else.\n                Every theme is one block in shared/web/themes.css; mice.css keeps\n                the SHAPE (radius, spacing, type scale) and stops naming colours\n                at all. The test is the project's usual one: can someone add the\n                next theme without opening any other file\nA10-2: done   2026-08-19 13:18  touches=mice.css needs=A10-1  \u2014 mice.css hands its palette over. Today :root holds both the\n                colours and the shape, so changing a colour means editing the\n                stylesheet every page depends on. After this it holds shape only,\n                and every colour comes from the theme file\nA10-3: done   2026-08-19 13:18  touches=hub.html,WebUI.h,studio needs=A10-1  hw=a phone, to look at the themes on a real screen (optional) \u2014 a theme picker that READS the file rather than shared/web/mice.js is served by the hub at /mice.js and compiled into board flash by gen_tables, like mice.css - so the hub, the module website and the help page all get one file. The picker DISCOVERS themes from document.styleSheets, so adding one is a block in themes.css and nothing else; the check adds a theme and fails if it does not appear. The model panel caught two things I missed: --ty-lift was byte-identical to --ok in all three themes, and the switcher hardcoded the default theme name. Gate 2122/0\n                repeating it: the list of themes is discovered from the blocks in\n                themes.css, so adding a block is the whole job - no second list\n                to keep in sync, the same rule the module and command registries\n                already follow. Remembered per browser and applied before the\n                first paint, or the old colours flash up and swap\nA10-4: todo touches=themes.css needs=A10-1  \u2014 three to start with, and dark stays the default because a\n                bright screen spills light into the show area: Midnight (what\n                the project looks like today), Carnival (the colourful dark the\n                user asked for - more hue, same contrast) and Daylight (a bright\n                room, or a TV with sun on it)\nA10-5: done   2026-08-19 13:18  touches=mice.css,hub.html needs=A10-2  \u2014 the joy pass. Colour that MEANS something stays\n                meaning it (module type, ok/warn/err) and decoration comes from\n                the theme's own accents, so a joyful theme cannot make a warning\n                look like a decoration. Never colour as the only signal\nA10-6: todo touches=qc/ needs=A10-3  \u2014 prove both halves. The check ADDS a theme block to the file,\n                reloads, and expects it in the picker (if it does not appear, the\n                list is being repeated somewhere); and it computes the contrast\n                of text on background for every shipped theme, because colourful\n                is where readable usually goes to die\n\n# A11  More it can do, without more to learn\nA11-1: todo touches=hub.html  \u2014 find a module by typing. One box that filters on name, id\n                and type, no modes and no syntax. It is the feature that stops\n                mattering how long the list gets\nA11-2: todo touches=hub.html needs=A11-1  \u2014 pin the ones you actually use; pinned first, the rest\n                below. A venue rig has four modules that matter and a shelf of\n                spares that do not\nA11-3: todo touches=hub.html,WebUI.h  \u2014 one STOP that stops everything, in reach on every screen.\n                It exists per module today, which is the wrong place to look for\n                it when something is moving that should not be\nA11-4: todo touches=hub.html  \u2014 the last thing you opened, one click away on Home. Nobody\n                opens a control panel to read it\nA11-5: todo touches=hub.html,WebUI.h  \u2014 the keyboard: / to search, ? for the list of shortcuts,\n                Esc closes anything open. Every one of them discoverable from\n                that list, so it is not folklore\nA11-6: todo touches=hub.html  \u2014 one button that copies a plain-text diagnostics bundle -\n                what this PC is, what it found, what firmware each board runs.\n                The thing anyone is asked for first when they report a problem\n\n# A12  The plan, and other documents, read themselves\nA12-1: done touches=docs/PLAN.html,tools/plan.py  \u2014 the plan updates while it is open, with nothing to\n                press. Asked for 2026-08-19. It is opened as file:///E:/..., and\n                a file:// page may not fetch a file beside it - the browser\n                gives it a null origin - but it MAY load a script, so plan.py\n                publishes docs/plan_state.js and the page pulls it in every four\n                seconds and redraws in place. No reload, so the scroll position\n                and any text being read stay put. Proved by putting a marker\n                ONLY in the published file and watching Edge render it. Also\n                fixed: a stale .staging/docs/PLAN.html from the day before,\n                which said work was still to do that had landed - it is now a\n                page that says so and points at the real one. check_plan_live.py,\n                four sabotages, including one for the obvious later tidy-up of\n                replacing the script tag with fetch, which works over http and\n                does nothing at all for the person reading the file\nA12-2: todo touches=tools/livedoc.py needs=A12-1  \u2014 the same for any other document. Asked for\n                2026-08-19: make it like this for another plan too. One tool\n                that renders a markdown file to a page that keeps itself\n                current, with the same script-tag trick and a checklist counter\n                when the document has one. The rule that keeps it honest: the\n                markdown is the only source and the HTML is generated, never\n                edited, or there are two copies of one document drifting apart\nA12-3: todo touches=docs/PLAN.html  \u2014 which hardware each change needs, on the change itself.\n                Done for the tasks that need it (hw= on the line, a chip in the\n                table and on the next-up card); this is the rest: keep it true\n                as tasks are added, and say so in the packing list when a part\n                stops being needed\n\n# A13  The camera board, and the bus that now exists\nA13-1: done   2026-08-19 13:22  touches=firmware hw=1 ESP32-CAM (here)  \u2014 the camera on this board is not one the firmware NOT A CAMERA FAULT AND NOT A WIRING FAULT. The board was running fw 1.0.0; flashed with the current mice_cam build it says [cam] ready, qvga, PSRAM NO and SNAP captured 14671 bytes. So the sensor is supported, the ribbon is fine and the 5V rail is adequate - the whole symptom was stale firmware. The diagnostic that reads the sensor id over SCCB stays in CamModule anyway: it is what turns Detected camera not supported into either an id to support or nothing answered on SCCB, and those need opposite fixes. Board is id 89, name undefined, type cam\n                accepts: it boots with camera_probe(): Detected camera not\n                supported. Find out which sensor it actually has - the driver\n                reports the id it read - and support that type, from the\n                registry rather than by adding a branch. Nothing about A7-7\n                means anything until a camera initialises\nA13-2: done   2026-08-19 15:02  touches=firmware,main.py hw=RS485 + nong id 67 (here)  \u2014 RS485 is real now, so the things a FOUND A REAL BUG THE FAKES COULD NEVER SHOW. On the bench bus the nong (id 67) answers a broadcast after 1344 ms, because RS485Bus.cpp:62 staggers replies by id*20 ms to avoid collisions - and main.py:1618 listens for 0.8 s. So the hub discovers only ids up to 40, while default ids run 1-247: most boards are invisible on a real bus. The hub sees the two USB boards and not the nong, with no error, which reads as an empty bus. Measured, not theorised: rts and dtr make no difference, the reply is 1344 ms every time, and 67 x 20 = 1340 MEASURED, and one half is still unmeasured, said plainly. The OLD stagger is confirmed on real hardware: the nong on id 67 answers a bus broadcast at 1344 ms, which is 67 x 20. The NEW bounded stagger cannot be measured on the bus yet, because the only board on the bus is the nong and it cannot be reflashed - it is on RS485, not USB, and esptool needs a direct UART. A board on USB answers a broadcast in 3-6 ms whatever its id, because the USB path does not stagger at all; that is the transport, not the fix. So: hub side measured and fixed, firmware side written and checked against a fake that now staggers, and honestly not yet proved on a real bus NOW MEASURED ON THE REAL BUS, both halves. The nong (id 67) was reflashed over USB and put back on RS485. Broadcast reply BEFORE: 1344 ms, which is 67 x 20. AFTER: 188, 195, 193 and 196 ms across four runs, against a design figure of (67 % 24) x 10 = 190. So the bounded stagger is real, not just modelled - every board now answers a broadcast inside the hub's floor whatever its id. The same board also reports chip B4BFE91C5A74 over the bus, so A3-4 is verified through RS485 as well as over USB. Why the flash had to happen over USB is worth keeping: esptool needs the ROM bootloader, entered by holding GPIO0 low during reset via DTR and RTS, and RS485 has no such lines to EN or IO0\n                fake bus could never show can be measured: an addressed command\n                to #67, a broadcast that several boards answer, and what happens\n                when two answers collide. The hub already speaks this protocol;\n                what has never been proved is that it survives a real bus\nA13-3: done   2026-08-19 13:37  touches=firmware hw=nong id 67 with WAIST and R-EL-R (here)  \u2014 the two bench servos are the first real VERIFIED ON REAL HARDWARE, and confirmed by the user watching it: JOINT WAIST 100 over RS485 answered OK WAIST=100.0 T=83ms, POSE? showed slot 9 change to 100.0, and the servo audibly and visibly turned. Returned to 90 the same way. The first real joint this project has moved through the bus. One correction worth keeping: the servo is R_EL_R with underscores, not R-EL-R - and the board says so itself, listing all ten names in the error, which is A7-1's one-sentence rule earning its keep\n                joints this project has had. Prove a move still runs after the\n                A7 refactors: send it to a named joint, watch it turn, and\n                assert on what came back rather than on what was sent\n\n# A14  One login across the PCs, later\nA14-1: todo touches=hub_auth.py,hub.html  \u2014 PARKED ON PURPOSE 2026-08-19, at the user's word: pair\n                test first, the login later. The want is one account that works\n                on every PC. Facebook or Google sign-in cannot do it: OAuth needs\n                internet AT LOGIN TIME, a registered redirect URL and HTTPS,\n                while this hub runs plain HTTP on a LAN address that changes, at\n                a venue that often has no internet - so on show day nobody could\n                log in to stop a robot. The offline options are: copy the account\n                file between hubs (one compromised PC becomes all of them), a\n                pairing code shown on screen like pairing a TV (no internet, and\n                the trust is deliberate), or a secret derived from the\n                installation group name (already exists for the hotspot, and is\n                only as strong as a group name someone chose). Recommendation\n                when this is picked up: the pairing code, because it is the only\n                one where a person decides, on both machines, that these two PCs\n                belong together\n\nA13-4: todo touches=main.py,hub.html hw=2 PCs on one WiFi + boards (here)  \u2014 asked for 2026-08-19: a second PC on the MEASURED THE LIMIT 2026-08-19: a second PC that only opens the hub in a browser can never share its USB port. The Web Serial API is the only route a page has to a serial port and it requires a secure context - the hub is plain HTTP on a LAN address, so the browser refuses. Two real options remain: run the hub on that PC (then /api/allmods aggregates its cables, which already works), or put the board on WiFi so no PC is involved at all - the board answers the hub directly and the second PC is only power. The second is better whenever the WiFi allows it, because it removes a machine from the chain rather than adding software to it Home counted ROWS across three lists, so a board on a cable was counted twice - a bench of four boards reported nine, six on USB and three on WiFi. It counts BOARDS from the merged list now, and says reachable rather than on, because a board with a cable and WiFi is one board with two ways in\n                same WiFi as the hub host can open the hub and see inside, but\n                it does not get the boards that host has on ITS cables. So the\n                host must SHARE its cables across the WiFi, not only its pages.\n                Half of this exists - /api/allmods aggregates every hub's\n                modules and rewrites the address as hub:IP/usb:COMx, and\n                parse_dev routes a command through the holding hub - so the\n                work is to find what is actually missing rather than to build it\n                again: the module list on the other PC, the module site opened\n                through the far hub, and Studio pointed at a board on a cable\n                that is not its own\nA13-5: done   2026-08-19 17:25  touches=main.py hw=2 PCs (here)  \u2014 the pair test itself. On this bench the hub found no MEASURED 2026-08-19: the two PCs can reach each other perfectly - this PC opened TCP to 10.123.98.148:8642 and its hub answered host=MSI with a cable holding id 42 lab-pair. Discovery is the ONLY thing missing: this PC is 10.126.226.203 and sweeps its own /24, so a hub on 10.123.98.x is unreachable by a sweep however long it runs, and mDNS cannot help either because multicast is link-local. So the fix is not a better sweep, it is letting a person NAME a hub - an address typed once, remembered, and probed like any other DONE 2026-08-19 and measured with both PCs: the two hubs are linked and this one lists the other's board as hub:10.123.98.148/usb:COM9 id 42 lab-pair host MSI, stable across three refreshes. Four fixes were needed. The finder ignored also_ask unless a patient probe was configured, so a typed address was remembered, shown as known, and never once contacted. probe_hub waits 0.7 s, which is right for sweeping 254 addresses and too tight for a named hub that is busy scanning its own cables - it appeared and vanished between refreshes until named hubs got a patient 5 s ask. /api/hubs/add was left ungated, which would have let anyone on the network point this hub at a machine of their choosing. And an apostrophe in a JavaScript string - this PC's own network - ended the string and killed the whole page script, which is why tabs stopped working\n                other hub at all, and the first suspect is not the code: the\n                WiFi is 10.126.226.x, a campus-style network, and those very\n                often isolate clients from each other so no PC can reach any\n                other. Prove which it is before changing anything - a hub that\n                cannot be reached is not a hub that was not found. If the\n                network does isolate, the answer is the hotspot path that\n                already exists, not a fix to discovery\n\nA13-6: todo touches=firmware,main.py hw=2 boards + RS485 (here)  \u2014 firmware over the BUS, since esptool cannot do\n                it. Asked 2026-08-19: why can we not upload over RS485 or WiFi.\n                WiFi already works - the board has OTA and writes the spare app\n                slot, so a failed update keeps the firmware it has. RS485 cannot\n                use esptool at all: the ROM bootloader is entered by holding\n                GPIO0 low during reset through DTR and RTS, and a differential\n                pair has no such lines. But the running firmware already accepts\n                a file over the bus (FBEGIN FDATA FEND) and already knows how to\n                write an OTA slot, so the two joined would be a real bus update\n                with no ROM bootloader involved. The board must be able to\n                refuse a bad image and keep running the old one, exactly as OTA\n                does now\nA13-7: done   2026-08-19 15:12  touches=firmware hw=2+ boards (here)  \u2014 pairing with no venue WiFi, which is what the user ROOT CAUSE FOUND ON HARDWARE, and it is a chicken and egg. WebPortal.cpp:1162 reads worthLooking = onRelay_ || peers_.count() > 0, so a board hunts for neighbours only if it is ALREADY on a relay or ALREADY knows a peer. A board that just booted with no venue WiFi knows nobody, so it never scans, so it never finds anybody. Measured with three real boards - nong 67 and lift-test 85 both in group mice-show, lift 1 in other-company, all three in ap-only with apclients=0 - and PEERS on each returns only itself. This is exactly the always show not linked in A3-5, and no fake could produce it. The guard was added because the scan fired every 60 s for nothing when there were no neighbours; the fix has to keep that cost down while letting a GROUPED board look, since its AP password is derived from the group and can only open a group-mate's network FIXED AND MEASURED ON THREE REAL BOARDS. One line at WebPortal.cpp:1162 - worthLooking = onRelay_ || peers_.count() > 0 - meant a board hunted for neighbours only if it already had one, so a cold room of boards stayed alone forever. Now a board that BELONGS TO A GROUP and has no network also looks, which keeps the cost the old guard was protecting (an ungrouped board still never scans for nothing) while making the scan one that can succeed: its AP password is derived from the group, so the only network it can open is a group-mate's. Before: three boards, apclients=0, each PEERS listing only itself. After: lift-test 85 joined the nong's access point (state=online ssid=nong ip=192.168.4.2 rssi=-29), the nong shows apclients=1, and PEERS on BOTH lists the other. The negative control held: lift id 1 in group other-company sat in the same room, never joined, apclients=0. One small thing to chase later - the nong reports its OWN ip as 192.168.4.2 in PEERS while hosting on 192.168.4.1\n                actually asked for: when the usual network is not there, the\n                modules should find each other by themselves. The pieces exist -\n                a module falls back to hosting its own hotspot, and the password\n                for it is derived from the GROUP so only modules in the same\n                installation can join - but nothing has ever been measured with\n                two real boards. Prove: with the network absent, one board hosts,\n                the others join, and PEERS lists them on both sides\n\n# A15  Getting it onto another PC\nA15-1: todo touches=MiceHub.spec,main.py  \u2014 ONE FILE, not one folder. Asked 2026-08-19: run the app\n                on another PC without the whole repo. Today the exe cannot:\n                MiceHub.spec has datas=[] and main.py reads HERE/web and\n                HERE.parent/shared/web off the disk, so a copied exe serves an\n                unstyled hub with no Studio. Bundle the web folders into the\n                exe and read them from sys._MEIPASS when frozen. Then the answer\n                to how do I run it elsewhere is: copy one file. The check is the\n                honest one - run the built exe from an EMPTY folder and open\n                every page\nA15-2: todo touches=git needs=A15-1  \u2014 branches, in the shape asked for: develop carries this\n                work, main stays what is known good. NOT a third branch holding\n                a copy of the app - a branch that duplicates files drifts from\n                the one it copied, and this project has already been bitten by a\n                stale copy of the plan. If a source-only bundle is still wanted\n                after A15-1, generate it with a script so it cannot drift\nA13-8: todo touches=none hw=a module on WiFi + a phone (here)  \u2014 prove a PHONE can update a robot. Asked\n                2026-08-19: if the module is on the hub's WiFi, can another PC\n                or a phone flash it, since the hub holds the .bin. Reading the\n                code says yes and nothing has ever tested it: the browser never\n                touches firmware, it posts to /api/ota ON THE HUB and the hub\n                pushes the bytes, so a phone needs only a browser and a login.\n                The two limits to prove as well: the HUB must reach the module -\n                a module on a board hotspot the hub is not on cannot be updated\n                from anywhere - and /api/ota is gated, so an unauthenticated\n                phone is refused\n\nA15-5: todo touches=main.py,hub.html needs=A15-1  \u2014 a button that updates the hub itself. Asked\n                2026-08-19: click to update when a new one is uploaded to git.\n                The hub asks GitHub for the newest release, says what version it\n                is running and what is offered, and replaces itself only when a\n                person presses it. It must refuse cleanly with no internet -\n                which is the normal case at a venue - and it must never replace\n                itself while a flash or a show is running\n\nA15-4: todo touches=README.md needs=A15-2  \u2014 a README that says how to get JUST the part you need,\n                asked for 2026-08-19. Three routes, each with the exact command:\n                the one file (download the exe from the release and run it),\n                one branch without the history (git clone --branch <name>\n                --depth 1 <url>), and one FOLDER out of the repo without the\n                rest (git sparse-checkout). Written for someone standing at\n                another PC who has not read anything else, so every command is\n                complete and none of them assumes this repo is already cloned\n\nA15-3: todo touches=git  \u2014 commit today's work. Nothing is committed yet: the RS485\n                census fix, the themes, the QR and mDNS, remote flash, the model\n                panel and every measurement from the bench are all working tree\n                on one machine. That is the single biggest risk on this project\n                right now, and it is not a technical one\n\nA13-9: done   2026-08-19 17:25  touches=main.py hw=4 cables incl RS485 (here)  \u2014 MY OWN REGRESSION, found on the bench: THREE bugs, one symptom - the hub listed 3 of the 4 boards it can reach. (1) probe_usb_all gave every port ONE shared 7 s budget and the RS485 census floor pushed a four-cable bench past it: raised to 20 s, with the measurement written beside it - an ordinary port answers in 3 s, a port with a bus behind it in 4.7 s. (2) modules_here skipped a port whose DIRECT probe found nothing, and that threw away its bus children - which is every module behind an RS485 adapter, because an adapter has no board of its own. My own code from A3-4, and the fake never showed it because the fake port has both a module and a bus. (3) a board answers the broadcast on its own USB line too, so a board whose direct probe was missed looked like a module behind an adapter - the camera was listed as rs485 COM18. It is asked once more, plainly: an adapter has nothing to answer with, a board does. All four boards now list correctly, each labelled by the route it is really on\n                the hub lists 3 of 5 boards, and the nong on the RS485 bus is\n                the one missing. Measured 2026-08-19 against the real cables -\n                COM23 alone answers in 4.7 s and DOES find the bus module, the\n                other three take about 3 s each, and the full scan takes 10.8 s\n                and returns 2 boards. The cause is mine: probe_usb_all gives\n                every port ONE shared 7 second budget, and the 1.6 s census\n                floor I added for the bus pushed the total past it, so whichever\n                ports are waited on last are reported as no answer. A single\n                port scan still works, which is why this looked like a bus\n                problem and is not one\n\nEND"};
