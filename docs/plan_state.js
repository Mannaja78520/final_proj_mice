window.PLAN_STATE={"stamp": "2026-09-16 16:15:20", "raw": "STATE\n# RUNNING: QC finished: 150 passed, 0 failed, started 2026-09-16 16:15\n\n# A0  Groundwork \u2014 the duplication that makes every later area harder\nA0-1: done 2026-08-18  \u2014 this file\nA0-2: done 2026-08-18  \u2014 docs/architecture/ written from three explorations\nA0-3: done 2026-08-18  \u2014 promoted 2026-08-18, QC 1574/0. Also fixed two things found on the\n                way: promt.md was about to be destroyed by promote (now in\n                SKIP_FILES, it is a live log not source), and a hub could forward\n                to itself over HTTP because all of 127.0.0.0/8 is loopback\n                (now short-circuited by is_self, guarded by a check that was\n                proven to discriminate)\nA0-4: done 2026-08-18 09:46  \u2014 promoted 2026-08-18, QC 1607/0. shared/web/mice.css is the only\n                :root in the product; every page links /mice.css and the BOARD\n                serves that same URL from flash (gen_tables.py compiles the file\n                into web/MiceCss.h), so a module looks the same over WiFi and\n                over USB. Radii unified on 10/14/18/999, the four caption\n                classes became one rule, soft tints and --code-bg are now\n                derived with color-mix instead of five hand-picked hexes, and\n                color-scheme:dark fixes the native checkbox/slider/scrollbar.\n                check_design_system.py guards it (broken twice, failed twice).\n                Two things found on the way: promote.py was about to push a QC\n                run's settings_shared.json over the real one (now in SKIP_FILES,\n                guarded by check_no_data_loss), and the three snapshot patchers\n                now carry the shared stylesheet, or an old page would restore\n                against today's tokens\nA0-5: done 2026-08-18 10:31  \u2014 promoted 2026-08-18, QC 1611/0. Deleted. It was a second module\n                page: 295 lines, six cards duplicated from WebUI.h, and 8 joint\n                sliders where a nong has 10, so WAIST and SHRUG were unreachable\n                through it. Nothing linked to it and it already redirected away\n                in JavaScript. /module.html survives as a 302 that carries\n                ?port=&id= across to /mod?dev=usb:PORT:ID, so an old bookmark\n                still lands on the right module; bare /module.html goes to the\n                hub instead of to a page addressed at nothing. The hub's AP-mode\n                message no longer sends people to it. check_hub_api guards both\n                halves (broken twice, failed twice). promote.py does not delete,\n                it warns \u2014 the file had to be removed from the real tree by hand\nA0-6: done 2026-08-18 12:26  \u2014 one logging helper in firmware. src/core/Log.h\n                holds the tag list ONCE (X-macro generates both the enum and the\n                names); mlog::line formats the whole line and issues exactly ONE\n                Serial.write, which cannot be split by another task \u2014 the bench\n                bug where a WiFi event glued its log onto the end of a reply.\n                34 call sites moved off Serial.println/printf across 5 files,\n                including the SSID scan that built one line out of twelve\n                writes. A log can no longer contain a newline (it would read as\n                two replies) and an over-long line is cut with a visible ...\n                The formatting is Arduino-free so the PC tests assert the real\n                bytes: 5 new native tests, 25 total. check_logging.py guards\n                that nothing goes around the helper (broken twice, failed\n                twice). Flash unchanged at 66.5%. COMMANDS.md carries the\n                one-write rule. NOT tested on hardware\nA0-7: done 2026-08-18 17:18  \u2014 RS485Bus::send. MY PREMISE WAS WRONG, and\n                the code said so: I parked this as an RS485 copy of the A0-6\n                logging bug, but Serial2 is written in exactly ONE function,\n                send() serialises on sendMtx_ (created because the async web\n                task and loop() both send), and the driver enable is held across\n                the whole frame. Nothing can get between those two writes. The\n                hole that WAS open is narrower: the lock is applied as\n                `if (sendMtx_)`, so a board that never created it would skip\n                locking in silence and let two tasks interleave halves of two\n                frames onto a bus every module obeys. That is now reported once\n                instead of ignored. check_rs485_frame.py pins the three things\n                that make a frame whole \u2014 one writer, the lock held ACROSS the\n                writes, the driver raised and lowered once \u2014 broken three ways,\n                failed three ways. Builds; not tested on hardware\nA0-8: done 2026-08-18 17:18  \u2014 the reply path (main.cpp emitLine) and the log path now both\n                build one buffer and write once, in two different places. Share\n                one, so the rule cannot be half-kept. Found while doing A0-6\nA0-9: done 2026-08-18 17:18  \u2014 Studio's side panel overflows 32px at its default width: the\n                joint number box sits past the edge and the panel grows a\n                scrollbar. Pre-existing, measured against the unmodified tree\n                while doing A0-4, NOT caused by it\nA0-11: done 2026-08-18 13:50  \u2014 the browser checks FLAKE under full-suite load: check_responsive\n                reported measurements missing inside run_qc.py, then passed 35/0\n                on its own; an earlier full run failed transports/ui which then\n                passed 194/0. A suite that fails randomly is a suite people stop\n                believing, and promote.py is gated on it. Give the browser\n                checks a retry, or more settle time, and prove it by running the\n                full suite several times.\n                FOUND IT: qcMark in qc/lib/browser.py swallowed a failed fetch\n                with a bare catch, so a dropped report vanished and the check\n                failed with measurements missing. It now retries four times.\n                check_qc_reports.py drives it against a network that refuses the\n                first two requests: before the fix R1 and R2 were lost, after it\n                all six arrive. Fixed as a BLOCKER \u2014 it was stopping A0-6 from\n                promoting, not a later cleanup.\n                REOPENED and finished during A1-3: the dropped report was only\n                HALF of it. The other half was drivers measuring on a stopwatch\n                \u2014 a fixed setTimeout, then count \u2014 which under load measured a\n                page that had not finished and reported a bug that did not\n                exist. Three more full runs failed on three more checks. Fixed\n                three ways: check_responsive now AWAITS each report instead of\n                firing 25 at a port that takes one caller at a time; a shared\n                qcWaitFor(condition, deadline) went into both browser preludes\n                and check_network_tab uses it; and the wait deadline went from\n                25s to 150s of grace, which costs nothing because the wait ends\n                the instant a page says done. Two consecutive full runs green\nA0-10: done   2026-08-19 02:31  touches=hub.html,WebUI.h,studio  \u2014 three tab systems, all called showTab, with different class One tab component in mice.css, used by the hub, the module website and Studio. They had three: the hub outlined its active tab while the other two filled theirs, so the hub looked like a different product from the screens it opens. Filled won - two of three already used it, it survives a dark room and an angled screen, and an outline differs from an unselected tab only by colour, which fails a screenshot and a colourblind reader. Every id, data attribute and handler kept; .mtab stays as a NAME beside .tab because scripts and QC select on it. check_tabs.py measures the COMPUTED colours of all three in a real browser. Two weak assertions of mine were exposed by breaking it: all-three-match is satisfied by three apps that are equally wrong, and the report separator was being eaten by the mark sanitiser so the comparison could never fail.\n                names and different active styles: the hub is an outline tab,\n                the module site and Studio are filled. Left alone by A0-4\n                because unifying them changes markup QC drives\n\nA0-12: done   2026-08-18 23:15  touches=qc/,promote.py  \u2014 a change costs two full QC runs: one by hand before promoting and\n                one inside promote.py as the gate. The gate must stay FULL \u2014 three\n                times on 2026-08-18 a change broke a check that looked unrelated\n                (a stylesheet broke Studio's panel, the logging helper broke\n                check_wifi_live, the login gate broke check_modsite_tabs) \u2014 but\n                the SECOND run is pure waste. Make promote reuse a run that just\n                passed on an unchanged tree, or run the fast checks first and\n                stop at the first failure so a broken change fails in seconds\n                instead of nine minutes. ~20 of the 50 checks launch a real\n                browser and that is nearly all of the time.\n                TWO SUITES CANNOT RUN AT ONCE TODAY, which is why waiting hurts:\n                browser.kill() kills every Edge carrying the QC tag, so a second\n                run murders the first one's browser, and every run reports\n                through the same fake serial port into one shared qc_marks list.\n                Fixing that \u2014 a tag per run, and marks owned by the run that\n                produced them \u2014 is what would let two changes be verified in\n                parallel instead of queueing\nA0-13: done   2026-08-19 02:49  touches=qc/,promote.py  \u2014 MORE THAN ONE .staging, so two changes can be verified at the PROVEN, not asserted: two browser-driven suites ran at the same time in two trees, both green - 45/0 in 47.6s and 45/0 in 48.3s, 49s wall clock for what was 96s serial. Each run takes its identity from its own pid: browser.TAG carries it (kill matches on the tag, so a shared one meant one run killed the other's browser mid-measurement - which looks exactly like a flaky check), and _hub_port starts at 8700+(pid%40)*25 (two runs on 8700 could have one tree's hub answer the other tree's request, which does not fail loudly, it silently tests the wrong tree). promote.py takes --staging DIR. Promoting stays SERIAL on purpose: both trees copy into the same real tree and two promotes touching one file is a lost edit - the failure that ate the settings_shared.json fix earlier.\n                same time instead of queueing behind one 10 minute suite. The\n                user's idea, 2026-08-18, and two staging trees are two PROCESSES\n                so they do not share qc_marks or the fake port \u2014 the collision\n                that bit us inside ONE process does not apply. Four things stop\n                it today, all small and all verified:\n                  * qc/lib/browser.py TAG is the constant MICEQCBROWSER and\n                    kill() matches on it, so one run kills the other run's\n                    browser \u2014 needs a per-process suffix;\n                  * qc/lib/qc.py _hub_port starts at 8700 in both, so the two\n                    trees fight over ports and one hub can answer the other's\n                    requests \u2014 base it on the pid or take a free port;\n                  * the QC auth file is named mice_qc_auth_<port>.json, so the\n                    same port means the same file (falls out of the port fix);\n                  * promote.py hardcodes STAGING = MAIN/.staging \u2014 needs to take\n                    the tree as an argument.\n                Then promote them one at a time, since both copy into the same\n                real tree and two promotes touching one file is a lost edit.\n                HOW MANY. Measured on this machine 2026-08-18: 24 cores, 31 GB\n                RAM, 47.8 GB free, and a staging tree is 648 MB of which 624 MB\n                is firmware/.pio \u2014 the SOURCE is about 24 MB. So disk allows\n                ~70 trees and the CPU is not the constraint either. The real\n                ceilings are different:\n                  * LOAD MAKES BROWSER CHECKS FLAKE. Every intermittent failure\n                    chased on 2026-08-18 was load-induced timing. Twenty suites\n                    at once is twenty times that load on the checks that were\n                    just stabilised, so the number must rise only as the flake\n                    causes are proven fixed;\n                  * INDEPENDENT WORK RUNS OUT. Parallel trees only help when the\n                    tasks touch different files, and this plan does not have\n                    twenty such streams \u2014 every A2 task edits hub.html and every\n                    A4 task edits WebUI.h. Three to five is what actually exists;\n                  * and one build cache would save most of the disk anyway:\n                    point PLATFORMIO_CORE_DIR at a shared folder so the trees do\n                    not each carry 624 MB of the same libraries.\n                So: start at THREE, measure wall-clock and how many runs come\n                back green first time, and raise it only if that stays clean.\n                Twenty trees on a suite that flakes under load is twenty times\n                the debugging, not twenty times the work\n\n# A1  Authentication \u2014 there is none today\nA1-1: done   2026-08-19 00:57  touches=WebPortal.cpp  hw=1 board (done on nong id 85, 2026-08-19) \u2014 [hw] runs in WebPortal.cpp on the board; fake_wifi is a Python stand-in, so auth there can be read but never executed. Original task: enforce auth on the board's mutating HTTP routes VERIFIED ON REAL HARDWARE 2026-08-19, nong id 85 lift-test at 192.168.137.72. Before: GET /api/cmd?c=SET NAME hacked returned 200 and ran, from any device on the WiFi. After: 401. Reading stays open (PING, INFO, /api/status, the page, the stylesheet) because the hub's own discovery probes /api/status and gating it would hide every board. STOP stays open on purpose - refusing a stop is the dangerous answer. What is gated comes from config/commands.json (query / safety flags compiled into COMMAND_DOCS), not a list in C++, so allowing a new command is one entry and an unknown command fails CLOSED. Two registry faults found on the way and fixed: CAM was declared query:true while its help said it changes the sensor, and PIN VALID now needs a login because PIN is correctly declared as changing - only PIN? is a query.\nA1-2: done   2026-08-19 01:28  touches=WebUI.h,UserStore.cpp  hw=1 board (done on nong id 85, 2026-08-19) \u2014 [hw] same: the board's login card and first-login change live in WebUI.h + UserStore on the chip. Original task: stop printing the default password; force a change on first login VERIFIED ON HARDWARE 2026-08-19, nong id 85. The card no longer prints manny/12345678 and neither does the served page source - view-source is not a lock. A board still carrying the shipped password answers mustChange:true and the website will not open Setup until it is changed. Checked BY VALUE, not by a flag set when the account is created: this very board had been in service since before the firmware, so a flag would have called it set up and never asked - the boards most likely to still have the shipped password are exactly the ones a flag misses. Caught before flashing. Minimum length went 1 to 8 (what the shipped one already was) and changing it TO 12345678 is refused, because forcing a change is theatre otherwise. Measured on the board: login mustChange:true, 7 chars refused, change-to-itself refused, real change accepted, old password then 401.\nA1-3: done 2026-08-18 13:50  \u2014 hub login for mutating routes. main_python/\n                hub_auth.py holds the gate; nothing that CHANGES anything is\n                reachable without a session, and everything that only reads\n                still is. Three things the obvious design would have got wrong:\n                (1) the rule is not POST \u2014 /api/usb/cmd, /api/robot/cmd and\n                /api/robot/delete are GETs that move servos and delete files, so\n                the gate is a LIST of what changes something; (2) STOPPING is\n                never gated (NEVER_GATED), because a password prompt while an arm\n                is about to hit someone is a safety failure \u2014 starting a show\n                needs a login, ending one never does; (3) a module on another PC\n                is gated by THAT hub, since a session here means nothing there,\n                and the error says to go and log in on that PC. Password is\n                PBKDF2 + random salt, generated on first run and printed only in\n                the hub's own console on the PC, never on a page and never over\n                the network; five wrong tries locks that caller for a minute and\n                no one else. check_hub_auth.py drives it from outside with no\n                session, 57 assertions, and asserts on fake_serial.wire \u2014 a gate\n                that answers 401 AFTER the arm moved has protected nothing.\n                Broken three ways, failed three ways. Gemini designed the login\n                card; its .banner.info/.warn moved into mice.css (a banner is a\n                shared component) and the lockout countdown now reads the real\n                number from the hub instead of a hardcoded 30s\nA1-4: done 2026-08-18 15:19  \u2014 remote flash confirms, naming the PC. The old\n                question was a browser confirm() box naming only a COM port or\n                an IP \u2014 neither tells you WHICH board in the room you are about\n                to overwrite. It is now an in-page panel that names the board\n                (name, id, the type it runs now), what it becomes and whether\n                that is a change, how it is reached, and THIS PC BY NAME \u2014\n                several machines at a venue run the same hub on the same\n                network, so the wrong browser tab is the easy mistake. Cancel is\n                the prominent button and under the thumb; overwrite is styled\n                destructive and has to be reached for; Escape cancels; the\n                dialog takes focus. Being a real panel and not confirm() also\n                means QC can READ it. check_flash_confirm.py clicks Cancel and\n                then asks the HUB whether a write began \u2014 a confirm that closes\n                while the write has started is worse than none. Broken twice\n                (drop the PC name; make cancel answer yes), failed twice.\n                Gemini designed it; .banner.ok moved into mice.css, its\n                id=flashBusy collided with hub.html's flashBusy() function, and\n                it needed Escape and focus handling. check_ota asserted the\n                literal confirm( and now asserts the panel instead\n\n# A2  The hub's screens\nA2-1: done   2026-08-18 23:15  touches=hub.html  \u2014 hub nav: Home Modules Firmware Network Tools Settings Help Seven destinations. Help is a LINK not a tab. Home and Settings are real, not placeholders: Home counts what the scans already found so it cannot disagree with Modules, and repeats any error banner from the other tabs; Settings says who you are on which PC. Firmware is an honest empty state pointing at where flashing lives today. Gemini designed the bar; its placeholder copy named internal task ids on a user-facing page, and its filled-active-tab idea is A0-10 territory. Also fixed a real bug found by looking: .banner is display:flex which BEATS the hidden attribute, so hidden banners showed as empty coloured bars - mice.css now makes [hidden] win everywhere. check_hub_nav.py drives all seven in a browser; broken three ways, and the third break exposed a weak assertion of mine plus a report truncated at the first newline.\nA2-2: done   2026-08-19 04:30  touches=hub.html needs=A2-1  \u2014 a real Firmware section, separate from module management one screen writes firmware; unbuilt types show their build command; a finished write sets the board type; flashBusy restored (the cleanup deleted it and the first Write threw) one Firmware screen writes over cable or WiFi; unbuilt types are listed with the exact pio command; a finished write sets the board type. Two real faults were found by rebuilding the checks rather than renaming their selectors: the cleanup deleted flashBusy so the FIRST click on Write threw before anything was sent, and check_hub_nav measured the panel before it had painted. check_flash_type now drives fwWrite itself with the network stubbed and asserts on the SET TYPE that reached the board - broken four ways, and three of those assertions were too weak on the first try. Gemini reviewed it and claimed the OTA path must poll /api/ota and that want is shared state; both are false, checked against the code. Patch 0006, gate 1917/0\nA2-3: done   2026-08-19 04:59  touches=hub.html,mice.css  \u2014 .danger styling so destructive actions look destructive danger is a component in mice.css now, with hover and disabled states and a compact icon button, not a colour pasted per control. The unlink button never had it; the remove-account control had a red spelled out in an inline style, which is how a design system quietly stops being one - and it is now a button, since a control that ACTS is not a link. check_danger.py holds the line: the rule must be built from --err with no literal colour, every destructive handler in the list must carry it, and no control may hand-roll it. Broken three ways plus a CONTROL edit that had to keep passing\nA2-4: done   2026-08-19 04:59  touches=main.py,hub.html needs=A2-2  \u2014 POST /api/flash/remote, image sent over the network the image travels, not the command. POST /api/flash/send hands this PC's built image to the hub holding the cable; POST /api/flash/remote receives it, unpacks it and runs esptool THERE; GET /api/flash/at proxies that PC's progress back, because the browser cannot ask another hub directly - the session cookie is SameSite=Lax so a cross-origin POST carries nothing. New decision, not in the plan: the operator types a login for the other PC in the confirm panel, used once and never stored, rather than a shared token that would leave every PC holding a credential that overwrites boards on every other PC. Found while writing the check: drain() swallowed whatever Content-Length claimed, so one header saying 900 MB blocked the hub on a REFUSED request, before any route saw it - capped at 4 MB. Eight sabotages, all caught. Gate 1950/0, patch 0007\nA2-5: done   2026-08-19 04:30  touches=hub.html  \u2014 move the pio build command off the first screen the pio run build command is off the first screen. It lives on Firmware now, built from the envs that are actually missing rather than one written into the page\n\n# A3  Discovery and identity\nA3-1: done   2026-08-18 23:15  touches=main.py  \u2014 one discovery layer: Discovery to Registry to ConnectionManager main_python/discovery.py holds the sweep once: Sweep walks one /24 in parallel, Finder is one kind of thing with a shelf life and an optional patient second pass. scan_hubs and scan_modules were the same loop with different probes and TTLs, and the divergence was real - the module scan had learned to be patient with a board that answers late and the hub scan never did. A new kind of thing is now ONE line, driven in the check by adding a fake one and watching it find something. Two design points worth keeping: probes are passed as wrappers so they are looked up WHEN THEY RUN (binding the function at construction silently breaks every monkey-patch, including the QC ones), and Finder.forget() is public so a check does not poke internals. check_discovery.py broken four ways - the fourth was a CONTROL, a harmless edit that must not fail, and it DID, exposing that I had asserted the spelling of a line rather than the object it produces\nA3-2: done   2026-08-19 05:00  touches=main.py  \u2014 one subnet sweep helper (scan_hubs and scan_modules are the same loop) no new work: A3-1 already did it. scan_hubs and scan_modules are each one line over discovery.Finder (main.py:255 and :321), which is exactly what this task asked for. Verified in the code rather than rebuilt\nA3-3: done   2026-08-19 08:56  touches=main.py,hub.html needs=A3-1  \u2014 QR at /api/qr and mDNS mice.local both halves, both stdlib-only because the hub is copied to a venue laptop and run. main_python/qr.py is a QR encoder written from the standard - GF(256) Reed-Solomon, zig-zag placement, all eight masks scored - served as SVG at /api/qr and shown on the Network screen beside the address, redrawn each time the tab opens because a laptop that moves between venue WiFi and its own hotspot gets a new IP. main_python/mdns.py answers mice.local; if Bonjour or avahi already holds 5353 the hub still runs and the page says the name is unavailable rather than printing one that answers to nothing. Verifying a thing that cannot be eyeballed: the check reads the picture BACK and also checks the parity by Reed-Solomon syndromes, which is different arithmetic; both constant tables are then verified by computation, not recall - all eight format strings match the BCH(15,5) code, and every version's capacity matches the free-module count from my own placement. Thirteen sabotages, all caught, one of which found a real fault: the responder sent the multicast copy FIRST, so on a network where multicast fails - the exact case mDNS exists for - the throw meant the unicast answer was never sent. Gate 1994/0, patch 0008\nA3-4: done   2026-08-19 09:47  touches=main.py  \u2014 stable identity, dedupe a board that is on WiFi and cable boards report chip - the ESP32 eFuse MAC - in INFO and /api/status, from the same source already used for the default name and bus id, so it adds no new truth. modules_here() merges cables, RS485 children and WiFi into one list per board with every route; the module screen renders one row saying reachable on COM9 and over WiFi. The dangerous direction is the opposite of the reported bug: two DIFFERENT boards out of one box share a default id, so merging on the id would send a command meant for one arm to the other - without a chip the id counts only with the type, and a board with a chip is never merged with one without. Six sabotages, all caught; the sixth needed a new case, a half-awake board reporting its chip and nothing else, which must not blank the name the cable already knew. Firmware builds mice_nong and mice_blank; COMMANDS.md documents the field. One check was found to be testing nothing: check_ui_states broke /api/scan, which the list no longer calls. Gate 2016/0, patches 0009 and fw 0038 VERIFIED ON HARDWARE 2026-08-19. A board flashed with the current firmware answers INFO with chip 6825DD163688 - the eFuse MAC, twelve hex digits, exactly as designed. Before the flash the same board reported no chip at all (fw 1.0.0), which is what every board on this bench still does\nA3-5: done   2026-08-21 16:58  touches=firmware  hw=2 boards; RS485 transceivers x2 + A/B wires for the bus half (the hotspot half needs no wiring) \u2014 [hw] RS485 peers and hotspot peers are board-to-board; nothing on a PC carries a real bus. Original task: carry group on RS485 and hotspot peers (they always show not linked) UNBLOCKED 2026-08-19: RS485 is wired and working - broadcast on COM23 answered @67 PONG 67 nong nong through a MAX485. Both halves are now doable The hotspot half is DONE - see A13-7, fixed and measured with three boards. What is left of A3-5 is the RS485 half: carrying the group to modules behind the bus WAITING FOR THE BENCH - the boards are back 2026-08-20 around 13:00 (leaving 12:30, at the bench 13:00). Blocked rather than todo so the page stops offering it as the next thing to do while there is no hardware; it goes back to todo the moment the boards are on the desk. hardware measured 2026-08-20: the RS485 rig is now PC -> CH340+MAX485 dongle (COM23) -> pair -> board 1 (lift, on COM9's cable) and board 67 (nong, NO usb cable of its own). Broadcast #* PING on the dongle answers @1 and @67, so both boards share the one pair. Board 67 is the only module here reachable ONLY over RS485, which makes it the honest test target for anything bus-shaped. Note the bootstrap limit that is inherent and not a bug: board 67 answers 'no such command here: FWBEGIN', so it cannot be updated over the bus until it carries the updater once - the same one-time step an STM32 needs from its factory bootloader. RS485 HALF DONE AND MEASURED ON REAL HARDWARE 2026-08-21. Rig: PC -> CH340+MAX485 dongle on COM23 -> board 67 (nong, group mice-show, no USB cable of its own). Two bugs, both invisible to 3120 passing checks because no fake had ever been shaped like the real rig. (1) The hub asks every bus module for INFO to learn its wifi address and chip, and INFO has always carried the group - it was read and dropped. Before and after on the same board minutes apart: unfixed hub returns id 67 name nong type nong ip 192.168.4.1 chip B4BFE91C5A74 and NO group key at all, so the Network tab read not linked; fixed hub returns the same row with group mice-show. The wrong reading was not cosmetic - ticking a board that says not linked re-sends GROUP to a board that already had one, and the AP password is derived from the group name. (2) /api/mine skipped a whole port when the cable had no module answering directly, and the rs485 loop sat inside that skip - so a plain USB-to-RS485 DONGLE listed nothing and the one board reachable only over the bus was invisible to the module list and to every other hub. Measured: probe_usb_port(COM23) found board 67 while /api/mine returned four modules without it; after the fix /api/mine returns five, including usb:COM23:67 group mice-show. check_bus_group holds both, driven by a new fake DONGLE_PORT (a cable with no board of its own, only boards behind it); three sabotages, all caught RS485 RE-CHECKED 2026-08-21 19:49 after a report that the bus from the nong was disabled. IT IS NOT: broadcast #* PING answered @1 lift and @67 nong from the dongle (COM23), from the NONG's own cable (COM29), and from the lift's cable (COM9) - so the nong both answers on the bus and sees the lift, in both directions. The one board off the shared pair is 85 lift-test (COM26): it hears only itself, so its A and B are not wired to the others. Nothing to fix in software; it is one pair of wires\nA3-6: done   2026-08-19 10:07  touches=hub.html  \u2014 hide IP, port, COM, dBm behind Advanced addresses, cable ports and signal strengths sit behind one switch in Settings, remembered per browser. It is ONE rule in shared/web/mice.css - .tech and body.adv .tech - not a decision repeated per page, and every page in the project already links that sheet, so the board site can join the same switch with one class. The module id stays visible with the switch off even though it shares a span with the address: it is how a board is addressed on the bus and how two identical arms on a shelf are told apart. Six sabotages; the one that got through first was the lazy fix - hiding the whole span, which takes the id with it - because innerText of a display:none element falls back to its raw text, so asking the hidden span reports it as visible. Measured on the rendered row instead. Gate 2031/0, patch 0010\n\n# A4  The module's own website\nA4-1: done   2026-08-18 23:15  touches=WebUI.h  \u2014 board site: what am I, at a glance The board reports fw, group and seq.running in the same status payload it always sent, and none of them reached the screen. The header now shows the firmware version - the first question anyone asks about a board behaving oddly, previously only reachable by sending INFO on a console - plus the group when it is in one, and a line saying what it is DOING and HOW you reached it: playing a named sequence or not running anything, over WiFi or over the cable through the hub. That distinction matters because unplugging a cable is not the same failure as losing WiFi. Also fixed the FAKE: it never reported fw or group though the firmware always has, so a page showing them looked broken here while working on hardware - it now reads FW_VERSION from the firmware header so the two cannot drift. check_modsite_glance.py broken three ways; break 2 survived at first because the check only ever drove the IDLE state, so the branch that names a running sequence was untested - it now drives both\nA4-2: done   2026-08-19 10:42  touches=WebUI.h  \u2014 fix two dead error handlers in WebUI.h the plan said two dead handlers; the code had five empty catches, four of which lied, and rule 1 says the code wins. Fixed: the re-login after a password change was ignored, so Setup opened on a DEAD session with every card visible and every action refused; mustChange was read from a body that might not parse, and swallowing it left false - a board still carrying the shipped password silently stopped asking anyone to change it, the security prompt turning itself off; logout was fired and forgotten directly under a comment promising it reaches the board; and a failed file list left an empty dropdown, which already means nothing on this card. The fifth catch was legitimate and now says catch(e){okCur=false;} so the check can keep an absolute guard - catch(e){} must not appear in that file at all, the pattern that produced all four. Four sabotages, all caught. Firmware builds nong, lift and blank\nA4-3: done   2026-08-19 10:42  touches=WebUI.h  hw=1 board on WiFi, to verify the half that runs on the chip (built and PC-verified) \u2014 a way back to the hub from the board site the trap is not the link, it is guessing the address: several PCs at a venue run this hub, so a wrong link lands someone on a stranger's control panel. Two cases only. Through the hub the page IS the hub, so / is right by construction. Reached directly, the board offers the hub that last identified itself - the hub sends X-Mice-Hub on every request it makes, from one helper so its two callers cannot disagree - and offers NOTHING when none has. The board keeps it in RAM with a five minute TTL and the header is character-checked before it becomes an href, since it arrives from the network. Five sabotages, all caught. The board-side memory runs on the chip and cannot be driven from a PC: that half is held to the source and the check says so. Gate 2055/0, patches fw 0039-0040 and hub 0011-0012\nA4-4: done   2026-08-20 02:09  touches=WebUI.h  \u2014 both pinout images, chosen by board type and transport The camera half is unverified on hardware in the same way: the drawn diagram is correct by construction (rendered from the table the firmware compiles) but nobody has opened it next to a real board yet.\nA4-5: done   2026-08-20 02:57  touches=WebUI.h  \u2014 the five states everywhere they are missing Landed 2026-08-20. NOT verified on a board: the stale banner is held by check_stale_pose against the source and the generated page, and mice_nong compiles, but nobody has watched it appear by pulling a cable. One intermittent to watch: check_responsive crashed once in a parallel gate and passed alone three times and in a full parallel run afterwards - unexplained, not fixed.\nA4-6: done   2026-08-20 03:58  touches=studio,WebUI.h  \u2014 Studio writes its status into a hidden tab\n\n# A5  Connection state\nA5-1: done   2026-08-20 11:09  touches=hub.html,main.py,WebUI.h  \u2014 one connection state machine, errors say what to do next\n\n# A6  Tools and help\nA6-1: done   2026-08-20 11:10  touches=hub.html needs=A2-1  \u2014 Tools and Help reachable from every screen ALREADY DONE when picked up 2026-08-20, no code needed. hub.html:212 has 'Help' as a real link inside the tab strip, which is visible on every screen, and Tools is one of the six tabs. check_hub_nav already holds both - its docstring records the original fault, that help was two clicks deep inside Tools. Verified rather than rebuilt: the code is the source of truth, not the plan.\nA6-2: done   2026-08-20 11:27  touches=hub.html,help.html needs=A6-1  \u2014 contextual help per tool, regrouped by task\nA6-3: done   2026-08-20 11:18  touches=hub.html  \u2014 broken registry no longer reads as no tools Taken before A6-2 on purpose: it is the same failure mode as the bug found today (A6-4), where a broken Tools list was indistinguishable from an empty one, so the context is fresh. A6-2 is a design task and comes next.\n\n# A7  Firmware structure\nA7-1: done   2026-08-18 23:15  touches=firmware  \u2014 one applyJointSetting helper (13 copies of 3 idioms) THE COUNT WAS WRONG: the plan said 13 copies of 3 idioms, the code has 5 of each. GEAR PULSE RANGE SERVO HZ each parsed the <1-10|name|ALL> word themselves, each carried its own copy of the sentence ERR joint 1-10 name or ALL, each wrote its own loop guard, and each built its own all-or-name label for the reply. The error message is the part that matters: it tells a person what they may type instead, so five copies means changing the rule in one command and leaving four telling people something untrue. Now one small struct JointSel - covers(i), label(), and one jointSelHelp() sentence - with selectJoints() parsing it once. No class hierarchy, readable in one screen. check_joint_select.py, broken two ways. It says plainly what it cannot prove: driving those commands over the wire was tried and REMOVED because the fake module is a Python stand-in that never implemented them, so a green run means the rule is written once, not that the five commands ran\nA7-2: done   2026-08-20 11:39  touches=firmware  \u2014 one joint-field validator table, used by JCFG too VERIFIED ON HARDWARE 2026-08-20 13:0x, board #85 on COM25 running the new mice_nong: RANGE 1 999 and JCFG 1 15 18 500 2400 200 999 50 30 150 both answer 'ERR servo travel must be 60..360 deg (180 normal, 270 wide)' - byte for byte identical, including the hint JCFG used to lack.\nA7-3: done   2026-08-20 11:53  touches=firmware  \u2014 one per-joint field descriptor (five hand-kept serialisers) NOT verified on a board: mice_nong compiles and check_joint_fields holds the table, but nothing has written a real nong_cal.yaml or read a real status JSON since. At the bench, confirm the YAML still has all eleven keys and that a saved calibration still loads after a reboot - the field ORDER changed (it now follows the table) and loadCal parses by key, so it should not matter, but that is the assumption to test. The status JSON deliberately still omits trim and neutral, exactly as before. VERIFIED ON HARDWARE 2026-08-20: LIMIT? on board #85 still reports min max gear_pinion gear_gear pulse_min pulse_max max_dps servo_range frame_hz. The YAML half is still unverified - board #85 has no SD card (sd:false); the nong with the card is #67, behind the RS485 bus.\nA7-4: done   2026-08-25 14:14  touches=firmware  hw=1 board + RGB strip + speaker + SD card (AudioPlayer reads its files off it) \u2014 [hw] moving RgbStrip and AudioPlayer needs a board with a strip and a speaker to prove nothing regressed. Original task: move RgbStrip and AudioPlayer into modules/lift/ SD CARD ARRIVED and is NOT mounting, measured on the nong 2026-08-19: INFO says sd=false after a clean reboot, FILES / answers ERR bad dir, while FILES /moves and /seq answer with empty arrays - so the board has a filesystem and no card. First suspect is the format: cards above 32 GB ship exFAT and the ESP32 driver reads FAT32 only. Second is seating. Not a firmware fault until the card is FAT32 and still refused SD CARD NOW MOUNTS - it was the wiring, not the format and not a pin clash. After the user rewired it: INFO says sd=true and caps includes sd. Then a full file round trip over RS485, which had never been tested outside fakes: FBEGIN, FDATA with 37 bytes of base64, FEND, the listing showing s=37, FREAD returning the identical bytes, FDEL removing it. So the file transfer, the card and the bus all work together on real hardware. Note for anyone testing by hand: FDATA takes BASE64 and reads only the first word, so plain text silently writes a few bytes rather than failing - my first attempt wrote 3 bytes from hello and looked like a bug in the firmware when it was a bug in the test WAITING FOR THE BENCH - the boards are back 2026-08-20 around 13:00 (leaving 12:30, at the bench 13:00). Blocked rather than todo so the page stops offering it as the next thing to do while there is no hardware; it goes back to todo the moment the boards are on the desk. RgbStrip+AudioPlayer moved core/ -> modules/lift/, self-includes fixed, platformio.ini filters simplified; all 4 envs build SUCCESS, quick QC 2688/0. Strip/speaker NOT wired yet - bench proof stays with A7-5 (need-to-test). LANDED 2026-08-25 14:20 - quick 2688/0, gate 3617/0, promoted (patch 0007). Bench proof of strip/speaker = A7-8.\nA7-5: done   2026-08-21 16:58  touches=firmware  hw=1 board + 2 servos minimum (a whole nong is better) + SD card for a saved show \u2014 [hw] SequencePlayer drives real servos; a fake cannot show a move still runs. Original task: SequencePlayer stops hard-coding lift and nong verbs SEQUENCE FROM SD PLAYED ON REAL SERVOS 2026-08-19, the whole chain the project was started for. A YAML sequence was written to /moves/bench-check.yaml over the RS485 bus (FBEGIN, two FDATA chunks of base64, FEND, listed at 231 bytes), then MOVE bench-check.yaml played it. POSE? sampled while it ran shows the board interpolating: R_EL_R and WAIST went 90 to 96/94 to 110/105, through 90, down to 70/75, and back to 90. Read back FROM THE BOARD while playing, not assumed from the file. So the SD, the file transfer, the YAML parser, the sequence player and two real servos all work together WAITING FOR THE BENCH - the boards are back 2026-08-20 around 13:00 (leaving 12:30, at the bench 13:00). Blocked rather than todo so the page stops offering it as the next thing to do while there is no hardware; it goes back to todo the moment the boards are on the desk. DONE AND MEASURED ON REAL SERVOS 2026-08-21, board 67 (nong, COM29, SD + RS485), flashed with the rebuilt mice_nong. The if-else chain in SequencePlayer::execStep is gone: every step key is declared on the command it calls in config/commands.json and gen_tables.py writes core/SeqSteps.h (15 steps), so adding one is an entry in the registry and no C++ at all. Only wait and cmd stay in code - they are not commands. THE BUG THE PANEL FOUND, fixed and proven: the old chain sent a bare HOME and threw the step value away, so a nong sequence could not time its own return. Measured on the bench: HOME alone answers OK home T=80ms, HOME T 2500 answers OK home T=2500ms, and the SEQUENCE step - home: T 2500 now takes 2093 ms where it used to take 80. A four-step file (joint WAIST 70 T 600, wait 300, home T 700, joint WAIST 110 T 600, home T 700) played end to end with each step waiting for the last: 90 to 70 to 90 to 110 to 90, read back from the board with POSE? while it ran. TWO QC CHECKS WERE SCRAPING THE CHAIN and would have failed a working player - check_contracts and check_sequences both grepped SequencePlayer.cpp for key == the step name; both now read the registry. check_seq_steps is new: it drives the claim by adding a step to the registry, running the generator, and finding it in the table with no C++ touched. Four sabotages, four caught - one of them silent at first, which is how the check gained its assertion that the player really uses the wait flag\nA7-6: done   2026-08-20 12:03  touches=firmware  \u2014 call nongmath::ease, delete dead maxDelta, generate the CFG key list VERIFIED ON HARDWARE 2026-08-20: CFG nosuchkey 1 on board #85 answers with all 15 keys from ConfigStore::MAP - encoder leds speed speed_mms stages stage_mm mm_per_rev counts_per_rev max_rpm counts_per_stage volume speed_dps max_dps link peer.\nA7-7: done   2026-08-19 18:37  touches=firmware,docs  hw=1 ESP32-CAM (COM17, CH340 shield) + SD card (SNAP writes the photo to it) \u2014 [hw] SNAP on a cam build needs the ESP32-CAM. Original task: SNAP can never work on a cam build: fix it or fix four documents PREMISE DISPROVED ON HARDWARE 2026-08-19: SNAP works on a cam build. The board answered OK captured 14671 bytes (SNAP <name> saves it, /api/cam.jpg shows it). What is left of this task is only the SD half - SNAP <name> writes to a card, and there is no card yet - plus checking whether the four documents describe it correctly. Do not rebuild what already works The SD half is now unblocked too: the card mounts on the nong and files write over the bus. SNAP <name> on the cam board can now be tested, which was the only part of A7-7 still waiting for hardware SNAP REGRESSED ON THE BENCH, and it looks like power rather than code. Earlier today, same firmware, SNAP answered OK captured 14671 bytes. Now it answers ERR capture failed every time, including straight after a reboot, while the camera itself still initialises - the boot line says [cam] ready, qvga, PSRAM NO. What changed is the bench, not the build: there are now four boards and a servo rig on this PC's USB. An ESP32-CAM draws about 300 mA in bursts while clocking the sensor and compressing, far more than at init, so a rail that is fine for booting can fail a capture. TO TEST: give the CAM its own port or a powered hub, then SNAP again. SNAP <name> correctly answers ERR no sd card - that board has no card, and the honest error is the right one DONE ON HARDWARE, and the user's hunch was right: it is a different board. The camera reports pid 0x5640 - an OV5640, not the OV2640 everything assumed. Two faults followed from that. CAM never reported the sensor at all, so a board that initialised and then failed every capture gave no clue which part it was; it now says board, sensor, pid and version. And the start frame size was ONE hardcoded value, SVGA, measured on an OV2640, applied to every board while the frame buffer stayed allocated for the init size - on a five megapixel part with no PSRAM that is exactly how a capture fails after a clean init. The size is now a table keyed on the sensor, and an unmeasured part keeps the size its buffer was allocated for. Result: 6 captures out of 6, ~3.2 KB each, where before it failed every time. Also asked for: work with boards nobody has identified yet. The pin MAP is now a table too - ai-thinker, esp-eye, m5stack, m5stack-wide, ttgo-journal - tried in order until the camera answers. Proved by deliberately breaking the compiled-in pins: the board came back saying board=ai-thinker and captured 2986 bytes\n\n# A8  The device API\nA8-1: done   2026-08-20 12:18  touches=main.py  \u2014 /api/robot/* and /api/usb/cmd become thin shims over /api/dev/*\nA8-2: done   2026-08-20 12:04  touches=main.py  \u2014 usb_close must not close a handle it failed to lock ALREADY FIXED when picked up 2026-08-20 - main.py:557 puts an unlockable port back instead of closing it, with the reason written down. What was missing was a guard: the dangerous version is SHORTER and looks tidier, which is what a cleanup does. check_usb_close now drives the real function with a lock somebody else holds.\nA8-3: done   2026-08-18 23:15  touches=studio  \u2014 Studio cannot parse an @peer dev Studio parsed a dev by cutting off usb: and splitting on colon, so usb:COM7@far-nong came out as a PORT NAME - it offered a serial port called COM7@far-nong in the dropdown and failed to reach a module that was perfectly reachable. wifi:<ip>@peer went the same way, into the IP box. Now the peer is split off first and SENT: with a peer the command goes to the unified endpoint, which the hub already understands and turns into REACH <peer> <command> down the cable, and without one nothing about the ordinary cable path changes. That is what makes one USB cable enough for a whole installation with no venue WiFi. check_studio_peer.py asserts on the WIRE, not the page - break 2 parses the peer correctly and never sends it, which the page reports as connected. Two harness bugs found doing it: the studio prelude still reported through rawCmd so a connection bug silenced the very check meant to find it, and fake_serial.wire is (when, command) pairs which my join treated as strings - the same mistake was sitting in check_hub_auth, only working because nothing had reached the wire\nA1-5: done   2026-08-19 03:24  touches=main.py,hub.html  \u2014 hub LOGINS become USERS, added from The hub has accounts now, added and removed from Settings, with the module's own rules copied so the two cannot disagree: names 1-20 of letters digits _ and -, passwords 8-32 with no spaces, at most 16, and the LAST account can never be removed. Two deliberate differences from the module: the hub HASHES (the module stores in the clear) and every account gets its OWN salt, so two people who pick the same password do not share a hash. The risky part was migration, not the feature - a hub that already had a password must not be locked out by the upgrade, so the old single-password file is read as an account named mice and a login that sends no user name still works. Driven against a real old-format file in check_hub_users. A wrong USER NAME gives exactly the same answer as a wrong password, because different wording is how a stranger learns which names are real.\n                the web, the same shape the module already has. The user asked\n                for this on 2026-08-18 after being locked out: make like\n                authority have the user and pass, make can add user like the\n                module. firmware/src/core/UserStore.h is the model to copy \u2014\n                names 1-20 of letters, digits, _ and -, passwords 1-32 with no\n                spaces, at most 16 users, and the LAST user can never be\n                removed so nobody can lock everyone out. The hub should hash\n                them, which the module does not. Half-started and then parked\n                on the user's instruction to follow the plan first; what DID\n                land is the one-password gate (A1-3) plus a readable\n                hub_password.txt, so nobody is locked out in the meantime\nA9-6: done   2026-08-18 23:15  touches=studio  \u2014 the splitter between the 3D view and the side\n                panel is 6px wide (style.css:71) and cannot be grabbed on a\n                laptop touchpad. Reported from a real machine, 2026-08-18: it\n                is too short and I cannot drag. 6px is under every pointer\n                target guideline and there is no keyboard way to resize either,\n                so a person who cannot hit it cannot make the panel wider at\n                all. Widen the HIT area without widening the visible line \u2014 a\n                transparent grab zone either side is the usual answer \u2014 and\n                give it arrow-key resizing. Belongs with A9 because it is the\n                same failure as the rest: it works on the machine it was built\n                on and not on the machine someone uses\nA3-8: blocked  2026-08-20 23:56  [hw] the PC's internet drops while it is sharing its hotspot,  hw=1 board + a second device (a phone will do) + the laptop that shows the fault\n                seen again 2026-08-18. Three causes were already found and\n                fixed on 2026-08-06 \u2014 a stale legacy ICS pairing, WiFi channel\n                roaming, and Radmin VPN pinned to ifMetric 1 stealing the\n                route \u2014 so this is either a fourth cause or one of those coming\n                back. Needs the real machine and a second device to reproduce;\n                `ping -S 192.168.137.1` is NOT a valid client test. Original\n                task:\n# A9  Every screen \u2014 phone, tablet, laptop, desktop, TV\nA9-1: done   2026-08-20 14:11  touches=qc/  \u2014 the device list QC measures stops at 1920. Add the ones people IN PROGRESS, unpromoted, stopped 12:30. Done in .staging: the device list went 5 -> 9 (320 phone, 1280x800 laptop, 1440p, 4K, both wall-screen sizes); the driver now also measures how much of the screen a page USES, not only overflow; rgb.html gained wall-screen scaling. All green in check_responsive and sabotage-proved both ways, but NOT through the full gate. Next session: run tools/land.py --done A9-1 first.\n                really use: a 320px phone, a 1280x800 laptop, a 1440p desktop, a\n                4K desktop, and a TV at 1920 and 3840. Today check_responsive\n                covers 360 / 390 / 768 / 1024 / 1920 only, so nothing above a\n                desktop monitor is measured at all and a TV is nobody's\n                breakpoint\nA9-2: blocked  2026-08-20 23:56  touches=mice.css needs=A9-1  hw=a TV or a large screen, to judge three-metre legibility (optional) \u2014 a TV is watched from three metres, not thirty centimetres. WAITING FOR THE BENCH - the boards are back 2026-08-20 around 13:00 (leaving 12:30, at the bench 13:00). Blocked rather than todo so the page stops offering it as the next thing to do while there is no hardware; it goes back to todo the moment the boards are on the desk. CHECKED 2026-08-27: the PC half is ALREADY BUILT and guarded - shared/web/mice.css:312-317 steps the whole page up with zoom 1.6 past 2560px and 2.4 past 3840px (Studio exempt, it is a viewport app), and check_responsive measures it. So nothing here needs writing. What is left is JUDGEMENT ONLY: stand three metres from a real TV and say whether 1.6x is enough. That cannot be done from a desk, and the hardware is marked optional for exactly that reason CODE HALF RE-VERIFIED 2026-09-03: shared/web/mice.css:313 and :316 still carry the zoom step (1.6 past 2560px, 2.4 past 3840px, body:not(.app) so Studio is exempt). Nothing to write - only the three-metre judgement at a real TV is left.\n                Everything is legible there or it is not usable: type, spacing\n                and touch/focus targets need a step up past about 2000px rather\n                than the same 15px body text stretched across two metres of\n                glass. One rule in mice.css, not five copies\nA9-3: blocked  2026-08-20 23:56  touches=mice.css,hub.html,WebUI.h  hw=a TV remote or a keyboard-only run, to prove arrow-key operation (optional) \u2014 a TV has no mouse and no touch: it is driven by a remote, which WAITING FOR THE BENCH - the boards are back 2026-08-20 around 13:00 (leaving 12:30, at the bench 13:00). Blocked rather than todo so the page stops offering it as the next thing to do while there is no hardware; it goes back to todo the moment the boards are on the desk. CHECKED 2026-08-27: the PC half (A9-3b) is ALREADY BUILT and guarded - check_keyboard_only drives a real browser over four pages (hub, module site, Nong Studio, rgb) and fails on a control that answers a click without being reachable by Tab, on focus you cannot see, and on a tab that stopped being walked. So nothing here needs writing either. What is left is a real remote in a hand: a TV remote sends arrow keys and Enter and nothing else, and only a run on the TV proves the DOM order is sane to a person pressing right-right-right\n                is arrow keys and Enter. Every screen must be reachable and\n                operable in DOM order with a visible focus ring, no hover-only\n                affordance, and no control that can only be reached by dragging.\n                The focus ring exists already (mice.css); the ORDER and the\n                hover-only cases have never been checked\nA9-4: done   2026-08-20 16:27  touches=mice.css,hub.html,WebUI.h,studio needs=A9-1  \u2014 stop the wide screens wasting themselves. The hub caps at\n                1400px above 1500px wide, Studio's panel grows once at 1600px,\n                and the module site caps at 1400px \u2014 three different answers to\n                the same question. Decide one behaviour for a big screen and\n                apply it everywhere: either a readable column with the space\n                given to the CONTENT that benefits, or a second column\nA9-5: done   2026-08-20 16:27  touches=qc/ needs=A9-1  \u2014 prove it, do not eyeball it. Extend the responsive check to\n                assert, at every width above, that nothing scrolls sideways,\n                that no PANEL clips its own controls (the check added in A0-9),\n                that body text is at least the size the distance needs, and that\n                every interactive element meets the target size for that input\n                kind. A screenshot at one width proves one width\n# A10  Colour and joy, without losing the dark\nA10-1: done   2026-08-19 13:18  touches=shared/web/themes.css  \u2014 ONE file holds every colour in the project. Asked for themes.css written: Midnight (default), Carnival, Daylight, designed by Gemini Pro and measured here - its Daylight accent failed 4.5:1 and was changed. mice.css now names no colour at all; both are served joined as one stylesheet by the hub and from board flash, so no page changed. Shared mice.js discovers themes from the stylesheet, so adding one is that block and nothing else. IN STAGING, NOT PROMOTED\n                directly, 2026-08-19: a file to change only the theme or colour,\n                so a new one can be added later without reading anything else.\n                Every theme is one block in shared/web/themes.css; mice.css keeps\n                the SHAPE (radius, spacing, type scale) and stops naming colours\n                at all. The test is the project's usual one: can someone add the\n                next theme without opening any other file\nA10-2: done   2026-08-19 13:18  touches=mice.css needs=A10-1  \u2014 mice.css hands its palette over. Today :root holds both the\n                colours and the shape, so changing a colour means editing the\n                stylesheet every page depends on. After this it holds shape only,\n                and every colour comes from the theme file\nA10-3: done   2026-08-19 13:18  touches=hub.html,WebUI.h,studio needs=A10-1  hw=a phone, to look at the themes on a real screen (optional) \u2014 a theme picker that READS the file rather than shared/web/mice.js is served by the hub at /mice.js and compiled into board flash by gen_tables, like mice.css - so the hub, the module website and the help page all get one file. The picker DISCOVERS themes from document.styleSheets, so adding one is a block in themes.css and nothing else; the check adds a theme and fails if it does not appear. The model panel caught two things I missed: --ty-lift was byte-identical to --ok in all three themes, and the switcher hardcoded the default theme name. Gate 2122/0\n                repeating it: the list of themes is discovered from the blocks in\n                themes.css, so adding a block is the whole job - no second list\n                to keep in sync, the same rule the module and command registries\n                already follow. Remembered per browser and applied before the\n                first paint, or the old colours flash up and swap\nA10-4: done   2026-08-20 16:32  touches=themes.css needs=A10-1  \u2014 three to start with, and dark stays the default because a ALREADY DONE when picked up 2026-08-20: themes.css ships exactly the three this asks for - Midnight, Carnival, Daylight - one block each, dark still the default. Verified, not rebuilt.\n                bright screen spills light into the show area: Midnight (what\n                the project looks like today), Carnival (the colourful dark the\n                user asked for - more hue, same contrast) and Daylight (a bright\n                room, or a TV with sun on it)\nA10-5: done   2026-08-19 13:18  touches=mice.css,hub.html needs=A10-2  \u2014 the joy pass. Colour that MEANS something stays\n                meaning it (module type, ok/warn/err) and decoration comes from\n                the theme's own accents, so a joyful theme cannot make a warning\n                look like a decoration. Never colour as the only signal\nA10-6: done   2026-08-20 16:32  touches=qc/ needs=A10-3  \u2014 prove both halves. The check ADDS a theme block to the file, ALREADY DONE when picked up 2026-08-20: check_themes does both halves - it writes a qcprobe theme block into the real themes.css, reloads and expects it in the picker, then computes CIE76 distance and contrast for every shipped theme against 4.5:1 floors. 48 assertions, green.\n                reloads, and expects it in the picker (if it does not appear, the\n                list is being repeated somewhere); and it computes the contrast\n                of text on background for every shipped theme, because colourful\n                is where readable usually goes to die\n\n# A11  More it can do, without more to learn\nA11-1: done   2026-08-20 16:58  touches=hub.html  \u2014 find a module by typing. One box that filters on name, id\n                and type, no modes and no syntax. It is the feature that stops\n                mattering how long the list gets\nA11-2: done   2026-08-20 22:57  touches=hub.html needs=A11-1  \u2014 pin the ones you actually use; pinned first, the rest\n                below. A venue rig has four modules that matter and a shelf of\n                spares that do not\nA11-3: done   2026-08-20 16:58  touches=hub.html,WebUI.h  \u2014 one STOP that stops everything, in reach on every screen.\n                It exists per module today, which is the wrong place to look for\n                it when something is moving that should not be\nA11-4: done   2026-08-20 16:58  touches=hub.html  \u2014 the last thing you opened, one click away on Home. Nobody\n                opens a control panel to read it\nA11-5: done   2026-08-20 23:28  touches=hub.html,WebUI.h  \u2014 the keyboard: / to search, ? for the list of shortcuts,\n                Esc closes anything open. Every one of them discoverable from\n                that list, so it is not folklore\nA11-6: done   2026-08-20 23:42  touches=hub.html  \u2014 one button that copies a plain-text diagnostics bundle -\n                what this PC is, what it found, what firmware each board runs.\n                The thing anyone is asked for first when they report a problem\n\n# A12  The plan, and other documents, read themselves\nA12-1: done touches=docs/PLAN.html,tools/plan.py  \u2014 the plan updates while it is open, with nothing to\n                press. Asked for 2026-08-19. It is opened as file:///E:/..., and\n                a file:// page may not fetch a file beside it - the browser\n                gives it a null origin - but it MAY load a script, so plan.py\n                publishes docs/plan_state.js and the page pulls it in every four\n                seconds and redraws in place. No reload, so the scroll position\n                and any text being read stay put. Proved by putting a marker\n                ONLY in the published file and watching Edge render it. Also\n                fixed: a stale .staging/docs/PLAN.html from the day before,\n                which said work was still to do that had landed - it is now a\n                page that says so and points at the real one. check_plan_live.py,\n                four sabotages, including one for the obvious later tidy-up of\n                replacing the script tag with fetch, which works over http and\n                does nothing at all for the person reading the file\nA12-2: done   2026-08-20 23:57  touches=tools/livedoc.py needs=A12-1  \u2014 the same for any other document. Asked for\n                2026-08-19: make it like this for another plan too. One tool\n                that renders a markdown file to a page that keeps itself\n                current, with the same script-tag trick and a checklist counter\n                when the document has one. The rule that keeps it honest: the\n                markdown is the only source and the HTML is generated, never\n                edited, or there are two copies of one document drifting apart\nA12-3: done   2026-08-20 23:57  touches=docs/PLAN.html  \u2014 which hardware each change needs, on the change itself. BENCH CONSTRAINT from 2026-08-20 22:15: the user has ONLY a laptop and a phone until further notice - no boards, no TV, no projector. So every task carrying an hw= field is blocked, and the ten that do (A3-5, A7-4, A7-5, A3-8, A9-2, A9-3, A13-4, A13-8, A16-1, A13-11) must not be picked up. The eleven with no hw= field are the working set. The PHONE is available, which matters: a 360px layout can be checked on a real phone rather than only in a resized browser. CODE-HALF AUDIT 2026-09-03, at the user's instruction (own words): let make the code all of everything we need to do if cannot use real hardware yet let do the code first and then when have real try it if it cannot use let edit it. RESULT: every one of the 10 hardware-blocked tasks already has its software half written, and all four firmware environments build green - mice_nong 38.3s, mice_lift 56.2s, mice_cam 14.5s, mice_blank 13.8s, 4 succeeded in 2m03s. mice_blank links at RAM 15.6% and flash 66.4%. So there is no PC-only code left to write for the blocked list; what each one needs now is the bench. Per-task evidence is in the notes on A7-8, A20-3, A20-4, A9-2 and A13-8. pio is NOT on PATH - call it as ~/.platformio/penv/Scripts/pio.exe, a bare pio fails with command not found and the failure looks like a build error.\n                Done for the tasks that need it (hw= on the line, a chip in the\n                table and on the next-up card); this is the rest: keep it true\n                as tasks are added, and say so in the packing list when a part\n                stops being needed\n\n# A13  The camera board, and the bus that now exists\nA13-1: done   2026-08-19 13:22  touches=firmware hw=1 ESP32-CAM (here)  \u2014 the camera on this board is not one the firmware NOT A CAMERA FAULT AND NOT A WIRING FAULT. The board was running fw 1.0.0; flashed with the current mice_cam build it says [cam] ready, qvga, PSRAM NO and SNAP captured 14671 bytes. So the sensor is supported, the ribbon is fine and the 5V rail is adequate - the whole symptom was stale firmware. The diagnostic that reads the sensor id over SCCB stays in CamModule anyway: it is what turns Detected camera not supported into either an id to support or nothing answered on SCCB, and those need opposite fixes. Board is id 89, name undefined, type cam\n                accepts: it boots with camera_probe(): Detected camera not\n                supported. Find out which sensor it actually has - the driver\n                reports the id it read - and support that type, from the\n                registry rather than by adding a branch. Nothing about A7-7\n                means anything until a camera initialises\nA13-2: done   2026-08-19 15:02  touches=firmware,main.py hw=RS485 + nong id 67 (here)  \u2014 RS485 is real now, so the things a FOUND A REAL BUG THE FAKES COULD NEVER SHOW. On the bench bus the nong (id 67) answers a broadcast after 1344 ms, because RS485Bus.cpp:62 staggers replies by id*20 ms to avoid collisions - and main.py:1618 listens for 0.8 s. So the hub discovers only ids up to 40, while default ids run 1-247: most boards are invisible on a real bus. The hub sees the two USB boards and not the nong, with no error, which reads as an empty bus. Measured, not theorised: rts and dtr make no difference, the reply is 1344 ms every time, and 67 x 20 = 1340 MEASURED, and one half is still unmeasured, said plainly. The OLD stagger is confirmed on real hardware: the nong on id 67 answers a bus broadcast at 1344 ms, which is 67 x 20. The NEW bounded stagger cannot be measured on the bus yet, because the only board on the bus is the nong and it cannot be reflashed - it is on RS485, not USB, and esptool needs a direct UART. A board on USB answers a broadcast in 3-6 ms whatever its id, because the USB path does not stagger at all; that is the transport, not the fix. So: hub side measured and fixed, firmware side written and checked against a fake that now staggers, and honestly not yet proved on a real bus NOW MEASURED ON THE REAL BUS, both halves. The nong (id 67) was reflashed over USB and put back on RS485. Broadcast reply BEFORE: 1344 ms, which is 67 x 20. AFTER: 188, 195, 193 and 196 ms across four runs, against a design figure of (67 % 24) x 10 = 190. So the bounded stagger is real, not just modelled - every board now answers a broadcast inside the hub's floor whatever its id. The same board also reports chip B4BFE91C5A74 over the bus, so A3-4 is verified through RS485 as well as over USB. Why the flash had to happen over USB is worth keeping: esptool needs the ROM bootloader, entered by holding GPIO0 low during reset via DTR and RTS, and RS485 has no such lines to EN or IO0\n                fake bus could never show can be measured: an addressed command\n                to #67, a broadcast that several boards answer, and what happens\n                when two answers collide. The hub already speaks this protocol;\n                what has never been proved is that it survives a real bus\nA13-3: done   2026-08-19 13:37  touches=firmware hw=nong id 67 with WAIST and R-EL-R (here)  \u2014 the two bench servos are the first real VERIFIED ON REAL HARDWARE, and confirmed by the user watching it: JOINT WAIST 100 over RS485 answered OK WAIST=100.0 T=83ms, POSE? showed slot 9 change to 100.0, and the servo audibly and visibly turned. Returned to 90 the same way. The first real joint this project has moved through the bus. One correction worth keeping: the servo is R_EL_R with underscores, not R-EL-R - and the board says so itself, listing all ten names in the error, which is A7-1's one-sentence rule earning its keep\n                joints this project has had. Prove a move still runs after the\n                A7 refactors: send it to a named joint, watch it turn, and\n                assert on what came back rather than on what was sent\n\n# A14  One login across the PCs, later\nA14-1: done   2026-08-21 16:58  touches=hub_auth.py,hub.html  \u2014 PARKED ON PURPOSE 2026-08-19, at the user's word: pair\n                test first, the login later. The want is one account that works\n                on every PC. Facebook or Google sign-in cannot do it: OAuth needs\n                internet AT LOGIN TIME, a registered redirect URL and HTTPS,\n                while this hub runs plain HTTP on a LAN address that changes, at\n                a venue that often has no internet - so on show day nobody could\n                log in to stop a robot. The offline options are: copy the account\n                file between hubs (one compromised PC becomes all of them), a\n                pairing code shown on screen like pairing a TV (no internet, and\n                the trust is deliberate), or a secret derived from the\n                installation group name (already exists for the hotspot, and is\n                only as strong as a group name someone chose). Recommendation\n                when this is picked up: the pairing code, because it is the only\n                one where a person decides, on both machines, that these two PCs\n                belong together\n\nA13-4: blocked  2026-08-20 23:56  touches=main.py,hub.html hw=2 PCs on one WiFi + boards (here)  \u2014 asked for 2026-08-19: a second PC on the MEASURED THE LIMIT 2026-08-19: a second PC that only opens the hub in a browser can never share its USB port. The Web Serial API is the only route a page has to a serial port and it requires a secure context - the hub is plain HTTP on a LAN address, so the browser refuses. Two real options remain: run the hub on that PC (then /api/allmods aggregates its cables, which already works), or put the board on WiFi so no PC is involved at all - the board answers the hub directly and the second PC is only power. The second is better whenever the WiFi allows it, because it removes a machine from the chain rather than adding software to it Home counted ROWS across three lists, so a board on a cable was counted twice - a bench of four boards reported nine, six on USB and three on WiFi. It counts BOARDS from the merged list now, and says reachable rather than on, because a board with a cable and WiFi is one board with two ways in WAITING FOR THE BENCH - the boards are back 2026-08-20 around 13:00 (leaving 12:30, at the bench 13:00). Blocked rather than todo so the page stops offering it as the next thing to do while there is no hardware; it goes back to todo the moment the boards are on the desk.\n                same WiFi as the hub host can open the hub and see inside, but\n                it does not get the boards that host has on ITS cables. So the\n                host must SHARE its cables across the WiFi, not only its pages.\n                Half of this exists - /api/allmods aggregates every hub's\n                modules and rewrites the address as hub:IP/usb:COMx, and\n                parse_dev routes a command through the holding hub - so the\n                work is to find what is actually missing rather than to build it\n                again: the module list on the other PC, the module site opened\n                through the far hub, and Studio pointed at a board on a cable\n                that is not its own\nA13-5: done   2026-08-19 17:25  touches=main.py hw=2 PCs (here)  \u2014 the pair test itself. On this bench the hub found no MEASURED 2026-08-19: the two PCs can reach each other perfectly - this PC opened TCP to 10.123.98.148:8642 and its hub answered host=MSI with a cable holding id 42 lab-pair. Discovery is the ONLY thing missing: this PC is 10.126.226.203 and sweeps its own /24, so a hub on 10.123.98.x is unreachable by a sweep however long it runs, and mDNS cannot help either because multicast is link-local. So the fix is not a better sweep, it is letting a person NAME a hub - an address typed once, remembered, and probed like any other DONE 2026-08-19 and measured with both PCs: the two hubs are linked and this one lists the other's board as hub:10.123.98.148/usb:COM9 id 42 lab-pair host MSI, stable across three refreshes. Four fixes were needed. The finder ignored also_ask unless a patient probe was configured, so a typed address was remembered, shown as known, and never once contacted. probe_hub waits 0.7 s, which is right for sweeping 254 addresses and too tight for a named hub that is busy scanning its own cables - it appeared and vanished between refreshes until named hubs got a patient 5 s ask. /api/hubs/add was left ungated, which would have let anyone on the network point this hub at a machine of their choosing. And an apostrophe in a JavaScript string - this PC's own network - ended the string and killed the whole page script, which is why tabs stopped working\n                other hub at all, and the first suspect is not the code: the\n                WiFi is 10.126.226.x, a campus-style network, and those very\n                often isolate clients from each other so no PC can reach any\n                other. Prove which it is before changing anything - a hub that\n                cannot be reached is not a hub that was not found. If the\n                network does isolate, the answer is the hotspot path that\n                already exists, not a fix to discovery\n\nA13-6: done   2026-08-20 22:19  touches=firmware,main.py hw=2 boards + RS485 (here)  \u2014 firmware over the BUS, since esptool cannot do WAITING FOR THE BENCH - the boards are back 2026-08-20 around 13:00 (leaving 12:30, at the bench 13:00). Blocked rather than todo so the page stops offering it as the next thing to do while there is no hardware; it goes back to todo the moment the boards are on the desk. PRIORITY RAISED 2026-08-20 by the user: *make it can flash through all this 3 method too if i need to shieft to stm 32 it cannot use wifi it can use only rs485*. That settles the design question this task had left open. esptool is not the answer and never can be over RS485 - the ROM bootloader needs DTR and RTS on EN and IO0, and the bus has no such lines - so the update has to live INSIDE the firmware: the hub sends the image as ordinary commands (FBEGIN / FDATA / FEND), the board writes it into its spare OTA slot, verifies it, and reboots into it. The point of doing it that way is that the protocol is transport-agnostic: the same commands ride a USB cable, the RS485 bus, or WiFi, and a part that cannot do WiFi at all is then still updatable. Testable TODAY over USB, because the command channel is the same one - so the bus is only the last step, not the first. PROVEN ON REAL HARDWARE 2026-08-20. Firmware now travels as ordinary commands (FWBEGIN/FWDATA/FWEND), so the transport stops mattering. Board 85 over a USB cable: app0 -> app1, 1.29 MB in 239 s. Board 1 over RS485 ONLY, through a CH340+MAX485 dongle as usb:COM23:1 - no USB serial to that board and no radio anywhere in the path: app0 -> app1, 1.43 MB in 282 s. Three real bugs the bench found and a fake never would: (1) a chunk truncated by 64 characters is still VALID base64, so it decoded to 102 bytes instead of 150 and nothing noticed until FWEND counted the image 48 bytes short - each chunk now declares its length and a short one is refused without advancing, so the sender simply repeats it; (2) the hub required the firmware's blank-line marker before any reply, which a plain dongle cannot produce because there is no board on the cable to add it - it read @1 PONG 1 lift lift and reported 'no reply from COM23', so the @<id> prefix is now its own marker; (3) FWEND would have timed out at the 2 s default while the board hashed 1.3 MB, reporting a FAILED update for one that succeeded. Also landed with it: a brownout guard (firmware/src/core/BrownoutGuard.h) so a board whose supply cannot start it comes up with the radio OFF and stays answerable on USB and RS485 instead of resetting forever; the hub now counts boots and names the power rail instead of saying 'no reply'; a USB-RS485 DONGLE's replies are no longer discarded for lacking the firmware's blank-line marker (the @<id> prefix is its own marker); the flash watcher finished only on FAILURE because it waited for a 'done' key /api/flash has never sent, so a successful write left the button disabled and never applied the board's type; and ai_panel.py stopped reporting outages as reviews (tool errors were parsed as findings, the head printed '(nothing)' when it never ran, and --dir listed only the top level so a question about BusUpdate.cpp reported CHANGELOG.md as its scope). ALL THREE FLASH METHODS RE-PROVEN 2026-08-21 on today's build, and every board on the bench updated. USB/esptool: lift 1 (COM9, mice_lift), nong 85 lift-test (COM26), nong 42 lab-pair (COM28), cam 89 (COM18, mice_cam), nong 67 (COM29), and the spare (COM27). RS485 ONLY: board 67 through the CH340+MAX485 dongle as usb:COM23:67, 1.30 MB in 250 s, OK FWEND restarting, and it answered @67 PONG 67 nong nong over the bus afterwards. All six now answer FWSTAT idle, so the one-time esptool bootstrap is done for every board here - 42 and 89 had been running firmware with no updater at all (ERR unknown cmd: FWSTAT). THE BOARD ON COM27 IS ALIVE AGAIN: the plan had it as a 30-pin ESP32 that boot-loops on brownout, and it did - Brownout detector was triggered, empty NVS, resetting forever. Flashed with today's mice_nong it comes up and stays up as PONG 65 MOD-83D440 blank. The brownout guard is what fixed it, which is the first time that guard has been shown to rescue a board rather than just to log RE-PROVEN ON HARDWARE 2026-08-25 ~19:00 at the user's request (*flash esp32 via rs485 by not flash from usb*), on today's build through the XY-485 dongle: POST /api/flash/bus dev=usb:COM23:67 type=nong - 1.30 MB in 260 s, OK FWEND restarting, then PING->PONG 67 nong nong, FWSTAT idle, INFO fw 1.0.0 sd=true over the bus only. Note: /api/flash/bus is login-gated since the A22-1 batch, so the run logged in first.\n                it. Asked 2026-08-19: why can we not upload over RS485 or WiFi.\n                WiFi already works - the board has OTA and writes the spare app\n                slot, so a failed update keeps the firmware it has. RS485 cannot\n                use esptool at all: the ROM bootloader is entered by holding\n                GPIO0 low during reset through DTR and RTS, and a differential\n                pair has no such lines. But the running firmware already accepts\n                a file over the bus (FBEGIN FDATA FEND) and already knows how to\n                write an OTA slot, so the two joined would be a real bus update\n                with no ROM bootloader involved. The board must be able to\n                refuse a bad image and keep running the old one, exactly as OTA\n                does now\nA13-7: done   2026-08-19 15:12  touches=firmware hw=2+ boards (here)  \u2014 pairing with no venue WiFi, which is what the user ROOT CAUSE FOUND ON HARDWARE, and it is a chicken and egg. WebPortal.cpp:1162 reads worthLooking = onRelay_ || peers_.count() > 0, so a board hunts for neighbours only if it is ALREADY on a relay or ALREADY knows a peer. A board that just booted with no venue WiFi knows nobody, so it never scans, so it never finds anybody. Measured with three real boards - nong 67 and lift-test 85 both in group mice-show, lift 1 in other-company, all three in ap-only with apclients=0 - and PEERS on each returns only itself. This is exactly the always show not linked in A3-5, and no fake could produce it. The guard was added because the scan fired every 60 s for nothing when there were no neighbours; the fix has to keep that cost down while letting a GROUPED board look, since its AP password is derived from the group and can only open a group-mate's network FIXED AND MEASURED ON THREE REAL BOARDS. One line at WebPortal.cpp:1162 - worthLooking = onRelay_ || peers_.count() > 0 - meant a board hunted for neighbours only if it already had one, so a cold room of boards stayed alone forever. Now a board that BELONGS TO A GROUP and has no network also looks, which keeps the cost the old guard was protecting (an ungrouped board still never scans for nothing) while making the scan one that can succeed: its AP password is derived from the group, so the only network it can open is a group-mate's. Before: three boards, apclients=0, each PEERS listing only itself. After: lift-test 85 joined the nong's access point (state=online ssid=nong ip=192.168.4.2 rssi=-29), the nong shows apclients=1, and PEERS on BOTH lists the other. The negative control held: lift id 1 in group other-company sat in the same room, never joined, apclients=0. One small thing to chase later - the nong reports its OWN ip as 192.168.4.2 in PEERS while hosting on 192.168.4.1\n                actually asked for: when the usual network is not there, the\n                modules should find each other by themselves. The pieces exist -\n                a module falls back to hosting its own hotspot, and the password\n                for it is derived from the GROUP so only modules in the same\n                installation can join - but nothing has ever been measured with\n                two real boards. Prove: with the network absent, one board hosts,\n                the others join, and PEERS lists them on both sides\n\n# A15  Getting it onto another PC\nA15-1: done   2026-08-19 23:44  touches=MiceHub.spec,main.py  \u2014 ONE FILE, not one folder. Asked 2026-08-19: run the app\n                on another PC without the whole repo. Today the exe cannot:\n                MiceHub.spec has datas=[] and main.py reads HERE/web and\n                HERE.parent/shared/web off the disk, so a copied exe serves an\n                unstyled hub with no Studio. Bundle the web folders into the\n                exe and read them from sys._MEIPASS when frozen. Then the answer\n                to how do I run it elsewhere is: copy one file. The check is the\n                honest one - run the built exe from an EMPTY folder and open\n                every page\nA15-2: done   2026-08-20 01:42  touches=git needs=A15-1  \u2014 branches, in the shape asked for: develop carries this\n                work, main stays what is known good. NOT a third branch holding\n                a copy of the app - a branch that duplicates files drifts from\n                the one it copied, and this project has already been bitten by a\n                stale copy of the plan. If a source-only bundle is still wanted\n                after A15-1, generate it with a script so it cannot drift\nA13-8: blocked  2026-08-20 23:56  touches=none hw=a module on WiFi + a phone (here)  \u2014 prove a PHONE can update a robot. Asked WAITING FOR THE BENCH - the boards are back 2026-08-20 around 13:00 (leaving 12:30, at the bench 13:00). Blocked rather than todo so the page stops offering it as the next thing to do while there is no hardware; it goes back to todo the moment the boards are on the desk. CODE HALF RE-VERIFIED 2026-09-03: the hub's over-the-air path exists end to end - main_python/main.py:2538 start_ota, :2569 _run_ota which POSTs the image with its md5, :4257 the /api/ota route, and :1608 reports ota_ready per image. A phone only needs the hub page, so nothing is missing but a module on WiFi and a phone in hand.\n                2026-08-19: if the module is on the hub's WiFi, can another PC\n                or a phone flash it, since the hub holds the .bin. Reading the\n                code says yes and nothing has ever tested it: the browser never\n                touches firmware, it posts to /api/ota ON THE HUB and the hub\n                pushes the bytes, so a phone needs only a browser and a login.\n                The two limits to prove as well: the HUB must reach the module -\n                a module on a board hotspot the hub is not on cannot be updated\n                from anywhere - and /api/ota is gated, so an unauthenticated\n                phone is refused\n\nA15-5: done   2026-08-21 00:31  touches=main.py,hub.html needs=A15-1  \u2014 a button that updates the hub itself. Asked\n                2026-08-19: click to update when a new one is uploaded to git.\n                The hub asks GitHub for the newest release, says what version it\n                is running and what is offered, and replaces itself only when a\n                person presses it. It must refuse cleanly with no internet -\n                which is the normal case at a venue - and it must never replace\n                itself while a flash or a show is running\n\nA15-4: done   2026-08-21 00:08  touches=README.md needs=A15-2  \u2014 a README that says how to get JUST the part you need,\n                asked for 2026-08-19. Three routes, each with the exact command:\n                the one file (download the exe from the release and run it),\n                one branch without the history (git clone --branch <name>\n                --depth 1 <url>), and one FOLDER out of the repo without the\n                rest (git sparse-checkout). Written for someone standing at\n                another PC who has not read anything else, so every command is\n                complete and none of them assumes this repo is already cloned\n\nA15-3: done   2026-08-20 01:42  touches=git  \u2014 commit today's work. Nothing is committed yet: the RS485\n                census fix, the themes, the QR and mDNS, remote flash, the model\n                panel and every measurement from the bench are all working tree\n                on one machine. That is the single biggest risk on this project\n                right now, and it is not a technical one\n\nA13-9: done   2026-08-19 17:25  touches=main.py hw=4 cables incl RS485 (here)  \u2014 MY OWN REGRESSION, found on the bench: THREE bugs, one symptom - the hub listed 3 of the 4 boards it can reach. (1) probe_usb_all gave every port ONE shared 7 s budget and the RS485 census floor pushed a four-cable bench past it: raised to 20 s, with the measurement written beside it - an ordinary port answers in 3 s, a port with a bus behind it in 4.7 s. (2) modules_here skipped a port whose DIRECT probe found nothing, and that threw away its bus children - which is every module behind an RS485 adapter, because an adapter has no board of its own. My own code from A3-4, and the fake never showed it because the fake port has both a module and a bus. (3) a board answers the broadcast on its own USB line too, so a board whose direct probe was missed looked like a module behind an adapter - the camera was listed as rs485 COM18. It is asked once more, plainly: an adapter has nothing to answer with, a board does. All four boards now list correctly, each labelled by the route it is really on\n                the hub lists 3 of 5 boards, and the nong on the RS485 bus is\n                the one missing. Measured 2026-08-19 against the real cables -\n                COM23 alone answers in 4.7 s and DOES find the bus module, the\n                other three take about 3 s each, and the full scan takes 10.8 s\n                and returns 2 boards. The cause is mine: probe_usb_all gives\n                every port ONE shared 7 second budget, and the 1.6 s census\n                floor I added for the bus pushed the total past it, so whichever\n                ports are waited on last are reported as no answer. A single\n                port scan still works, which is why this looked like a bus\n                problem and is not one\n\nA13-10: done   2026-08-19 18:50  touches=main.py  \u2014 plain mice.local, with no port. Asked 2026-08-19: DONE 2026-08-19 and proved both ways on this machine. http://mice.local now works with no port: a second listener takes port 80 and only redirects, so the hub stays on 8642. Measured - 302 to mice.local:8642 and the hub page lands. The half that matters more was tested by taking port 80 away with a socket first: the hub starts exactly as before and the short name simply does not exist. Moving the hub TO port 80 would have meant a PC with IIS or Skype could not run it at all. One real Windows trap found on the way: ThreadingHTTPServer sets SO_REUSEADDR, and on Windows that is not the harmless TIME_WAIT reuse it is on Unix - it lets one process bind a port another process is LISTENING on, and then connections go to whichever. The redirect listener sets allow_reuse_address False so it can never steal port 80 from another server. Three sabotages, all caught TWO LIMITS, both deliberate. Port 80 is per machine: if something holds it on the other PC, that PC keeps the port in its address while this one does not - the hub must never fail to start over a convenience. And mice.local is one name: two hubs on the SAME network both answer it and whichever replies first wins, at random. That cannot happen on this bench because the two PCs are on different subnets and mDNS is link-local, but it would on one network. The answer is not a name to remember - mice-msi.local was rejected for exactly that - it is that mice.local means the hub on the machine you are at, and other PCs are LINKS in the Other PCs list, named by their own hostname and clicked rather than typed\n                why type mice.local:8642. Because a bare name means port 80 and\n                the hub is on 8642. Do NOT move the hub to 80 - on Windows\n                something else often holds it (IIS, Skype, another dev server)\n                and the hub would fail to start at all, which reads as the app\n                being broken. Instead take port 80 as a SECOND listener that\n                only redirects to the real one, and if the port is already\n                taken, carry on silently: the hub still works, the short name\n                just does not. It must never delay startup waiting for a port\n                it may not get\n\n# A17  The suite, and the app, keeping themselves current\nA17-1: done   2026-08-20 00:37  touches=run_qc.py,qc/lib,check_build_split.py  \u2014 the suite uses the whole machine.\n                Asked for 2026-08-20: *make it faster before do other thing it\n                will make all of my process faster than before*. It ran 86\n                checks one at a time - 836s on one core of a 24-thread PC.\n                MEASURED, same assertions passing: 836s -> 236s, 3.5x.\n                Checks now run in a process pool (10 workers), which is safe\n                because a separate process gets its own fake_serial, its own\n                qc_marks and its own hub. The five-model panel was asked BEFORE\n                any of it was written and named five real hazards, all\n                confirmed in the code and all fixed:\n                  * _qcdriver.html was written under ONE name into the shared\n                    studio folder - two workers would each load the other's\n                    test, which passes the wrong test rather than failing;\n                  * hub ports came from 8700 + (pid % 40) * 25, and Windows\n                    pids are multiples of 4, so that has TEN values; a\n                    collision makes one worker's hub answer another's request;\n                  * gen_tables.py ran on first use in every process, all\n                    writing the same generated files;\n                  * browser profiles were keyed by millisecond, and workers\n                    start in the same millisecond;\n                  * port 80, mDNS 5353 and firmware/.pio are single-occupancy,\n                    so those four checks are kept serial.\n                check_qc_parallel holds all of it. The floor now is one browser\n                check that alone takes 175s: the pool cannot finish sooner than\n                its slowest member. Firmware builds run together too, but that\n                is only 107s -> 91s, because PlatformIO already parallelises\n                internally and four builds contend for the same cores\n\nA17-2: done   2026-08-20 00:37  touches=run_qc.py,promote.py  \u2014 the plan page updates itself while work runs.\n                Asked for 2026-08-20: *why it not update plan.html make update\n                everytime as default when run this plan*. run_qc.py now writes\n                progress every 10 checks and promote.py reports its stages, so\n                a gate that takes minutes cannot be mistaken for one that died.\n                Best effort by design: a plan file it cannot write must never\n                fail a gate\n\n\n# A16  The nong's real geometry, measured not guessed\nA16-1: blocked  2026-08-20 23:56  touches=app.js,help.html hw=nong + a ruler (lab)  \u2014 the 4-bar's own curve, which is still WAITING FOR THE BENCH - the boards are back 2026-08-20 around 13:00 (leaving 12:30, at the bench 13:00). Blocked rather than todo so the page stops offering it as the next thing to do while there is no hardware; it goes back to todo the moment the boards are on the desk.\n                missing. Told 2026-08-19: the shoulder bar is now a 4-BAR\n                LINKAGE, not a see-saw, and the joint swings 26 deg (90 +-13)\n                instead of the old 6. The TRAVEL is done and checked\n                (check_shrug_range holds the firmware, Studio, COMMANDS.md and\n                the help table to the same number). What is NOT here is how far\n                each shoulder actually RISES at those angles - the user has not\n                measured the linkage yet and said so plainly: *i didn't put the\n                4-bar measure to you yet so put it to plan too i will check it\n                later*.\n                Until those rows exist the preview still assumes a symmetric\n                rock, which check_shrug_curve already says is wrong for a 4-bar:\n                the two sides do not rise by the same amount and the ratio\n                changes across the travel. Measure at 77 / 90 / 103 at least,\n                in mm, up positive, and put them in Rig setup > shrug curve\n\nA13-11: done   2026-08-21 19:45  touches=main.py,hub.html hw=cam + 2 viewers (here)  \u2014 one camera, many screens. The board has ONE PARKED 2026-08-20 00:15 - needs the camera and two viewers on the bench; picked up again when the hardware is back (~11:00) WAITING FOR THE BENCH - the boards are back 2026-08-20 around 13:00 (leaving 12:30, at the bench 13:00). Blocked rather than todo so the page stops offering it as the next thing to do while there is no hardware; it goes back to todo the moment the boards are on the desk. DONE 2026-08-21. The second person to open the live view was told *someone is already watching (one at a time)*, and the board was right: one frame buffer (PSRAM off for the camera build, measured twice on hardware) and take() reclaims the frame already on loan, so two streams tear each other. What was wrong is that the hub piped each browser straight to the board, turning the hardware's limit into the product's. main_python/cam_relay.py holds ONE connection and hands the newest frame to every viewer. Two failure modes designed for, both named by the panel: a slow viewer gets the LATEST frame rather than a queue, so it drops frames instead of holding others back; and a board that goes quiet without closing its socket is dropped after 8 s rather than leaving viewers on a picture that stopped being now. check_cam_viewers proves it with the fake camera refusing a second watcher exactly as the board does - two viewers both get frames, the board is opened ONCE, and the quick viewer is not throttled to the slow one. Two sabotages, both caught. The module page's take-turns message is gone with the limit\n                frame buffer and no PSRAM, so two viewers streaming straight\n                from it fight over the same memory and both stutter. The fix is\n                not on the board: the hub already proxies /api/cam.stream, so\n                it should pull ONE stream from the camera and fan it out to\n                every viewer. Prove it with two browsers open at once, and\n                measure what the board sees - it should be one client, whatever\n                the number of screens\n\nA13-13: done   2026-08-19 23:44  touches=discovery.py,main.py,mdns.py,hub.html hw=2 PCs + a flaky WiFi (here)  \u2014 the venue WiFi drops, and nothing may built and quick-green (1508/0); full gate was still running at 19:55 when the bench had to be packed up. All work is in .staging, nothing promoted for A13-12/A13-13 yet - rerun promote.py first thing\n                blink. Reported 2026-08-19 while the second PC was being\n                connected: the wifi it not stable sometime i lost connect some\n                time other pc lost connect. That is the NORMAL condition at a\n                venue, not a fault to wait out, and it breaks two things that\n                both looked finished:\n                  * discovery FORGETS on a single missed sweep. A hub or module\n                    that misses one probe is dropped from the list entirely, so\n                    the screen says it is gone when it is merely late. Keep it,\n                    marked last seen Ns ago, until a grace period really expires\n                    - the stale state the design rules already ask for;\n                  * the mice.local claim happens ONCE, at startup. If this PC is\n                    off the air when the other one boots, both claim the name\n                    and it flips between machines with nothing said. Needs a\n                    periodic re-check with a deterministic tie-break, so the two\n                    hubs settle it the same way without talking to each other\n\nA13-12: done   2026-08-19 20:37  touches=mdns.py,main.py hw=two hubs on one link (here)  \u2014 two hubs, one name. Raised Asked whether hubs should be picked by speed, like the internet choosing the fastest server. They should not, and the reason is worth keeping: hubs are NOT copies of each other. Each one owns its own cables, so a board plugged into the MSI can only be DRIVEN by the MSI - that is physics, not policy. What they share is visibility: any hub lists every board across every linked hub, and forwards a command to the machine holding the cable (dev = hub:IP/usb:COMx). So open whichever hub is nearest, it is a front door for all of them; the only differences are one network hop for a far board, and that a board is reachable only while its own PC is running a hub. That is exactly why the fix is NAMING rather than load balancing - you need to know which machine will do the work, not to be sent to the quickest one\nA17-3: done   2026-08-20 01:08  \u2014 an open hub page notices when the app it came from has changed. Asked 2026-08-20: *in web why i need to refresh by my self to see the change*. Data already refreshes (modules every 5s, rescan every 15s) but hub.html, mice.css and themes.css are only fetched when the tab loads, so a UI change is invisible until a manual reload\nA4-7: done   2026-08-20 02:24  \u2014 the camera says which board it is even when the compiled-in pins worked. Found by the five-model panel 2026-08-20 and confirmed in CamModule.cpp: esp_camera_init is tried first with the compiled-in AI-Thinker pins, so on the commonest board it succeeds and board_ is never set - the CAM reply says board= with nothing after it and the hub reports a GUESS for the board the firmware is certain about. Also probeSensor scans only the compiled-in SCCB pair, so the dead-camera-versus-wrong-pins diagnosis is wrong on any other board, and the flash LED pin is compiled in rather than per board NOT VERIFIED ON A BOARD: written, compiled (mice_cam SUCCESS) and held by check_camera against the source, but no camera was on the desk. The three things to confirm when the boards are back at 11:00 - CAM now answers board=ai-thinker instead of board=compiled-in on the AI-Thinker; the flood LED still lights on FLASH 1; and a deliberately wrong pin map still reports which SCCB pair answered.\nA17-4: done   2026-08-21 00:40  \u2014 an intermittent nobody has explained: check_responsive crashed once during a parallel gate on 2026-08-20, passed alone and in a later full parallel run. Every shared resource found so far is fixed (scratch page names, hub ports, browser tags are per process). Next time it happens, keep the output: the crash text was lost because land.py prints only the verdict ROOT CAUSE FOUND 2026-08-20 19:40, and it is not one bug but one SHAPE repeated. Three different browser checks failed in three consecutive gates and every one passed alone: check_flash_confirm, check_network_tab, check_modsite_back. In all three the page had simply not finished loading inside the window, and in all three the check then reported something ALARMING AND UNTRUE rather than the one true fact. flash_confirm said 'clicking Flash ASKS instead of flashing' - which reads as the safety gate being gone - when nothing had been clicked at all. network_tab said 'it lists the modules it can find (0)' when the tab was still searching. modsite_back reported six wrong VALUES for the hub link when its own driver had already reported missing=showHubLink, because the assertions never looked at that mark. Fixed in each: wait for the thing instead of betting on a fixed pause (modsite_back had setTimeout 6000, the exact bet this suite warns about elsewhere), widen the page window to cover the waits inside it (flash_confirm allowed 40s for waits totalling 60s), and report a page that did not load as a page that did not load. The browser lane stays at 3 - one-at-a-time was measured at 932s against 836s, so the cure cost more than the disease. MODEL AVAILABILITY 2026-08-20 19:30, measured not assumed: gemini-3.1-pro-high ok, gemini-3.7-flash-high ok, and claude-opus-4-6-thinking, claude-sonnet-4-6 and gpt-oss-120b-medium all answer 'Individual quota reached ... resets in 117h', which is about five days - until roughly 2026-08-25. That is a subscription quota, not a login: re-authenticating does nothing for it. The Gemini pair needed a re-login today, which the user did, and they have answered every time since apart from one transient network error and one DNS failure on daily-cloudcode-pa.googleapis.com. So the panel runs with TWO voices until about 2026-08-25 - which is the case the user's own rule already covers: one model running out costs one opinion, never the review. ai_panel.py now says NO REVIEW HAPPENED and exits non-zero when every model is down, rather than printing an empty list that reads as approval. CORRECTION on WHY those three are down, measured 2026-08-20 19:35: claude-opus-4-6-thinking and gpt-oss-120b-medium reported 'Resets in 116h29m48s' and '116h29m38s' when asked ten seconds apart - the SAME absolute deadline. So it is ONE pooled quota shared across the non-Gemini models on the Antigravity side, not a per-model limit and not the user's Anthropic subscription. The user checked and had 41 percent of their Anthropic week left, which is a different account entirely: agy resells Claude and GPT-OSS under Antigravity's own Individual quota, while Gemini Pro and Flash keep working because they are Google's own models and do not come out of that pool. Nothing on the Anthropic side changes it. Expect the three back around 2026-08-25 late afternoon. REVIEW VOICES CAN COME FROM CLAUDE CODE TOO, the user's decision 2026-08-20 19:43: 'you can use this sonnet and opus to run not only the antigravity one can use both and if thinking consume more token use default'. So while the Antigravity pool is empty (about five days, to roughly 2026-08-25) the panel is not down to two voices - Gemini Pro and Flash come through agy, and Sonnet and Opus come through Claude Code subagents on the user's Anthropic plan, which had 41 percent of its week left. Use the DEFAULT models, not the thinking variants, because the user pays for the output. That keeps four voices from three makers, which is what the multi-model rule was for. TWO MORE VOICES THAT DO NOT DEPEND ON ANTIGRAVITY, both the user's suggestion 2026-08-20 19:52. (1) Claude Code subagents on the user's own Anthropic plan - sonnet and opus, DEFAULT not thinking. Already used: a sonnet review found a real hole in BusUpdate.cpp the same evening (md5 was optional, so an update could run with no integrity check and FWEND still answered OK). (2) OLLAMA is installed on this PC with qwen2.5-coder:7b and qwen2.5-coder:3b-base - free per token and unaffected by the network, which matters because an unreliable link is not free: the panel run at 18:36 spent 603,425 tokens and returned nothing, since every model read files for 400s and only THEN hit the auth error. The catch with ollama is that it cannot read files itself, so a local voice has to be given the text - fine for two or three files, not for a whole tree. MAKER DIVERSITY IS WEAKER EVIDENCE THAN I CLAIMED - the user caught this 2026-08-20. check_local_panel asserted that no model family may appear twice, on the theory that two models from one maker share their blind spots and the second teaches nothing. This project's own evidence contradicts it: on 2026-08-20 a SONNET subagent and an OPUS subagent - same maker - found completely non-overlapping bugs (sonnet: md5 was optional so a corrupted image could boot; opus: NUL boot lines returned as replies, and forwarded WiFi commands never got PEER_WAIT). The honest caveat is that those two were also given DIFFERENT FILES to read, so it was never a clean test of maker-vs-maker. What it does suggest is that SCOPE diversity is the stronger lever: two models on the same 200 lines converge, two models on different files cannot. Relax the check to a floor of distinct makers, drop the no-duplicates claim, and consider giving each panel model its own slice of the tree instead of the same prompt. MEASURED 2026-08-20 22:30 - WHAT LOCAL MODELS ARE AND ARE NOT FOR. Benchmark: the pre-fix BusUpdate.cpp with the md5 hole a Sonnet subagent had found hours earlier, same file, question 'can this board end up running a corrupted image, name the line'. deepseek-coder-v2:16b and qwen2.5-coder:14b, each under the terse ai_brief and under a reasoning-allowed brief. Four runs, FOUR MISSES - and not quiet misses: deepseek quoted the written_ != total_ guard and called it the line that ALLOWS corruption (it prevents it), then claimed lines 59/65/73 lacked base64 and length checks that are written there; qwen said a sender would have to fail the final md5 check, ASSUMING the md5 was registered, which is exactly the bug. Both pattern-matched Update.end(true) to md5-verified without tracing whether setMD5 was ever called. Caveat on fairness: they were given only BusUpdate.cpp while Sonnet could also read CommandRouter.cpp and see the md5 arg was optional - but the md5_.length() == 32 line was in the file they had, so it was findable. CONCLUSION: do not use local models to answer is-this-code-correct. Use them for lookups whose answer can be checked in seconds, and for throwing up alternatives where a wrong idea is cheap. Every finding checked against the code, which is already the rule and now has a number behind it. LOCAL MODELS: MEASURED, THEN DELETED, 2026-08-20. 100 cases with answers computed from the real tree before asking - five job kinds, twenty each, nothing graded by another model. Scores (fact = read a constant's value / exists / classify an error / count occurrences / extract the failing check from a log): phi4:14b 100/55/80/15/25, qwen2.5-coder:14b 100/85/35/25/15, llama3.1:8b 100/55/75/5/15, deepseek-coder-v2:16b 100/60/60/20/5, qwen2.5-coder:7b 60/50/20/0/45. Exactly ONE job clears 90% and four models are perfect at it. THE POINT THAT SETTLED IT: even that job is one grep already does better. To ask a model for a constant's value, something must first FIND the line and hand it the snippet - and the grep that finds it has already printed the answer. The model adds a load, a step, and a chance to be wrong to a question answered for free. Combined with the earlier result (0 of 8 valid runs on a real bug, with confident false findings about guards written in front of them), the conclusion is that local models save nothing on this project: not review, not search, not lookups, not comments - the comments here carry measurements and dates that live in the session's context, not in the code. The four review models were deleted, freeing 32 GB; qwen2.5-coder:7b and 3b-base stay because the USER uses them directly. tools/local_panel.py and check_local_panel were removed with them. Do not rebuild this without new evidence. EXPLAINED and guarded 2026-08-21. Three browser checks failed intermittently in parallel gates on 2026-08-20 (check_flash_confirm, check_responsive, check_flash_remote) and every one passed alone. Two causes, both found: (1) MY OWN LOAD - the gate that failed check_flash_remote with ConnectionAbortedError was running while ollama held the GPU at 96% and a 9 GB model download had the network; the same check passed alone in 11.9 s straight afterwards. That is now a rule in CLAUDE.md: nothing heavy runs during a gate, and .staging is never edited during one. (2) A BUDGET BUG with a mechanical cause - check_flash_confirm could spend 60 s in qcWaitFor calls inside a 40 s raw_page window, so under load the page was killed before it reported anything and the check announced 'clicking Flash ASKS instead of flashing', which reads as the safety gate being gone when nothing had been clicked at all. Fixed, given real margin (115 s), and check_browser_budget now asserts the arithmetic for EVERY browser check: window >= worst-case waits + 8 s of startup. It caught the regression when the old window was put back.\nA6-4: done   2026-08-20 04:16  \u2014 the hub Tools list drew NOTHING. Found by the five-model panel 2026-08-20 while it was asked about something else, and confirmed in a browser: zero rows. The row builder had been copied from the module list and still read m.type, but the loop variable is a and an app has no module type - m was undefined, the exception left loadApps half-finished, and the card stayed empty. It hid for weeks because an empty list is a plausible state: the page has a careful no-tools-yet message, so broken and genuinely-empty looked identical, and every existing check asked the SERVER, which was answering perfectly. check_tools_list now drives the real page and counts rows against /api/apps\nA13-14: done   2026-08-20 14:11  \u2014 the hub's own flash route is defeated by the hub's own scanner. Measured 2026-08-20 at the bench: POST /api/flash for COM25 failed at 21 percent with 'A fatal error occurred: The chip stopped responding', and left the board in the bootloader answering nothing. The identical flash from the command line with the hub STOPPED succeeded first try (1349856 bytes, hash verified). So esptool and the hub's port scanner are fighting over the cable during a flash. The project rule says stop the hub before flashing, which is exactly the thing the flash route exists to make unnecessary VERIFIED ON HARDWARE 2026-08-20 13:2x. Before: POST /api/flash on COM25 died at 21 percent, 'the chip stopped responding', board left in the bootloader. After: the same call, with the hub running and the cable held by a module scan, ran 15-44-73-100 percent and finished ok=True. The cause was usb_close declining to close a port it could not lock - correct behaviour - while the flasher slept 300ms and ran esptool into a handle that was still open.\nA13-15: done   2026-08-20 23:57  \u2014 RS485 replies arrive corrupted, and the fix is written but cannot be proved from this desk. MEASURED 2026-08-20 at the bench: every reply over RS485 came back with runs of NUL bytes replacing characters - even a 20-byte PONG read PO then four NULs then ined cam - while two boards on plain USB cables answered byte-identically through the same hub process. The five-model panel found the cause in RS485Bus::send: flush() empties the FIFO but not the shift register, and the code then waited 20us before dropping the driver enable, when one byte at 115200 takes 86.8us. Half a character on the wire is a framing error, and a framing error arrives as 0x00. The hold is now two byte periods derived from RS485_BAUD (172us), held by check_rs485_turnaround. TO VERIFY, one wiring change is needed: the two boards on the bus (#67 and #89) can only be reached over RS485, and firmware cannot be flashed that way (A13-6). Either put the A/B pair on board #85, which is on COM25 and already runs this firmware, or give #67 a USB cable for one flash. Then read INFO over the bus and count NULs - it was 100 percent corrupted before NOT done - the fix is written, checked and landed, but NOTHING has proved it on a real bus. Blocked 2026-08-20 at the user's word: they have no spare MAX485 for the two ESP32s yet and will say when they do. Until then the corruption measured today stands unexplained-in-practice: the theory is strong (20us hold against an 86.8us byte) and the evidence fits (NUL runs, only on RS485, never on a direct cable), but a theory that fits is not a measurement. UNBLOCKED 2026-08-20: the user has a spare 30-pin ESP32 and another MAX485. Wiring given: GPIO16=RO, GPIO17=DI, GPIO4=DE and /RE tied, GND common to every transceiver, 5V to VCC, A and B onto the existing pair, and leave the board on USB so it can be flashed. That makes the test clean rather than merely possible: the new board runs the FIXED turnaround while #67 and #89 still run the old one, on the same wires at the same time - so the comparison is same-bus, same-moment, and no wiring change can be blamed for the difference. Measure: read INFO over the bus 5 times from each and count NUL bytes. Before, every RS485 reply was corrupted; a direct cable was byte-identical twice. 2026-08-20 16:5x: the 30-pin board is wired and FLASHED with the fixed firmware (mice_nong, upload verified), but it cannot run - the serial output is 'Brownout detector was triggered' repeating, so it resets before it can answer. That is power, not firmware. Most likely the MAX485 is on the 3.3V pin rather than VIN/5V; those modules are 5V parts and pull more than the devkit regulator gives. The test is one command once it stays up: python tools/rs485_test.py DIAGNOSIS WAS WRONG, corrected 2026-08-20 by measurement. The corruption was NOT RS485 and NOT the firmware turnaround. Same board, same port, same minute: read directly 10/10 clean with 0 NULs; through the hub 0/10 clean with 679 NULs. Opening the port RESET the board on that FTDI adapter, and the ESP32 ROM log prints at a different rate from 115200, so it arrived as NULs and fragments - and usb_cmd handed that back as the answer. Fixed in the hub (see A13-17), measured after: 6/6 clean, 0 NULs, identical lengths. The RS485 turnaround change STAYS because 20us against an 86.8us byte is a real defect, but it fixed nothing that was observed and must not be credited with it. A theory that fits the evidence is not the same as the cause. DIAGNOSIS WAS WRONG, corrected 2026-08-20 by measurement. The corruption was NOT RS485 and NOT the firmware turnaround. Same board, same port, same minute: read directly 10/10 clean with 0 NULs, through the hub 0/10 clean with 679 NULs. Opening the port RESET the board on that FTDI adapter, and the ESP32 ROM log prints at a different rate from 115200, so it arrived as NULs and fragments, and usb_cmd handed that back as the answer. The turnaround change STAYS because 20us against an 86.8us byte is a real defect, but it fixed nothing that was observed and must not be credited with it. the A/B this needed is now possible and was not before: board 1 (COM9) carries the NEW firmware with the derived turnaround, board 67 the OLD one, and they sit on the SAME RS485 pair - same wires, same moment, no wiring change to blame. Indirect evidence already in hand 2026-08-20: 1.43 MB was flashed to board 1 over that bus in ~10,000 round trips with only occasional retries, which a broken turnaround would not survive. Still to do: run tools/rs485_test.py for the measured before/after. MEASURED 2026-08-20 19:26 and the PREMISE WAS WRONG, like A0-7. Board 1 (NEW firmware, derived turnaround) and board 67 (OLD firmware) on the SAME RS485 pair through a USB-RS485 dongle, same minute: 12/12 whole replies each, ZERO NUL bytes on either, 693 and 1000 byte INFO replies. The OLD firmware is not corrupting anything, so there was nothing for the turnaround fix to prevent and this run is not proof that it works. What actually caused the corruption that started this task is A13-17: opening the port reset the board and the ESP32 ROM log arrived at another baud as NULs. The turnaround change is still correct on its own terms (flush leaves a byte in the shift register) and it is harmless, but do NOT report it as the fix for the corruption - it was not. CLOSED 2026-08-20 as a WRONG THEORY, not as work done. The task asked to prove an RS485 turnaround fix on a real bus. Measured on the bench the same day with tools/rs485_test.py: old firmware and new firmware both answered 12/12 clean, zero NUL bytes, on the same pair at the same time. The turnaround was never the fault. The corruption was the BOARD RESETTING when the port was opened - an FTDI adapter pulls DTR/RTS however carefully they are cleared, and the ESP32 ROM log prints at another baud, so it arrives as NULs and fragments and usb_cmd handed that back as the reply. That is A13-17, which is fixed and landed. The RS485_SETUP_US/RS485_HOLD_US timing derived from the baud stays - it is correct and costs nothing - but it fixed nothing that was broken. Do not re-test this.\nA13-16: done   2026-08-21 00:20  \u2014 command a module that belongs to the OTHER PC, over WiFi. Asked 2026-08-20 at the bench: *we still can't go to open module or command the module which connect to the other pc and can see through wifi. make can command it through hub when hub connect the same wifi as this hub too make it peer to peer when connect module via module pair wifi too*. Three parts, and the plan already settles part of it: dev=hub:IP/usb:COMx forwards a call to the hub that owns the cable (A2-4), so the machinery exists - what does not is the hub FINDING the other hub by itself on the same WiFi and offering its boards in one list. Second part: two hubs on one WiFi should link peer to peer without anyone typing an address. Third: a module reached through its OWN hotspot (module pair WiFi) should be commandable the same way. MEASURED TODAY: only one hub answered on 8642 across the whole /24, so the other PC is either not running the hub or its firewall blocks inbound - that has to be settled before any of this can be tested SCOPED 2026-08-20 by reading the code, not guessing. Most of it already exists: /api/allmods (main.py:2818) sweeps for other hubs, asks each /api/mine, and rewrites every module address as hub:IP/<dev> so a command forwards to the machine holding the cable - and hub.html already uses it in three places. So the hub PAGE can already list and drive another PC's boards. Two real gaps remain: (1) Nong Studio never calls /api/allmods - it only knows its own hub, so a pose or a show cannot be sent to a board owned by the other PC, which is the case the user actually hit; (2) the other PC has to be answering on 8642 at all - measured today, only one hub replied across the whole /24, so nothing can forward to it. Gap 2 is theirs; gap 1 is a Studio change and needs a design decision first: how a robot picker shows WHICH machine owns each board, without making the common case (one PC, one robot) any heavier.\nA13-17: done   2026-08-20 22:19  \u2014 the hub mistook a rebooting board for a corrupted reply. Opening a port resets some boards however carefully DTR and RTS are cleared first - measured on an FTDI adapter at the bench 2026-08-20 - and the ESP32 first-stage ROM log prints at a different rate from the firmware, so it arrives as NUL bytes and fragments. usb_cmd parsed that as the answer. Now: a fresh handle waits for the line to go quiet before anything is asked, boot output is recognised rather than parsed, and a board that restarted is REPORTED as having restarted instead of as 'no reply' - which had sent us to check wiring that was never wrong. Before 0/10 clean and 679 NULs, after 6/6 clean and 0 NULs, on the same board. check_boot_noise drives it with a port that reboots measured on the bench 2026-08-20: the 30-pin ESP32 on COM10 boot-loops - 4 brownouts and 4 boots in 8 seconds, always right after the SD check, which is where portal.begin() starts the radio (main.cpp:105). It never reaches the command loop, so neither RS485 nor USB could ever see it. Still loops with the MAX485 UNPLUGGED, so the transceiver is not the load: it is the cable, the USB port or that board's regulator. The hub used to call this 'no reply from COM10' and that cost most of an afternoon on bus wiring that was never wrong - it now counts boots and names the power rail. bench 2026-08-20: the 30-pin ESP32 on COM10 boot-loops - 4 brownouts and 4 boots in 8 seconds, always right after the SD check, which is where portal.begin() starts the radio (main.cpp:105). It never reaches the command loop, so neither RS485 nor USB could ever see it. Still loops with the MAX485 UNPLUGGED, so the transceiver is not the load - it is the cable, the USB port, or that board's regulator. The hub called this 'no reply from COM10', which cost most of an afternoon on bus wiring that was never wrong; it now counts boots and names the power rail.\nA15-6: done   2026-08-21 01:16  hw=a second PC (optional - the code path is testable on one)  \u2014 hand the app to the other PC over the LAN, and update it from the hub rather than GitHub. Asked 2026-08-21: a download button on the hub page, so a PC that just connected over WiFi gets the program that CAN see its own serial ports - a browser never can, at any address. What it downloads is MiceHub.exe itself; running it makes that PC a hub, and scan_hubs already finds it with nothing connected by hand. The NEW part is the source: at a venue there is no internet, so the newest build a PC can reach is the hub next to it, not GitHub Built and checked on ONE PC 2026-08-21: /api/app serves this hub's own MiceHub.exe, /api/app/version says its sha and size, the Settings page offers the link only when there IS an app to give, and update_state now asks every hub on the LAN as well as GitHub - preferring whichever build is newer, with do_update fetching from whatever source was chosen. THE TWO-PC BEHAVIOUR IS NOT VERIFIED: everything here ran against a source checkout, where the honest answer is 'this hub has no app to hand out'. What still needs a second PC is the end-to-end run - download from hub A, run it on PC B, watch A find B by itself, then watch B offer A's newer build back. Added to the packing list's second-PC row.\n\n# A18  Holes found by reading the code\nA18-1: done   2026-08-21 05:33  \u2014 SECURITY: /api/ota writes firmware BEFORE the login is checked. allowed(req) sits in the completion handler, but AsyncWebServer runs the BODY handler first - so Update.end(true) has already switched the boot partition when the 401 is sent. Anyone on the venue WiFi can curl -F an image onto a board. Found by an Opus subagent 2026-08-21 and confirmed by reading WebPortal.cpp:1005-1070. Also: the WiFi path uses UPDATE_SIZE_UNKNOWN with no setMD5, so a truncated upload answers OK updated and reboots into a corrupt image - the same hole closed on the RS485 path the same day, armed on one path only\nA18-2: done   2026-08-21 16:58  \u2014 one wedged board must not hang the hub. FOUND ON HARDWARE 2026-08-21: a board on COM26 sent 23,395 lines in three seconds and never paused, and a probe of six ports never returned - five minutes, no answer, no error. Cause is _usb_get in main.py: the settle wait pushed its own deadline forward on every arriving byte, so a cable that never goes quiet held it forever, and because the scan walks every port in turn ONE noisy board froze the whole module list. At a venue that is a hub that looks dead with every other board working. Fixed with an absolute 4s cap on top of the 1.2s settle window. check_chatty_board holds it, driven by a fake cable that never stops talking (fake_serial.FLOOD_PORT, which no fake had ever done - 3120 passing checks said nothing about this). One sabotage, caught. STILL OPEN: why board 85 lift-test is wedged. It answers PING endlessly. Needs a power cycle and a look at its RS485 wiring, and neither can be done from here VERIFIED ON THE REAL WEDGED BOARD 2026-08-21: with the flooding board still on COM26, the fixed hub answered /api/mine in 4.9 s and identified it as id 85 type nong group mice-show. Unfixed, a probe of that port never returned at all - five minutes, no answer, no error\nA18-3: done   2026-08-21 17:33  \u2014 the hub could not update a board over WiFi at all - found on real hardware 2026-08-21. A18-1 moved the board's login check into the OTA body handler, which is right: it refuses BEFORE any byte is written. But the hub never logs in to a board, it just posts the image, so every up-to-date board answered 401 ERR log in first. It looked like it worked before only because the old code wrote the image FIRST and refused afterwards - the update landed, the hub reported an error, and the two cancelled out. Reproduced on board 42 over WiFi, then fixed: the hub logs in to the board (POST /api/login user+pass, cookie mice_board) and carries that session on the OTA, defaulting to the shipped account and saying plainly when a board keeps its own. Verified on five real boards over WiFi: id 1 lift 21s, id 42 nong 16s, id 67 nong 16s, id 89 cam 16s, id 77 cam 14s. THE FIX HAD A BUG OF ITS OWN, also found on hardware: camera id 77 runs firmware old enough to have no /api/login, answered 404, and insisting on a session stranded it on old firmware - the very board that most needed updating. A 404 now means carry on without a session, which is what its ungated /api/ota expects. QC could not have caught either half: fake_wifi took an unauthenticated image happily, so it now demands the session the real board demands and can also play an old board with no login route. Two sabotages, both caught. STILL OPEN: board 85 lift-test has a password nobody here knows, so it cannot be updated over WiFi - USB and the RS485 command channel still work on it (FWBEGIN answers a usage error, not ERR auth)\nA18-4: done   2026-08-21 18:46  \u2014 the camera: every sensor setting, and the picture that did not change. Asked for 2026-08-21 at the bench. FOUR THINGS. (1) It could not stream or snap at all - the sensor ribbon was not plugged in, so every pin map failed and the board reported no camera. Plugged in, it comes up board=ai-thinker sensor=OV2640. (2) *it fix only 1 size*: the size WAS changing, the picture was not. With one frame buffer and GRAB_LATEST the next capture handed back the frame already in flight, so a SNAP right after switching to qqvga returned 15,007 bytes where a real qqvga frame is 1,700, and vga returned 2,127. Two frames are dropped after a size change now and the captures scale properly: 1.7k qqvga, 4.1k qvga, 10.5k vga, 15k svga, all measured on board 77. (3) Every sensor setting is exposed - 24 of them, declared in config/cam_controls.json, generated into the table AND the switch that applies it, so adding one is one entry and no C++. CAM LIST reads them all back with ranges and defaults, CAM SET changes one, CAM AUTO restores every default. Driven on the real sensor: brightness, special_effect, wb_mode, gainceiling all took; agc_gain 99 refused with its range; an unknown name refused by name. NOTE: the OV2640 is FIXED FOCUS - there is no autofocus to expose, and saying so beats adding a control that does nothing. (4) The panel found a crash waiting to happen and the bench confirmed it: sensor_t is a struct of function pointers and a part that lacks a control leaves its pointer NULL, so calling it panics the board rather than failing. Four setters were already called unchecked. Every generated call is guarded now, and CAM AUTO answers 23 of 24 on this part because the OV2640 has no sharpness - which is exactly the case that used to crash. Four sabotages, all caught after two were silent first BOTH CAMERAS VERIFIED ON HARDWARE 2026-08-21 19:45, and they are DIFFERENT SENSORS - which is the case the per-sensor design existed for. Camera 77 (COM30) is an OV2640: CAM AUTO answers 23 of 24 and CAM LIST marks sharpness as missing=1, because that part does not implement it. Camera 89 (COM18) is an OV5640: 24 of 24, nothing missing. Same firmware, same 24 declared controls, each board honest about what it really has. Captures scale on both: 77 gives 3.6k/9.5k/14.1k and 89 gives 2.8k/8.9k/13.9k for qvga/vga/svga. Camera 77's sensor had stopped answering earlier (camera init failed 0x105 on every board layout) and came back after the ribbon was reseated - the board was never at fault\nA18-5: done   2026-08-21 18:46  \u2014 a module going stale must not shove the page around. Asked for 2026-08-21: *when the module it lost connection now it add the line and make below shift down make it not shift down it make user headace*. A row that misses a sweep gains 'last answered 41s ago' inside its meta span, which is a flex child with flex:1 - the text wrapped, the row grew a line, and every row below jumped while the operator was reading. The note is its own flex slot now, one that never shrinks, and the meta line is held to one line so a long address truncates the technical tail instead. check_no_shift measures it in a real browser at 900px AND at 360px, because a wide screen has room to spare and passed with the guard removed. Three of my own mistakes were caught by the check itself before it was trusted: it measured a hidden tab and compared 0 to 0; it measured the list after appending its own rows rather than a row going stale in place; and the first fix clipped the note away entirely, trading a page that jumps for a page that lies. Two sabotages, both caught\nA18-6: done   2026-08-21 19:45  \u2014 the camera settings panel on the MODULE's own website. Asked for 2026-08-21: *make the esp32 cam can setting all everything*, and then *make firmware update the web too, not only in hub* - the board's own page still showed the old four controls (size, quality, flip, mirror) while the firmware had 24. The card builds itself from what the board answers to CAM LIST, so a control added to config/cam_controls.json appears on the page with no page code touched: the answer now carries each control's group, label, whether THIS sensor has it, and what it depends on. Gemini Pro designed the card (four groups, templates per control kind, one Auto All); the grouping is DATA rather than JavaScript so a new control lands in the right group by itself. Controls the sensor lacks are shown disabled saying so, and ones that only apply while another is off (exposure time, gain) say which\nA18-7: done   2026-08-21 19:45  \u2014 watch a camera without opening the module. Asked for 2026-08-21: *how to go to camera view while not go to the module do you have tool?*. Opening a camera meant opening that board's entire website and finding the Camera card - right when configuring a board, wrong when you only want to see what it sees. It is an APP, which in this project means one entry in apps/camera/app.json and nothing else to edit: the hub lists, serves and links it because the folder is there. It lists every camera the hub can reach from its own two lists (cables and WiFi), merges a camera reachable both ways into one, and shows them together. Several people can have it open at once because the live view goes through the relay (A13-11)\nA18-8: done   2026-08-21 22:35  \u2014 check_no_undefined_names is fragile under load and has cost three gates in one day. It drives several pages in one browser run and fails with reject:Failed~to~fetch when the machine is busy - 6/6 passing alone in 18 s, 4/6 in 62 s under a full gate. A fetch that never completed says nothing about undefined names, which is what the check is for, so it should tell a connection failure apart from a page that threw: retry the fetch once, and if it still cannot connect say the machine was too busy rather than reporting the page as broken. A red gate that is really contention sends the next half hour into code that was never wrong DONE 2026-08-21. The check now classifies before it judges: a transport failure (failed to fetch, a network error, a page that never loaded) gets ONE reload, and if it still cannot be reached the page is reported NOT PROVEN rather than broken - a pass that says out loud that nothing was tested. A throw from the page's own code is never retried and never softened. Three probe iframes make both halves happen on purpose in a real browser: busy probe fetches a dead port on every attempt and must come back busy, busy once does it only on the first attempt and must be rescued by the reload (tries=2), throw probe calls a name that does not exist and must still be reported as a throw and never retried (tries=1). Three sabotages, three caught (no retry, a fetch failure counted as a page throw, every error softened to busy). Same 17s runtime as before, because the reload only happens for pages that could not be reached.\nA18-9: done   2026-08-21 22:35  \u2014 the stale exe trap: MiceHub.exe goes out of date whenever main.py changes and nothing says so. Measured 2026-08-21: the hub answering on 8642 was main_python/MiceHub.exe from the day before, missing pairing, the chatty-board fix, the bus group carry and the OTA login - and the only reason it was noticed is that its password did not match. The hub should say plainly, on its own page, when the running build is older than the source it was built from (it already knows both: /api/selfupdate reports frozen and the running sha). Related and worse: the rebuild command written in CLAUDE.md built a BROKEN exe and overwrote MiceHub.spec while doing it - fixed 2026-08-21 and guarded by check_onefile, but the exe itself still has no way to say it is stale PROVEN ON A REAL FROZEN EXE 2026-08-21 22:36, not only against a fake tree. MiceHub.spec now calls build_stamp.write at build time, which records a sha for every source file that went in (AREAS covers main_python/*.py, the three web folders, firmware/config, apps, tools/registry.py, WebUI.h and the icon) and bundles it as build_stamp.json. Measured on dist/MiceHub.exe: straight after the build /api/version answers built 2026-08-21 22:36 stale false; adding ONE new source file (main_python/zz_probe_stale.py) flips it within the 30s cache to stale true with the sentence naming that exact file; deleting it goes quiet again. /api/diag carries the same line. Content, never mtime - a git checkout rewrites every mtime and would cry stale on a tree identical to the build, which check_stale_build proves by touching a file to epoch 0 and expecting NOT stale. No source tree beside the exe (a venue laptop) means no claim at all, guarded by MiceHub.spec being the marker rather than a stray main.py. hub.html shows it as a banner warn with no dismiss button, painted BEFORE the version baseline returns - a stale exe is true on the first answer, not only after the web files change. Six sabotages, six caught (mtime instead of content, always stale, the password store counted as source, the paint call deleted, the banner element renamed, and the spec no longer writing the receipt).\nA9-3b: done   2026-08-21 22:35  \u2014 the keyboard-only pass, without a TV. A9-3 is blocked on a TV remote, but the half that needs no hardware is real: every control on the hub and the module site reachable and operable with Tab and Enter alone, focus visible at every step, and nothing that can only be done by pointing. Doable on this PC with a browser DONE 2026-08-21, on this PC with a real browser. check_keyboard_only walks the hub, the module site, Nong Studio and the rgb page, opening EVERY tab (77/53/72/17 focusable controls counted, floored well below that so a page that stopped being walked fails instead of reporting no problems). Three real faults found and fixed, none of them visible in any of the 3300 other checks: (1) the Studio timeline was mouse-only - a keyframe could only be selected by clicking its chip, so the timeline could not be walked at all. The move number is now a real button (Gemini Pro chose that over making the chip tabbable, because the chip contains a text field and two number fields and a control that contains controls takes the focus its own fields need); the chip keeps its click and declares data-keyboard=.kgo, which the check reads. (2) .kname:focus carried outline:none, so the move-name box - the field a keyboard reaches most - looked identical focused and unfocused. (3) the module site's file list built delete as an <a> with no href: it answered a click and could not be reached by Tab. That one was missed by four solo runs and caught by the gate, because the fake's file rows had not arrived yet - the check now settles 700ms after each tab before looking, and a sabotage putting the <a> back is caught every time. Nothing else on any page is pointer-only. Five sabotages, five caught. The TV half (A9-3) still needs a remote.\n\n# A19  Lists that must never come back empty\nA19-1: done   2026-08-21 23:47  \u2014 todo the Tools list must be impossible to lose. The user hit *Could not read the tool list. The hub may have stopped. m is not defined* on 2026-08-21 22:51 - a ReferenceError from the m.type line that was fixed in 97df38a on 2026-08-20 11:09, running in an exe built at 01:36 the same day. The fix is nine hours older than the running program, which is exactly the stale-exe trap A18-9 was written for. So: prove the current page really draws tool rows in a browser (check_tools_list reads source today), and make the error say WHICH build it is\nA19-2: done   2026-08-21 23:47  \u2014 todo Tools first, and reachable from Home. The user: *make the tool as the first tab in the home to click easy to go to the tool too*. Today Tools is the fifth tab and Home offers only Open Modules\nA19-3: done   2026-08-21 23:47  \u2014 todo ONE module list, each module once. The user: *the module make it list the module only once for me please the other like this pc can see the module from which module or other make it below make it list once show which module arviable on the top and make can connect and control insite the module*. Today the Modules tab has two lists - Modules found over WiFi and On other PCs - so one robot appears twice, and which PC holds the cable is a heading rather than a detail on the row. Gemini Pro 2026-08-21: one unified list at the top, each unique module once, the row carries name, status and an Open button, and the routing detail (which PC, or which module it is reached through) goes UNDER the row DONE 2026-08-21. The merge moved into the hub: modules_everywhere() in main.py joins this PC's own list with every other hub's /api/mine using board_key - the same rule that already merged a cable and a WiFi route into one row - and /api/modules/all serves it, each board once, every way in kept as a route carrying which PC it goes through. The page draws ONE list and puts the ways in UNDER the row (reached cable COM9 this PC - over WiFi - through LAB-LAPTOP), so which machine holds the cable is a detail on the module rather than a second card of modules further down. The On other PCs card is now one row per PC, not per module. Two things the gate caught that a solo run did not: asking for everything in one call made the list wait on a whole-network sweep before it could draw the board on this PC's own cable, so the local list paints first and mergeFar() repaints when the wider answer arrives; and that second answer could land ON TOP of a newer scan's error banner, so a scan generation counter now discards a sweep that has been overtaken. Addresses in the new line stay inside .tech, which the technical-detail switch hides - check_advanced caught an IP printed in plain text. check_one_module_list drives it with a fake second PC reporting the SAME chip; five sabotages, five caught\nA19-4: done   2026-08-21 23:47  \u2014 todo Home shows the modules, not a count of them. The user: *the home it look noting show show anything show only connected each module and connect from what it hard to use*. Today Home is one sentence and one button\nA19-5: done   2026-08-21 23:47  \u2014 todo the tools work with no login; the login is for commanding the robot. The user: *make the tool can use everytime like the nong studio it not the program to control the robot only it can connect to make the sequence before having the robot... but when need to command the robot need to use the login*. Today /api/save, /api/export, /api/model/upload and /api/rigdefault are gated, so a sequence cannot be built without a password even though nothing reaches a robot DONE 2026-08-21. /api/save, /api/export, /api/model/upload and /api/rigdefault left GATED and are now in NEVER_GATED: none of them reaches a board, so posing, timing, saving a project and exporting a YAML all work with no password - which is most of what Studio is for before a robot exists. Everything that reaches a MODULE is exactly as gated as it was (usb/robot/dev cmd, OTA, uploads and deletes on the board, flash). What it costs is written where the decision is: anyone on the venue WiFi can write a project or a sequence into this PC's own folders. check_hub_auth now drives both halves from outside with no session - the gated ones must 401, the tool ones must not - and a sabotage putting /api/save back behind the gate is caught\nA19-6: done   2026-08-22 05:12  \u2014 doing Home first, and simpler than the Modules tab. Asked 2026-08-22: *make home tab become first before tool* and *make the home show the each module once easy to use but in module you show like before which from what*. So the tab bar goes back to Home, Tools, Modules... with Tools second rather than fifth, and the Home rows drop the reached-via line: on Home a module is one row you can open, and the detail of which cable, which PC and which bus stays on the Modules tab where it was asked for\nA19-7: done   2026-08-22 05:12  \u2014 doing Home counts them per way in, and nothing is drawn twice. Asked 2026-08-22: *make it show like before in the home (the show that show which module connect now from wifi how many from rs485 from usb)* and *show each module once no dupicate or more than that i found the problem it show more than 1 time that why i consern this user don't know which module*. USB and RS485 were one number (on a cable); the cable card below the list drew a full second row for every board already in the list above DONE 2026-08-22. Home counts per way in again - N modules, reachable: 1 on a USB cable, 1 on the RS485 bus, 1 over WiFi, 1 through another PC - instead of lumping USB and RS485 together as on a cable, which hid the difference that matters when a board is missing (one is in your hand, the other is reached THROUGH another board). THE DUPLICATE THE USER SAW was the cable card: it drew a full module row for the board on the port AND for every RS485 board behind it, both of which are already in the one list at the top of the tab - a bench of two boards read as four. Both are now one line saying which board answered there and that it is in the list above; hotspot peers stay full rows, because they are NOT in the merged list and that card is their only way in. The check counts .mod .nm across the WHOLE Modules tab and fails on any name drawn twice; it caught nong-far (the RS485 board) the moment it was made to wait for the cable card to finish probing - counting before that measured a page with nothing in it. Two sabotages, both caught. The first card is no longer called Modules found over WiFi, which stopped being true when it started carrying every way in\nA19-8: done   2026-08-22 05:12  \u2014 doing a module that renames itself. Asked 2026-08-22: *if that module change it own name what happen? fix this case too* DONE 2026-08-22. Answer: it stays ONE row and takes the new name, because the merge is on the chip (board_key), which cannot be renamed - proven in a browser by renaming a board between two scans and asserting four rows with the new name and no trace of the old one. What was really broken is what happened NEXT: a WiFi record kept for the grace period after the board stopped answering there was still allowed to write its old name over the fresh one from the cable, so the row flipped between the two names every few seconds and the person who had just renamed it could not tell which board they were looking at. A late answer can now only ADD a field nothing else knows (its ip), never overwrite one. Also fixed: the last-opened button on Home showed the name the board had when it was opened, so a rename left it offering a robot that no longer exists by that name - it now looks the name up by address, which a rename does not change. Two sabotages, both caught\n\n# A20  Nong Studio, and a nong on two boards\nA20-1: done   2026-08-23 13:15  \u2014 Nong Studio: clicking a move takes the timeline to that move's TIME, not only its pose. Asked 2026-08-22: *when click on the move in nong studio go to the time which have that move too some time need to check the move afer go to that move in live control*. Today selectKey() (app.js:1649) sets the pose and travels there, and leaves playT alone - so the scrubber and the clock stay where they were, and the next play or the live monitor carries on from a different point in the show than the one on screen. What it needs: the cumulative time of that keyframe (the same sum totalMs() already walks over playKeys(), counting t + hold), written into playT and into the #scrub slider, with the suspended keyframes skipped exactly as playback skips them. Check: click move 3, assert playT equals the sum of the moves before it, that the scrubber moved, and that pressing play from there starts at move 3 rather than at the beginning AND AT THE MOVE'S OWN SPEED, asked 2026-08-22: clicking a move travels there under the speed limit - the per-move dps override first, then the sequence speed, never below the physical floor (keyMin/slowestDps, which mirror NongModule::minDuration). Asserted on what reached the module, not on the UI landed 2026-08-23: gate green 3411, staging vs main byte-identical (--diff), but land.py printed REFUSED wrongly - see A22-2. Marked done after manual verification of the copy\nA20-2: done   2026-08-23 13:50  \u2014 play a sequence that is ALREADY SAVED - on the board's SD card, or on the hub PC - and watch it in Studio's 3D the way the live monitor does. Asked 2026-08-22. What already exists, so this is smaller than it sounds: Studio can LOAD a saved sequence back into the timeline from both places (editLocalSeq -> /api/loadseq for the hub's sequences/ folder, editSdSeq -> sdDownload for the board's /moves), the hub has a show clock that drives a sequence over any transport (ShowPlayer, POST /api/play with steps), the board can play one off its own SD with MOVE <file>, and the live monitor already samples the board's real pose into the 3D. What is missing is the one press that joins them: pick a saved file and play it, with the 3D following - and the choice of WHICH clock runs it. Two honest options to decide when it is picked up: (a) the hub's clock - load the file into the timeline and play it exactly as an edited sequence plays, which gives scrubbing, pause and the collision guard for free; (b) the board's own clock - send MOVE <file> and let the monitor follow, which is what a show does when the PC is unplugged, and cannot be scrubbed. Both are worth having, and (a) is the smaller step\nA20-3: blocked  2026-08-25 15:58  hw=2 nong boards + RS485 pair + servos on both (a whole nong is better)  \u2014 a nong driven by MORE THAN ONE ESP32, for when the pins do not fit on one board. Asked 2026-08-22. THE MECHANISM ALREADY EXISTS IN THE FIRMWARE and has never been run on two real boards: a nong has a link flag and a peer id (NongModule.cpp:51-53, reported in INFO at :833 and as the 'link' capability at :797, settable from the module website - the 'this board is the leader' checkbox at WebUI.h:282 and CFG link/peer at :697); a leader forwards the RESOLVED full pose and its duration to its partner over the RS485 bus (NongModule::forward, :311 - '#<peer> POSE ...' to one board, '#*' to all), which is exactly why every sequence step carries an explicit T; and any joint whose pin is -1 is simply not driven on that board (pins_[i] < 0 is skipped in attach, write, LIMIT and RELAX), which is how the two boards divide the ten joints. So the answer to *can it use more than 1 esp32* is YES, by design, and what is missing is proof and the way in: (1) two boards on one RS485 pair, leader with joints 1-8 and partner with WAIST+SHRUG, moved together and measured with POSE? on both; (2) how far apart they drift over a long sequence, which is the number no fake can produce; (3) a way to SEE it - the hub's module list shows two rows for what is one robot, and neither says it is half of one; (4) the pin map is the only place ownership is written, so a wrong -1 silently means a joint nobody drives - it needs a check and a plain warning. Until it is measured, treat linked-nong as unproven rather than working WIRING MAP for the two-nong RS485 link test (pins as the boards themselves report in INFO, 2026-08-25): joint order is 1 L_SH_P=GPIO32, 2 L_SH_R=GPIO33, 3 L_EL_P=GPIO25, 4 L_EL_R=GPIO26, 5 R_SH_P=GPIO21, 6 R_SH_R=GPIO22, 7 R_EL_P=GPIO27, 8 R_EL_R=GPIO14, 9 WAIST=no servo wired (-1), 10 SHRUG=no servo wired (-1). Servo signal = orange wire to that GPIO, red to 5V, brown to GND. RS485 between the boards: MAX485 A terminal -> other board A terminal, B -> B, GND -> GND (RO=16 DI=17 DE/RE=4 are already wired on the driver board). TEST: #85 COM26 is leader, #67 COM29 partner; leader runs JOINT 1 (L_SH_P left shoulder pitch, GPIO32) 90->120->70->130->90 and #67 must copy every move over the wires; drift measured at the end. Boards already flashed with today's firmware. SERVO INVENTORY per the user: #85 (leader, COM26) has R_SH_P wired; #67 (partner, COM29) has WAIST on a dummy servo. Forward sends the SAME joint index to both, so the test drives JOINT 5 (moves #85 shoulder) AND JOINT 9 (shows #67 waist copying). Both boards currently report waist pin = -1: if the waist does not move, map it with PIN waist <gpio> (reboot to apply) - need the GPIO the dummy waist signal wire uses. PROBE 2026-08-25 15:05: link CFG sticks (link cap shows in INFO) but ZERO cross-board traffic - #* PING and #85/#67 directed pings heard nothing on either console. Learned: bridge() answers self-addressed commands LOCALLY (RS485Bus.cpp:146) so earlier self-tests proved nothing about the PHY. Both boards share the PC USB ground, so prime suspect is A/B swapped between boards; asked user to swap. Next isolation step if still dead: single-board A-B jumper loopback. PARKED at the user's call 2026-08-25 ~15:30: wiring checked correct by the user (A-A B-B), tried BOTH orientations, still zero cross-board traffic while each board's local paths are perfect. Working theory per user: one MAX485 transceiver module is dead. Next isolation step when wanted: move the pair onto the USB-RS485 adapter (COM23, CH340) as a listener and ping from each board - whichever side never reaches the dongle owns the fault. Everything else about the task is ready: both nongs flashed current fw, link CFG persists, bench_a20_3.py drives JOINT 5 (#85 R_SH_P) + JOINT 9 (#67 waist dummy). User has NEW hardware: one console now uses an XY-485 module (was the suspect MAX485). Mixed MAX485 + XY485 on one bus is fine - both are plain RS485 transceivers; rules stay A-to-A, B-to-B, common GND, 115200. User will say when wiring is complete. Prepared bench_busfind.py: auto-lists COM ports, finds which carry a bus console by broadcast PING, then cross-pings the two consoles - no configuration needed. MAX485 and XY485 are BACKUPS for each other by user choice - interchangeable on the bus, no software change. bench_busfind.py written (root): auto-lists COM ports, finds consoles by broadcast PING, cross-pings between consoles. Run when user says wiring is done; hub must be closed. MEASURED 2026-08-25 18:0x, new wiring with XY485+MAX485: USB-RS485 dongle on COM23 sent #67 PING on the shared pair and got @67 PONG back - 67 side works BOTH directions. #85 never hears and never transmits (dongle->85 silent; 85 broadcast silent on dongle). All five USB boards alive on own cables (ids 1,77,89,85,67). Fault isolated to the 85-side transceiver/jumpers (VCC, RO->GPIO16, DI<-GPIO17, DE/RE->GPIO4, A/B). Swap test offered: put the known-good 67-side module on the 85 spot. USER GO 2026-08-25 ~18:4x: okay now you can use it to test - all we need rs485 and nong by use 2 esp32 and flash esp32 via rs485 by not flash from usb and the thing that we need to test with this. Bench session live: re-probe bus first (did the swap fix 85?), then flash-over-RS485 (A13-6 path) then leader/partner JOINT 5+9. USER SCHEDULE 2026-08-25 ~18:56: test the hardware first i will in the lab to 19.50 and then stop all task then i back to dorm and you can do all thing that not need hardware after that. Lab window closes 19:50 - all hw tasks stop then, PC-only work after. USER ASK 2026-08-25 ~19:10, own words: okay let try again and you can change the name of all board - (a) retry the bus after replugging every USB cable, (b) Claude may RENAME all boards and choose sensible names. Names to be applied after the bus test so bench scripts that quote old names stay truthful. RENAMED ON HARDWARE 2026-08-25 ~19:2x over each board's own cable (SET NAME, verified by INFO): id85 lift-test -> nong-leader, id67 nong -> nong-partner, id1 stays lift, id77 cam -> cam-a. id89 (was cam-two -> cam-b) DID NOT TAKE - board went silent after the USB replug (no answer within 8 s on COM18), needs power/cable reseat at the bench. BUS AFTER THE USER'S A-A/B-B/GND FIX: dongle COM23 still hears ONLY 67 both ways; 85 never heard nor answers - fault remains on the 85 rig's transceiver/jumpers; swap test is next. MEASURED 2026-08-25 ~19:35 - BOARD 85 IS DEAF, not just mute: watched nong-leader's own USB console while blasting #* PING through the dongle; firmware surfaces every heard @reply on USB (RS485Bus.cpp:38 busLine_). Dongle heard @67 PONG x3; 85's console showed ZERO bus bytes. RX path broken before the UART: suspect order = (1) XY-485 VCC/power LED at 85, (2) A/B silkscreen possibly swapped on the XY-485 brand - swap pair at ONE end only, (3) RO->GPIO16 jumper, (4) swap in the known-good MAX485 from 67's spot. Mixed-brand note for the user: MAX485 and XY-485 are the same signalling, mixing brands on one bus is normal; difference is direction control (MAX needs DE/RE on GPIO4, many XY-485 are auto-direction) and VCC (MAX485 wants 5V). FINAL BENCH STATE 2026-08-25 ~19:55, window closed - ONE clean simultaneous run settled it: dongle broadcast x6 -> @67 PONG 6/6; boards 85 (COM26) and 42 (COM28) consoles watched at the same moment showed ZERO bus bytes EACH. So today, across every swap the user did (XY-485 on 85, XY-485 on 42, MAX485 back), exactly ONE node ever worked on the pair: nong-partner id67 + its MAX485. Common factor for the dead spots = their jumper wires / rig wiring, not any one ESP32 (two different boards failed identically) and probably not only the XY-485 (a MAX485 on 85 also stayed silent). RESUME HERE NEXT SESSION: replace the four jumpers at the dead spot with fresh ones or move 67's ENTIRE working set (module + its own wires) onto the other rig; then rerun probe_final.py equivalent; then bench_a20_3.py leader/partner JOINT 5+9. BENCH CLOSE 19:58 per user. BREAKTHROUGH tonight: after the user's final rewiring BOTH boards live on the pair - leader #85 heard partner's PONGs on its own console AND dongle heard @85 PONG 6/6; #67 POSE? asked FROM THE LEADER'S CABLE returned the full pose over RS485 - first true board-to-board traffic of the project. CFG link 1 + peer 67 sticks (link cap YES). NOT WORKING YET: forwarded JOINT never moved the partner (stayed 90) though NongModule.cpp:399 forwards on JOINT and PHY works both ways - last diagnostic (watching both consoles during one forward) crashed on a double-open of COM26 before sending anything. RESUME: rerun that forward-watch, then bench_a20_3.py end-to-end. CODE HALF RE-VERIFIED 2026-09-03: the split-over-two-boards mechanism is in NongModule.h:37-46,208,236 - same firmware on both, link: picks leader/follower, peer: <id> addresses one partner, peer: 0 broadcasts #* to all. Nothing to write; it needs 2 nong boards on an RS485 pair to be proven.\nA20-4: done   2026-09-07 17:50  hw=1 nong board + I2S amp + speaker + SD card  \u2014 the nong gets a SPEAKER. Asked 2026-08-22, as the robot's half of the voice AI (A20-5): it has to be able to answer out loud. Today the speaker belongs to the lift - AudioPlayer is in firmware/src/core/ and only LiftModule.h uses it, and the nong pin header says so in as many words: *no encoder, no RGB strip, no speaker - those pins belong to the lift wiring*. So this is a pin question and a build question, not new code: which GPIOs are free on a nong once SD (5,18,19,23), RS485 (16,17,4) and ten servo pins are taken - I2S needs three - whether the audio is played by the board off its SD or streamed from the hub, and what MUTE/STOP means for it (the STOP that reaches every board must silence it too). Related: A7-4 already wants AudioPlayer moved into modules/lift/, and that move should leave it usable by nong rather than locked to one type PIN AND BUILD QUESTIONS ANSWERED 2026-08-27 by A24-16, without a board: which GPIOs are free on a nong = 0, 2 and 12 only, and the MAX98357A the user chose takes exactly those three (BCLK 12, LRC 0, DOUT 2); an analog amp needs one of them instead. The board plays from its OWN SD card, not streamed from the hub - AudioPlayer reads /music through SDStore under the SPI mutex. What MUTE/STOP means is settled too and landed as A24-18: STOP, a linked partner's forwarded STOP, and MOVE STOP all silence the speaker through Module::silence(). REMAINING here is bench only: wire an amp to a real nong, play a file, and check that GPIO0 carrying LRC does not hold the board in download mode at reset (add a 10k pull-up to 3V3 if it does) CODE HALF RE-VERIFIED 2026-09-03: the nong already owns a speaker in software - NongModule.h:8,126,152 holds an AudioPlayer, NongModule.cpp:235,345,775-781 begins it, loops it and answers PLAY/VOL/AMP, and firmware/config/amps.json is the one place an amp is described. Needs the I2S amp, speaker and SD card on a real board. PROVED ON THE REAL BOARD 2026-09-07, nong id 67 (chip B4BFE91C5A74) on WiFi 10.126.95.70, MAX98357A + speaker wired by the user. Four measurements a fake could never give: (1) upload over WiFi works - a 132344 byte wav landed in /music and the board listed it back at exactly that size; (2) the board's STORED pins had bclk and dout swapped against today's firmware default (it held bclk 2 / dout 12, the user wired bclk 12 / dout 2) and the first PLAY was silent - no rewiring, three PIN commands and a REBOOT fixed it, which is what putting the pin map in data buys; (3) with the pins right the speaker plays - the user heard the 440 Hz tone; (4) the board's HTTP server stops answering for the length of the file while the decoder holds the SD card, so a page polling status looks dead mid-track. Also learned: this board's firmware PREDATES the amp table - AMP and VER answer unknown cmd - so amps.json is not on it yet (see A24-25)\nA20-5: done   2026-08-23 14:41  \u2014 a VOICE app: someone talks to the rig, a local AI answers, and the nong reacts. Asked 2026-08-22, with the prototype that already works named: E:/final_proj/mice/code/llm/test.py - faster-whisper (small) for Thai speech, Qwen3.5-4B through transformers with 4-bit quantisation for the answer, edge-tts th-TH-PremwadeeNeural for the voice with an md5 mp3 cache beside it (llm/tts_cache), a fuzzy-matched FAQ in llm/qa_data.json answered WITHOUT the model when it scores over 0.75, push-to-talk on Enter, and the Qwen think block stripped. That file is the starting point and the thing to keep: the FAQ-first design is what makes it answer a venue question instantly on a laptop. Turning it into part of this rig, in the project's own shape: it is an APP (one folder under apps/ with an app.json, the way Camera and Studio are, so the Tools list picks it up by itself) whose page talks to a SEPARATE local service - the hub is stdlib-only and frozen into one exe, and torch plus a 4B model can never live inside MiceHub.exe. The mic belongs to the PC running that service, which is why the sharing question below matters. TO DECIDE WHEN IT IS PICKED UP, all genuinely open: (1) ONE brain or many - one hub runs the model and the mic and every other hub asks it over WiFi (one GPU, one voice, one set of answers, and the robots keep working when it is off), against every PC running its own (works with no network, needs a capable PC each, and each answers differently); (2) THAI and ENGLISH - whisper is told language=th today, and both languages means either detecting it or a switch, plus a second TTS voice; (3) edge-tts is Microsoft's cloud, so it is not local and not available at a venue with no internet - a local voice (piper) is the honest fit for a robot that has to work in a hall, with edge-tts kept for when there is internet and better sound; (4) what the AI is allowed to DO - answering is one thing, moving a robot is another: a spoken command that reaches a servo goes through the same login gate as every other command, and the safest first version is answer-with-voice plus a fixed list of named sequences it may play; (5) the nong needs A20-4 (a speaker) to answer from the robot rather than from the laptop, and the lift or another module can join the same service later, which is what the app shape is for SPLIT 2026-08-22 into A20-5 (shell) + A20-6..A20-12 so every part can be checked on its own. This entry is now only the shell: apps/voice/ with an app.json so Tools lists it by itself, the service grown from llm/test.py, and a hub proxy at /api/voice/* the way cam_relay fronts the camera - the hub never imports torch and says plainly when the service is not running. TWO LAYERS: the store is data (config/voice.json + qa_data.json), the surface is a SCREEN (A20-12) - the people using this are designers and nothing may need a file to be edited\nA20-6: done   2026-08-23 15:26  \u2014 the voice test bench, BEFORE the accuracy work: ~25 real questions recorded as WAVs with their expected text, and a script printing word error rate, answer-hit rate and per-part latency. Nothing after this may be called better without moving one of those numbers\nA20-7: done   2026-09-12 05:53  \u2014 speech to text, local, measured, and NOT Thai-only: the language comes from the store or is detected, never the hardcoded language=th the prototype has. Bias words per language, condition_on_previous_text=False, and a measured choice between medium, large-v3 and a fine-tune. A second language must be one entry in the store and no code. The prototype's small model mishears and then answers the wrong thing, which is what started this QC fix: added check_voice_stt and check_voice_tts to the SOLO list in run_qc.py - they spawn the real helper subprocess and crashed with WinError 10061 under parallel load (the helper was starved past its connect window), same signature as the check_voice bug from 2026-08-25\nA20-8: done   2026-08-23 16:32  \u2014 the answer: FAQ first, the model only when nothing matches, and a near-threshold match ASKS AGAIN instead of answering - a confident wrong answer in front of a visitor is worse than asking twice\nA20-9: done   2026-08-23 23:42  \u2014 text to speech, local: edge-tts (Microsoft cloud) and mpg123 (Linux-only) both go. Voices come from the store, the md5 cache and FAQ pre-build stay, and the page plays the audio until the nong has its own speaker (A20-4). MMS voices are non-commercial - check the licence\nA20-10: done   2026-08-24 00:38  \u2014 an answer that moves the robot: optional move and module per answer, and the PAGE plays it through /api/loadseq + POST /api/play because the page is what holds the login. No session, no movement; the service never holds the password\nA20-11: done   2026-08-24 01:20  \u2014 one brain, many mics, one robot anywhere: other hubs ask the main hub over WiFi, the robot is chosen BY NAME from /api/modules/all so it can be plugged into any PC, and moves are serialised per module - a second request while one plays is answered in words, never queued. STOP stays ungated\nA20-12: done   2026-08-24 03:09  \u2014 the voice SETTINGS SCREENS, because the person changing this is a designer: Answers (add an answer, the ways people ask it, hear it, pick a robot move from a dropdown of sequences that exist), Voice and language (on/off, a voice with a Try it button, the speech model as fast/balanced/most accurate rather than ids), Words it gets wrong (one box, one Add button), Advanced (ids and thresholds behind the technical-detail switch). Reading open, saving gated like /api/settings. Adding an answer with a move, a language or a bias word must work by clicking alone - no restart, no file edited\n\n# A21  The rules the project holds itself to\nA21-1: done   2026-08-24 03:36  \u2014 TWO RULES INTO CLAUDE.md, so every session follows them without being told. (1) EVERY SCREEN IS FOR A DESIGNER, NOT A PROGRAMMER - asked 2026-08-22: *all of the website in this project are designer use not programmer... if find the web which it hard to use make it simple*: anything settable is clickable, plain words on the surface, technical detail one click deeper (the switch the hub already has), simple on top and complete underneath - the Home and Modules split as the pattern. (2) ANYTHING THE USER ASKS FOR IS WRITTEN INTO THE STATE BLOCK THE MOMENT IT IS ASKED, in their own words, before the work starts - asked 2026-08-22 after having to ask several times: *make sure you will update the plan.html everytime too i need to told you everytime*. If the user ever has to ask whether the plan was updated, the rule was broken. check_designer_first holds the first, with the hard-word list kept in DATA so the next word costs no code\nA21-2: done   2026-08-24 11:12  \u2014 the technical-detail switch on EVERY page, not only the hub: module site, Nong Studio, rgb, camera and help - same control, same memory, same .tech convention, so addresses, ids and ports are hidden by default everywhere and one click away when somebody needs them\nA21-3: done   2026-08-24 18:00  \u2014 errors a designer can act on: every failure says what happened and what to do, with the exception text, status code and path moved behind the technical switch. No raw exception or bare status code outside .tech on any page, and every error still offers the control that fixes it\nA21-4: done   2026-08-25 11:36  \u2014 the help page rewritten for a designer: short sections saying what a thing does and how to use it, with the technical part its own click-to-open area rather than mixed in. The existing coverage rule stays - every command, module type, servo preset and QC area still has to be described\nA21-5: done   2026-08-25 01:31  \u2014 the bug report a designer can send: /api/diag becomes two halves - a plain summary (what is connected, what failed, what to try) that reads without any ids in it, and the technical bundle behind a click, still carrying everything support needs LANDED 2026-08-25 01:10 in batch with A21-6+A24-1 (gate 3595/0). Plain-words diagnostics half + DIAG_TECH_MARK split, tech behind .tech switch. Post-QC panel fixes all applied and sabotage-proven: disp_name strips IP-shaped names (unnamed WiFi module carried its address AS its name), showDiag slices after the marker, check reads DIAG_TECH_MARK from main.py not a copy, leak regex gained /dev/ paths.\nA21-6: done   2026-08-25 01:11  \u2014 a REPORT button anyone using the hub can press, asked 2026-08-22: *make the report button in the hub make everyone who use the hub can complain*. On every tab, the way the STOP control is, because a problem is noticed wherever you happen to be. One box - what were you doing, what went wrong - in whatever language the person speaks, no login (complaining must never need a password), and it saves the page it was pressed on, the module in front of them, the build and the time, offering to attach A21-5's plain summary. Stored one file per report beside the hub's other machine-local files, and NEVER promoted - it belongs to the machine, not the source LANDED 2026-08-25 01:10 (code-cc built JS+modal+/api/report; Claude fixed the landing blocker). The handler's local import json inside the api() router made json local to the WHOLE function - pairing, atomic saves, OTA and play all answered 500 UnboundLocalError: 33 red checks across four unrelated areas from two lines. Fixed by module-top imports; qc/checks/check_handler_scope.py guards the class (dispatcher + local import of an owned name), proven by sabotage.\nA21-7: done   2026-08-25 01:53  \u2014 the complaints screen: newest first, one short line each with the whole report one click deeper, and fixed / not-a-problem to close one. A closed report leaves the list without being deleted, because the next person to hit the same thing needs to find it\nA21-8: done   2026-08-25 12:36  \u2014 ANY LANGUAGE in, English for whoever fixes it, asked 2026-08-22: *after complain we will correct that complane and translate it to english before we fix it later but keep the thai word to reduce the token use when do the task* - and then *include other language too not only thai*. The language is detected and recorded, the report is translated LOCALLY by the same model the voice app loads (never a cloud translator), and the ORIGINAL is kept exactly as written, whatever language it is in. tools/reports.py prints the open ones in English so a work session reads three short lines instead of three paragraphs, and turns them into plan entries. With no local model the report is stored untranslated and says so, rather than guessing at what somebody meant\nA21-9: done   2026-08-25 18:23  \u2014 the tools must not die on a Thai word, and must not print characters a console cannot draw. Found 2026-08-22 when the user asked why the output said question marks: plan.py printed an em dash and a middle dot, which this machine's cp1252 console mangles - and worse, printing ANY Thai raised UnicodeEncodeError, so a Thai note or report would have changed the file and then killed the command, which reads as it did not work and gets run twice\n\n# A22  Every file read for bugs, before the boards go back\nA22-1: done   2026-08-26 12:57  \u2014 when EVERYTHING that needs no hardware is complete: check ALL of the code for bugs, every single file, and make sure the web is easy to use - including the module website. Asked 2026-08-23: *add to the plan when do everything conplete for none hardware please check all of the bug insite my code everysingle code and make sure the web it easy to use include module website*. The sweep covers hub Python, firmware C++, Nong Studio and every page the hub serves (module site WebUI.h, rgb, camera, help) - not only the files a task touched. Ease-of-use follows A21-1..A21-8 on every page: plain words, clickable, technical detail one click deeper SWEEP DONE 2026-08-25: five-model panel on WebUI.h + 12-agent workflow over 6 areas = 60 CONFIRMED findings, 3 refuted, full list in docs/a22_sweep_findings.json. Headliners: hub-to-hub forwarding ALWAYS 502s (dev_route NameError main.py:3341), /ws executes commands without login (WebPortal.cpp:658), AUTH+GROUP are query:true so runnable without login, /api/report unauthenticated write with client-controlled filename prefix + no size cap, /api/flash/bus ungated, stopall can wait 20s behind a USB scan before stopping boards, ShowPlayer.stop can leave two clocks running, module-site Report button calls undefined openReport(). Fixing worst-first in staging with sabotage-proven checks per fix. WebUI flash-model claims judged against code: REAL = loadPeers g out of scope in own catch (:1279/1295), delete-button fetch no error feedback (:1244), loadFiles missing empty-folder row; FALSE = svJoint onchange (nongStatus polls it every 500ms), loadList value='' (options disabled+value empty+if(v) guard), /api/status catch (WS push re-renders same state). Three small fixes queued after gate. batch 0009 LANDED (gate 3628/0): AUTH+GROUP no longer queryable over WiFi, board login goes through /api/login only, /api/flash/bus gated, upload gated in body handler, board /ws push-only, /api/report size+path caps, stopall one thread per board with slow reported. All sabotage-proven. Next: ShowPlayer stop race, Flasher claim race, dev_download ERR-as-EOF. next batch verified REAL from sweep: MOVE plays wrong show from RAM without naming it (CommandRouter 440), tokenize drops past 16 tokens silently (Util 5), negative wait wraps to 49 days (SequencePlayer 133), unknown step key skips silently (144), RS485 RX line wiped past 250 chars truncates FILES through a bridge (RS485Bus 25), report endpoint reads uncapped body though ungated (main 3842), report RMW race translate-vs-status, two hubs can both bind PORT (allow_reuse_address), start_received leaks temp dir when claim refuses. 2026-08-25 22:08 gate on batch 2 failed RED 3656/2 - both real, both fixed: start_received imported shutil twice in one function (check_handler_scope), report rows were click-only with no keyboard path (check_keyboard_only - now each row carries an open button named by data-keyboard). Both checks had caught the regressions themselves; re-running the gate. 2026-08-25 23:20 batch 2 LANDED gate 3658/0, promoted 17 files. In it: tokenize>16, negative wait, unknown step keys, RS485 RX 2048, MOVE-wrong-show, plus tonight's two gate-caught regressions (start_received double shutil import; report rows click-only - each row now has an open button named by data-keyboard). check_translate+check_voice moved to SOLO (helper starved under 10 workers = WinError 10061 refused at 129s, passed alone 3.7s - same signature as check_hub_clock). Sweep re-check: ShowPlayer race, Flasher claim race, dev_download ERR, voice_proxy Request-in-try, report caps all already fixed - plan note was behind the code. Batch 3 LANDED 2026-08-26 00:20 - gate 3669/0, promoted 14 files (patch 0061). Studio: hand-typed keyframe times survive delete/dup/move (retimeAt retimes only the changed predecessor); Pause stops the hub's show even when Live is unticked; project keeps seqNext; export/load/play surface hub errors instead of silent failure. Design: .pin used undefined tokens --muted/--fg (now --mut/--txt); button.secondary existed nowhere (browser default leaked). NEW CHECKS both sabotage-proven: check_studio_edits (browser, 8 marks) + check_css_tokens (every var(--token) defined). A7-9 speaker PC-half + panel follow-ups LANDED 2026-08-26 (gate green, promoted). Remaining firmware-core: pendingReply_ slot, uploadDest_ sharing, checkLink tie-break = batch 4 next. batch 4 coded + proven: RS485 queue, PeerPick pick (owning std::string names), per-request upload paths; 3 new checks sabotage-caught, native tests 30/30, quick 2773/0. Panel running, then gate. batch 4 LANDED 2026-08-26 (gate green): RS485 PONG queue (evict-newest, signed delta), wifilink::PeerPick + native tests incl copy-semantics, per-request upload paths + safePath(dest), AudioPlayer cleanup SD lock, SequencePlayer null-module guard. Panel: 4 fixed, _tempObject-leak refuted vs lib source. Remaining sweep clusters: voice #49-53, nong-studio, help-accuracy - many already fixed in earlier batches, need re-verification. batch 5: sweep tail complete - notice()=fail-banner-only across Studio, first-move speed export, reports read under lock, voice port/timeout fixes, unique temp names, help 10 items; panel before gate batch 5 LANDED - sweep tail complete: notice=fail-banner-only across Studio (panel caught connModeChanged dangling-else), first-move speed export, reports list+status read under _reports_lock, voice port/timeout fixes, unique temp names, help 10 items\nA22-2: done   2026-08-24 21:54  \u2014 land.py reported REFUSED - nothing was copied while promote.py had actually promoted: gate green 3411, all files byte-identical in main afterwards, yet the word promoted never reached land captured output, so the task stayed doing. Found 2026-08-23 landing A20-1. Reproduce with a real diff end-to-end, find why out lacked the line (capture? buffering? early exit?), fix the detection - land.py gates every future landing FIXED 2026-08-24 21:53: land now prints LAND RESULT as its LAST line, so a truncated read of the output can never again turn a landed promote into REFUSED; a tree already byte-identical to main is reported LANDED (nothing to copy) and still marks --done; promote captures the suite output live so the QC verdict reaches the caller's pipe instead of relying on handle inheritance. FOUND ON THE WAY: plan.py set_status ate the NEWLINE after a bare task line (no em-dash note), gluing the next id onto it - later edits to that id silently did nothing. The new end-to-end sandbox check drove all three; three sabotages caught. The first gate also proved the guard: quick suite stopped a contention-red check_ota (two suites on one fake module), and check_hub_pair died with WinError 10053 under panel load - both green solo, matching the documented load-flake class\n\n# A23  A second design for every page\nA23-1: done   2026-08-26 16:56  \u2014 when every other task is done: ox-alpha redesigns ALL web pages as a SECOND version - same backend, same features, UI only - to compare against the Gemini-designed version; the user judges which one wins (asked 2026-08-23) scope is FULL UX + UI, not visual skinning: flows, interaction design, information architecture, wording, all five states (loading/empty/error/stale/ready) - only the backend and APIs stay the same fair-comparison rule (user, 2026-08-24): from NOW Gemini Pro designs FULL UX + UI too - flows, interaction, information architecture, wording, all five states - not only visual screens; both versions answer the same brief, only the backend stays fixed brief drafted (docs/a23_brief.md); panel round 1: 3 real fixes folded in - inventory-not-prescription, hosting /g/=web_gemini + /o/=web_ox (incumbent neutral), token ADDITIONS allowed; module site = screens+spec (no board to host it); judgement stays the user's alone both versions built+serving, quick suite 2833 green, panel ran (pro+flash), real findings fixed+2 sabotage-proven; pre-existing Studio innerHTML spots recorded as new task; gate deferred until bench work pauses\n\n# A24  The bench: real boards, real cables, real speakers\nA24-1: done   2026-08-25 01:11  \u2014 todo save EVERY change as a numbered patch snapshot, not only Nong Studio - asked 2026-08-24: *save other thing too not only nong studio*. The PATCHES.md system in nong/main_python_set_nong/ keeps one restorable row per web-app change; the same safety should cover hub Python, firmware, shared web and tools so any change can be rolled back by number without git knowledge LANDED 2026-08-25 01:10. tools/save_code_patch.py - whitelist globs over main_python/*.py shared/web qc tools promote.py MiceHub.spec; engine hardening in snapshot.py (number parse past 9999, --restore guards, description sanitised one line). land.py auto-snapshots every green gate - first auto patch 0002_landed-a21-5-a21-6-a24-1 fired on this landing. Secrets guarded by value in sandbox test.\nA7-8: blocked 2026-08-25 14:07  hw=lift board + wired WS2812B strip + I2S speaker  \u2014 Bench-proof the lift's RGB strip + speaker once they are wired: RGB 255 0 0, RGB EFFECT rainbow, PLAY <file>, VOL per COMMANDS.md. Code is landed and builds green via A7-4; only the physical strip + speaker are missing. CODE HALF RE-VERIFIED 2026-09-03 (no hardware needed): RGB + audio are complete and consistent in three places - firmware/COMMANDS.md:398-406 documents RGB/RGB BRIGHT/RGB EFFECT/PLAY/PLAY STOP/VOL/AMP/AMP VALID/AMP?, firmware/config/commands.json:94-97 carries them as registry entries with their steps, and LiftModule.cpp:243-260,343,375 really drives RgbStrip. RgbStrip did NOT disappear - it moved from firmware/src/core/ to firmware/src/modules/lift/, which is the per-module rule. Only the physical strip and speaker are missing.\nA7-9: done   2026-08-26 01:05  hw=nong board + I2S amp board + speaker to prove  \u2014 Speaker on a nong board - user asked 2026-08-25: if I add the speaker for nong are pins enough? PINS: yes - nong uses 8 servo GPIOs + 16/17/4 RS485; I2S amp needs 3 free outputs and 5/13/18/19/23 are all free (34-39 are input-only, 6-11 flash, 0/1/2/3/12/15 strap or USB). Suggest BCLK=18 LRC=19 DIN=23. FIRMWARE: speaker code (AudioPlayer, PLAY/VOL) is lift-only today (modules/lift/, excluded from mice_nong) so this needs it shared behind a cap/config entry plus servo-pin table checks - PC work first, bench proof when an amp is wired.\nA24-2: done   2026-08-25 18:38  \u2014 remove ise esc to stop pleaseeeee it accident press it all time - Esc must not close or stop anything on any page, the user presses it by accident; close buttons stay CORRECTION same day - user meant ESC IN CLAUDE CODE, not the web apps: *keep if it in the hub or program keep it i said in claude code like this when you run i still miss press esc but in main or program we make can use esc*. Hub.html edits were reverted byte-for-byte; no product change. Real ask: stop accidental Esc from interrupting Claude Code\nA24-3: done   2026-08-26 15:18  hw=the 2 nong boards on the bench  \u2014 test 2 nong first i need to see if it work so another thing you don't need me to approve user added mid-test: use demo login everywhere while testing - user manny pass 12345678 - do not keep changing passwords; user will add their own and delete the old account themselves BENCH RESULT 2026-08-26 15:2x - both nongs WORK: leader id85 chip 3076F593B454 COM26 pose 90->100->90 OK, partner id67 chip B4BFE91C5A74 COM29 pose OK, partner ALSO moves through the bus via usb:COM26:#67 (RS485 forward OK); both read back real joint positions; STOP answers; boards left at home all-90; board web login manny/12345678 accepted (mustChange:true) FOLLOW-UP 2026-08-26 15:3x - user heard NO servo: leader #85 had ALL arm-servo pins disabled (-1) in its stored config, only waist(13)/shrug(15) set; partner #67 correctly had 32 33 25 26 21 22 27 14. Wrote the standard map onto leader via PIN servo1..8 + REBOOT, verified status pins now [32,33,25,26,21,22,27,14,13,15]; big sweeps sent to both arms after. Same empty-pin risk as the known USB pin-config bug family - worth a QC check that a nong reports a FULL servo map USER CONFIRMED both arms physically move (2026-08-26 ~16:00). Root cause of silence: stored pin configs were incomplete - leader #85 had ALL 8 arm servos disabled (-1), partner #67 had 8 but no waist/shrug. Wrote the standard full map on each (servo1-8 = 32 33 25 26 21 22 27 14, servo9 WAIST=13, servo10 SHRUG=15) + REBOOT, user heard wrist then whole arm. Boards left at home all-90. SD-card board is partner #67; neither board's sd: flag reads true yet USER: now it all work - both arms dance together (opposite-phase loop ~45 s through usb:COM26 + usb:COM29). Bench test CLOSED. Both boards left at home.\nA24-4: done   2026-08-26 16:56  \u2014 it show the the same module in once it not show only once - or it need to be like this when it connect from different source it need to show? can you make it show once if it will have the problem if different esp use same id and name you can use mac of each esp to classify it user saw every board twice on the hub page - once direct once through another cable/bus; wants one row per physical board, MAC as the identity when id+name collide\nA24-5: done   2026-08-26 15:49  hw=a nong board on WiFi  \u2014 hub cannot drive a module over WiFi - /api/dev/cmd to wifi:<ip> gets 401 because the board gates /api/cmd and the hub never sends a board login; found on the bench 2026-08-26 when POSE failed over WiFi but worked over USB+RS485; board login itself is fine (manny/12345678 -> cookie -> POSE OK) LANDED in staging 2026-08-26 ~16:2x. main.py: board_login() extracted to module level (one body for OTA + dev_cmd), robot_get retries once through the board's own web login with the shipped/demo account and caches per-ip sessions. fake_wifi now models the gate (/api/cmd refuses changing commands without a session - bench-measured behaviour). check_transports proves it: stranger POSE -> 401, same POSE through the hub -> 200 + session exists + command reached the board; sabotage caught (login removed -> PIN VALID fails). BENCH: partner .70 POSE over WiFi through the hub now OK (was 401). Leader .54 still refuses manny - that board carries its own changed account; needs the user's password or a manual reset on its Setup page\nA24-6: done   2026-08-26 18:37  \u2014 hub printed TWO different generated passwords in one start (MOYQPI8hQHSV then EBEZlauHCP95) - two Auth objects were built, files hold the second, the running session only accepts the first; seen 2026-08-26 after deleting staging hub_auth.json; find the double init and make one Auth per process\nA24-7: done   2026-08-26 19:22  hw=a nong board  \u2014 QC: a real-flashing flow or bench check asserts every nong board answers PIN? with all 8 servo pins set - leader id85 shipped with all arm pins -1 and nobody could hear a servo until the config was rewritten by hand 2026-08-26\nA24-8: done   2026-08-26 18:53  \u2014 audit Nong Studio app.js innerHTML uses fed from hub/module data (hubs dropdown ~411, exportYaml status ~2328) - escape or rebuild with textContent so a hostile board name cannot inject markup\nA24-9: done   2026-08-26 17:07  \u2014 user: rename partner to nong (the real one) and make the leader the lift - reflashed mice_lift, type lift, name lift\nA24-10: blocked  2026-09-07 17:38  \u2014 when user says they have RGB strip + speaker hardware: add them to the lift (user will tell us; nothing to build yet) SPEAKER HALF IS DONE IN SOFTWARE 2026-08-27 (A24-16 + A24-18): the user named their amps - lift TPA3118 (analog, one wire from DOUT), nong MAX98357A - both are entries in config/amps.json, stored per board in NVS and picked by name on the Hardware-pins page, and STOP/MOVE STOP now silence the speaker. What is left here is PHYSICAL only: wire the strip and the speaker to a lift board and prove it on the bench (that is A7-8). Nothing further to build for the speaker until a board is on the desk back to blocked 2026-09-07 17:36 - the user's message was about the NONG (MAX98357A + speaker), not the lift; this task is the lift's strip and speaker\nA24-11: done   2026-08-26 17:18  \u2014 measured 2026-08-26 ~17:20, live boards: hub->lift(#85)->RS485->nong(#67) matrix - USB raw bus POSE ok (servos moved), INFO ok, SD file list ok through lift; REACH nong PING ok from both USB-cable and WiFi .54 entries; REACH with a CHANGING cmd refused 'ERR log in first' from BOTH entries (far board's own HTTP gate on the peer hop - raw bus frames stay open). Mirror matrix hub->WiFi->nong->RS485->lift proven earlier same hour (PONG relays back). Nong ready to unplug into real hardware.\nA24-12: done   2026-08-26 17:27  \u2014 measured 2026-08-26 ~17:40 full connectivity inventory, all 6 live boards (nong67 COM29/.70, lift85 COM26/.54, labpair42 COM28/.62, lift1 COM9/.159, camA77 COM30/.57, camTwo89 COM18/.218): own USB 6/6 ok; own WiFi by ip 6/6 ok; wifi:<name>.local ok BUT both lifts named 'lift' collide - lift.local answers lift#1; WiFi peer REACH mesh 30/30 ok (every board reaches every other by id); physical RS485 wire exists ONLY between lift85 and nong67 (all other pairs 502 no reply); raw-bus changing cmds open on that wire; usb:<port>@<name> hotspot form works through a cable board to its peers; REACH changing cmd over peer hop still gated by far board login (by design); hub:<pc>/ relay needs a second PC, untested solo.\nA24-13: done   2026-08-26 17:37  \u2014 measured 2026-08-26 ~17:55 SET NAME drives <name>.local live: renamed lift85 to 'lift-two', wifi:lift-two.local answered PONG 85 within 5s (WebPortal applyHostname restarts STA and mDNS re-announces), old wifi:lift.local immediately resolved only to lift1 - the duplicate-name collision clears on rename with no reboot. Code also says the AP hotspot SSID follows the module name (Identity.h hostname() feeds both mDNS and raiseAp). Left named 'lift-two' so the two lifts stop colliding; user can pick any name.\nA24-14: done   2026-08-26 17:41  \u2014 2026-08-26 ~18:05 name swap per user: lift85 is now 'lift' (their chosen name), old lift1 renamed 'lift-one'. Verified live - wifi:lift.local -> PONG 85, wifi:lift-one.local -> PONG 1, both cables agree. Renamed 1 first so the name was free; no reboot needed on either.\nA24-15: done   2026-08-26 17:51  \u2014 2026-08-26 ~18:15 user unplugged EVERYTHING - nong67 goes into the real robot hardware, other five boards powered off too. Verified: zero Mice boards on USB (only Bluetooth COM ports remain) and all six WiFi addresses silent. Bench is empty; next session must not chase ghost routes. All identities/names/types/auth were verified good before power-off, so they persist in NVS for next time.\nA24-16: done   2026-08-27 22:40  \u2014 user: we need to add the speaker are you finish - and the amp is a TPA3110. THE FIRMWARE ASSUMED THE WRONG AMP: AudioPlayer::begin builds AudioOutputI2S and writes DIGITAL I2S (MAX98357A/PCM5102), but a TPA3110 is an ANALOG class-D amp with a line input - fed raw I2S bits it plays loud noise, not sound. Also answers the earlier question (2026-08-26: nong with i2c speaker need to change the port of servo or not, now we use 21 and 22 it i2c port): NO servo pin changes - 21/22 are the I2C pins and nothing here uses I2C. So: make the AMP KIND data (config/amps.json), stored per board in NVS, picked by NAME from the Hardware-pins page - i2s amp, analog amp like TPA3110/PAM8403 (one wire, delta-sigma, AudioOutputI2SNoDAC), or the built-in DAC on 25/26. Nong has only GPIO 0/2/12 free after 10 servos + SD + RS485, and the analog path needs only ONE of them USER 2026-08-27 19:4x: fix the nong full setup add speaker / use tpa3318 to be speaker of nong - read as TPA3118 (TI has 3110/3116/3118/3128, no 3318); same ANALOG class-D line-in family as the TPA3110 already in amps.json, so it is one more entry there and becomes the nong default STOPPED at the user's call 2026-08-27 19:58, work is IN .staging and NOT promoted - no QC gate has run on it. DONE so far: amps.json gained a tpa3118 entry (analog, DOUT only, its own 12-24V supply) and NONG_AUDIO_AMP_DEFAULT is now tpa3118; AudioPlayer::begin held out_ as AudioOutput* and called SetPinout/SetOutputModeMono on it, which are declared on AudioOutputI2S only - it could not have compiled, now a typed AudioOutputI2S* local; the analog path never named its pins, so AudioOutputI2SNoDAC would have run on the library defaults bclk 26 / lrc 25 / dout 22 - on a full nong those are servos 4, 3 and 6, i.e. 44 kHz audio on three servo signal lines - now SetPinout(-1,-1,i2sDout,-1), and the i2s path passes mclk -1 too so no master clock appears on GPIO0; AMP? and a bare AMP now answer the current amp as JSON instead of ERR usage (ampJson added to the generated AmpTable.h, gen_tables.py rerun); NongModule routes AMP/AMP? (commands.json had declared the scope with nothing behind it); the Hardware-pins page has an Amplifier picker (Gemini Pro design: picker under the group title, wiring sentence beneath, only the GPIO boxes that amp uses, an unsaved marker, saved by the same Save and reboot button), pin group renamed I2S speaker -> Speaker; COMMANDS.md documents AMP/AMP VALID/AMP? and the mode table. STILL TO DO: no QC check for any of it yet (check_amp_kind: amps.json <-> AmpTable.h <-> picker <-> NVS <-> scopes) and no sabotage run; pio build of every env NOT run since these edits; check_designer_first may object to the picker wording; then land.py USER 2026-08-27 22:1x: change nong use max98357a - so the nong's default amp becomes the MAX98357A (digital I2S, 3 wires) instead of the TPA3118. It fits: a full nong has exactly GPIO 0, 2 and 12 left and I2S needs exactly three, already mapped BCLK 12 / LRC 0 / DOUT 2. Both amps stay in amps.json, only the default moves. Waiting for the running QC suite to finish before editing staging USER 2026-08-27 22:2x: lift use TPA3318 - read as TPA3118 again, the analog amp already in amps.json. So the two defaults swap ends: nong = max98357a (I2S, 3 wires on its last 3 free pins), lift = tpa3118 (analog, DOUT only through an RC filter). Both are only DEFAULTS - either board can be changed from its Hardware-pins page without reflashing PANEL FOUND A REAL ONE 2026-08-27 22:09 (gemini-3.1-pro-high, verified against the library before acting): SetPinout(-1,...) does NOT mean I2S_PIN_NO_CHANGE. AudioOutputI2S keeps bclkPin/wclkPin/doutPin/mclkPin as uint8_t (AudioOutputI2S.h:73-76), so -1 arrives as 255 and i2s_set_pin gets an invalid GPIO instead of no-change - the analog amp would have been silent. Its mclkPin also stays 0 by default, and on IDF 4.4.7 (this framework) mck_io_num 0 routes CLK_OUT1 onto GPIO0, which is the nong's LRC line. FIX: both paths now call the 3-arg SetPinout with the board's real audio pins, and a new AudioPlayer::applyPins() re-runs i2s_set_pin through the driver right after the decoder installs it, with a real I2S_PIN_NO_CHANGE for MCLK. check_amp_kind now fails on any SetPinout(-1 in that file GATE RED 2026-08-27 22:27, one check: check_docs - every command in COMMANDS.md must also appear on the hub help page, and AMP did not. Fixed in help.html: the lift Audio card became Speaker and names the amplifier picker, the nong Speaker card says MAX98357A on 12/0/2 plus the GPIO0 pull-up if the board will not start, and AMP rows were added to both command tables. Re-gating\nA24-17: done   2026-08-27 22:51  \u2014 USER 2026-08-27 22:3x: why not update the name too plan.html too - the Progress card showed A24-16 under 'Getting it onto another PC'. CAUSE: the STATE block's area headings stopped at '# A15' while ids ran to A24, and the renderer gave each task the LAST heading above it (PLAN.html:1375), so ~60 tasks from A16 up wore the A15 label and the out-of-order strays wore whichever heading they landed beside. FIXED: nine headings added (A16-A24) and the renderer now labels a task by its OWN id prefix, falling back to the nearest heading only for an id with no heading. Counts unchanged after the edit (151 done / 1 doing / 10 blocked), plan_state.js republished. STILL TO DO: a QC check that every id prefix in the STATE block has a heading and that the page labels by id - cannot be written yet, a gate is running and .staging must not be edited mid-gate\nA24-18: done   2026-08-27 23:05  \u2014 doing the STOP that reaches every board must SILENCE it too - the last open half of A20-4, and the only part of it a PC can do. Measured 2026-08-27 in the promoted tree: NongModule.cpp:542 STOP freezes the pose, LiftModule.cpp:140 STOP cuts the motor, and CommandRouter.cpp:432 MOVE STOP ends the sequence - none of the three touches the speaker, so a show stopped in a hurry leaves the music playing over a robot that has frozen. STOP is the panic button; it has to mean quiet as well as still\nA24-19: done   2026-08-28 18:22  \u2014 USER 2026-08-27, own words: nong studio make when i click pose 2 or another pose in sequence warp the play time warp to time of that pose when i run it will follow sequence not warp to pose 1 and run in sequence make it dynamic my body of robot will broke it hit other thing that i don't want if it warp to pose 1 and then follow sequence. SAFETY, not convenience: pressing play after clicking a pose must CONTINUE the sequence from that pose's time, never jump back to pose 1 first - the arm sweeping back to the start can hit the rig and break the robot. A20-1 already made a CLICK park the play clock at that move; what is reported broken is the RUN after it PLAN TOOL BUG, found by the user 2026-08-28 17:4x asking why is it in todo why not do it right now: the task WAS being worked and the page said todo with nothing in flight. Cause: plan.py add takes <id> <note>, so the command written as 'add A24-19 doing <note>' filed the word doing as the first word of the NOTE and let the status default to todo. It had already happened silently to A24-17. Status corrected and both notes cleaned. NEXT: make add refuse a status word as the first word of a note - a task recorded as todo while it is being done is the exact failure the update-the-plan-at-every-step rule exists to prevent GATE RED TWICE, both from machine load not from the fix. Run 1: check_docs (AMP missing from help.html) - real, fixed. Run 2 (605s): check_network_tab timed out; passes alone in 57.2s. Run 3 (670s): THREE failed - network_tab again, check_responsive could not measure /mod at 2560px, and check_connection_states CRASHED with WinError 10061 connection refused, i.e. a hub that was not listening yet. One check took 176s. CAUSE FOUND: not stray processes - the 15 msedge instances are the user's own visible browser and were left alone - but my own two new SLOW browser checks, each starting its OWN hub and browser, on a suite that already runs 45 browser checks on 3 workers. FIX: merged them into ONE check (check_studio_resume) that drives all three phases in a single page, and asserts on the uploaded files decoded from the FBEGIN/FDATA wire traffic rather than NONG.held, which only keeps the last one. Suite back to 158 checks and one hub start instead of two. check_network_tab keeps its raised patience (40s->55s inner, 62s->80s page) with the measurement written beside it\nA24-20: done   2026-09-03 00:39  \u2014 USER 2026-08-28, own words: find the ref to ref.html for me please which thing have use which equation or other ref for everything in my project alway update this ref too if we add something new or edit it. So: ONE page, ref.html, that says for every part of the project which equation or outside reference it uses - the timing floor, the speed mapping, the gear ratios, the 4-bar, the servo pulse maths, the delta-sigma audio path, the WiFi roam thresholds, the PID, and the datasheets and libraries behind them - each with the real file and line it lives at. And it is a STANDING rule, not a one-off page: every new thing or edit updates it, held by a QC check the way COMMANDS.md and help.html already are DECIDED with the user 2026-08-28: ref.html is a DOCUMENT in docs/, beside PLAN.html - opened from the file system, travels with the repo, needs no hub running. Not a served hub page. It is written in .staging/docs/ref.html so promote copies it out like any other file (PLAN.html is in SKIP_FILES because the running system writes it; ref.html is not, so it promotes normally) DESIGN from Gemini Pro 2026-08-28: docs/ref.html renders docs/ref_data.js, loaded with a script tag rather than fetch - a file:// page is given a null origin and refused its own neighbours, the same reason plan_state.js exists. Sticky search box filters the entries, one article each: plain name, the equation in pre/code with var and sup, why it is that way, and a link to the real file and line. So an entry is DATA, and adding the next one costs no page code - which is also what lets a QC check demand one for every equation in the code USER 2026-09-02: opened file:///E:/final_proj/mice/code/docs/ref.html and it showed 15 of 15 with no outside references. Cause: the 21-entry version WITH the from: citations was written into .staging/docs/ref_data.js and never promoted, so the real tree still carries the 15-entry version with 6 from: fields. A24-20 was marked done off the staging copy. Promoting now. Also synced .staging/.claude/settings.local.json from the real tree first, because the staging copy was from 2026-07-12 and promoting it would have deleted the user's Bash(nvidia-smi*) permission. STOPPED 2026-09-02 20:5x at the user's call - they are moving the PC. State to resume from: .staging/docs/ref_data.js holds 21 entries all carrying from: citations; the real tree still has 15 with 6. Nothing was promoted - promote refused twice, correctly. Two full gates ran and BOTH were red in DIFFERENT browser checks (7 fails, then 14, only network_tab repeated), and all three of run 1's failures passed alone straight afterwards (8/0, 5/0, 10/0), so this is machine load, not a regression. Third attempt was the suite at --jobs 2 to halve browser contention; killed part-way. RESUME: run .staging/qc/run_qc.py --jobs 2 on a quiet machine, then promote.py, which reuses the green receipt instead of gating again. LANDED. QC 4327 passed, 0 failed in 780s at --jobs 2, then promote copied on that receipt. docs/ref_data.js in the real tree is now 21 entries, every one carrying a from: citation (was 15 entries, 6 citations). Verified by rendering file:///E:/final_proj/mice/code/docs/ref.html in headless Edge: it reads 21 of 21 and every card shows its Comes from line. Why it took three gates: two full runs went red in DIFFERENT browser checks (7 fails, then 14, only network_tab in both) and all three of run 1's failures passed alone straight after (8/0, 5/0, 10/0) - machine load, not a regression. --jobs 2 halves the browsers running at once and it went green first time. Use --jobs 2 when the user's own Edge/VSCode is open; 56 webview processes were up and free RAM was 7.4 of 31.4 GB.\n                2026-08-19: mice.local:8642 does not fix the ambiguity, because\n                the PORT was never the ambiguous part - the NAME is. Two hubs on\n                one network both answer mice.local and whichever replies first\n                wins, differently each lookup.\n                Do what Bonjour does: PROBE before claiming. A hub asks whether\n                mice.local is already answered by someone else; if it is, it\n                does not answer for it and claims mice-<hostname>.local instead.\n                The first hub on a network then owns the short name outright,\n                every other hub has a unique one, and nobody has to remember a\n                unique one because other PCs are links in the Other PCs list.\n                Testable here, unlike on the bench: two hubs on THIS machine\n                share a link, which is exactly the case two PCs on different\n                subnets cannot reproduce\n\nA24-21: done   2026-09-03 02:15  \u2014 USER 2026-09-03: \"do the ref link to it own web too\" - ref.html is now accessible via the hub's web server at /docs/ref.html and /docs/ref_data.js. The /docs/ route was added to static routing in main.py, serving the docs folder from DATA/docs. PLAN.html is also accessible at /docs/PLAN.html, and plan_state.js (generated at runtime by plan.py) is available at /docs/plan_state.js when the hub writes it. Verified: check_ref passes, quick QC passes.\nA24-22: done   2026-09-07 12:29  \u2014 user 2026-09-07: is nong now can connect to speaker and run the speaker via wifi?? and push the music to sdcard and run follow the pc - if not make the app that can run, in to the plan and make follow the plan. ANSWER FROM THE CODE: speaker over WiFi = YES (AudioPlayer is core, nong handles PLAY/VOL/AMP, caps carry audio, mice_nong has the decoder lib, pins 12/0/2) but never proven on a real board (A20-4 still blocked); pushing music to the SD card = YES (module /api/upload?dir=/music, hub proxy /api/dev?what=upload over WiFi or USB); FOLLOW THE PC = NO - main.py seq_steps drops every non-pose step (main.py:1296) so a show played from the hub never starts the music, and ShowPlayer.stop leaves it playing. THIS TASK: make the hub-run show carry music - keep play/vol steps on the timeline, fire PLAY/VOL at their moment, silence on stop, and a QC check that fails when a music step is dropped promoted 2026-09-07 12:28, full gate 4350/0. Hub-run shows now carry the file's non-pose steps as CUES: seq_steps keeps them in the file's own words, cue_lines turns them into commands from commands.json (motion left out - the hub is the clock), ShowPlayer fires them with their keyframe, and stopping a show that started audio sends PLAY STOP. Studio keeps cues through load, hubPlay and export, so editing a show no longer deletes its music. check_show_music + check_studio_music (real browser, asserts PLAY on the wire); 8 of 8 sabotages caught. STILL UNPROVEN ON HARDWARE: no board has ever played a note - A20-4 stays blocked\nA24-23: done   2026-09-07 12:43  \u2014 found while landing A24-22: promote.py and tools/make_app_branch.py each printed their OWN rebuild line, both the bare PyInstaller --onefile form that CLAUDE.md warns about - it builds a hub with no web pages and no registries AND overwrites the curated MiceHub.spec (that happened 2026-08-21, the spec was restored from git). Both now print build_stamp.REBUILD, the one there is, and check_stale_build fails on any second rebuild command\nA24-24: done   2026-09-07 13:11  \u2014 user 2026-09-07: where to upload the music and where to use, how to use, do we have it own app or not need to use? ANSWER: no new app - upload is hub > Modules > the board > Open module > Files > /music, playing one track is the Speaker card on that page, and a show carries music as a play: step. THE GAP: nothing in Nong Studio can ADD that step - today somebody has to type a line into the YAML, which the designer-first rule calls a bug. So: a click path in Studio to give a keyframe a track from the robot's own /music list, and a volume, with the file uploaded from the same place. Design goes to Gemini Pro promoted 2026-09-07 13:10, full gate 4366/0. Every keyframe carries a music button beside its speed: it opens a picker whose list is read from the robot's own /music folder (GET /api/dev/files?dir=/music), with a volume beside it and 'no music' to take both off. Design from Gemini Pro, with one change - the control sits on the existing speed line instead of in a popover, because Studio has no popover pattern and a row per keyframe costs timeline height for something most moves never use. check_studio_music_ui drives a real browser and asserts the offered list, the exported yaml and PLAY/VOL on the wire; 3 of 3 sabotages caught. fake_serial now answers FILES per folder\nA24-25: done   2026-09-07 18:19  hw=the nong on WiFi, or a USB cable to it  \u2014 the bench nong (id 67) runs firmware older than the amp table: AMP? answers unknown cmd, so config/amps.json, the amp picker on the Hardware-pins page and the A24-18 silence-on-STOP are not on that board. Today's mice_nong build also DEFAULTS to the pins the user actually wired (bclk 12 / lrc 0 / dout 2). Reflash it - over the air from the hub, or by cable - then re-run the tone and the show test. Needs the user to agree: a failed OTA means fetching a USB cable DONE 2026-09-07 18:2x, over the air, user agreed (no problem when fail i can use usb to flash). The board took the 1.44 MB image and came back in about 2 seconds; nothing needed a cable. What the reflash bought, all proved against the real board and not a fake: AMP? now answers the max98357a entry from config/amps.json where it used to answer unknown cmd, so the amp table, the Hardware-pins amplifier picker and A24-18 are on that board; the stored pins survived the flash (i2s_bclk 12, i2s_lrc 0, i2s_dout 2) and so did /music; the board's status now carries module.audio, which is what made the rest checkable without ears - PLAY set playing true with the file name, MOVE STOP set it false (A24-18 proved on hardware for the first time), and a show run FROM THE PC with a play cue showed playing true mid-show. CORRECTION to A20-4's note: VER is not a command at all, so its unknown cmd answer said nothing about the firmware age - AMP? was the real evidence OTA re-run 2026-09-07 19:2x could NOT run: the board had left the network (no HTTP on 10.126.95.70, no nong.local). The first reflash earlier today did land; this second one carries the module-page fixes (A24-27/29/31) and is still owed. Build is ready\nA24-26: done   2026-09-07 18:14  \u2014 user 2026-09-07 after hearing the tone: have the button to upload the music and have way to make it play the sound from pc. So the music picker in Studio gets both - an upload button that sends a track from this PC to the robot's /music, and a play button that plays the selected track on the robot right now, from the PC, without running the whole show promoted 2026-09-07 18:12, full gate 4371/0 (the session died just after; the tree has it). The music picker now carries play, stop and add: play sends PLAY to the robot from the PC without running the show, add sends a track from this PC straight onto /music through the hub and selects it. check_studio_music_ui proves each on the wire or in the file; 3 of 3 sabotages caught after a first attempt caught none - the first assertions could not tell the button's PLAY from the show's own cue\nA24-27: done   2026-09-07 19:23  \u2014 user 2026-09-07 18:3x: why when i press triangle to play the sound in module it not play any sound. FOUND IT, on the real board: /api/cmd answers 401 ERR log in first for PLAY and VOL when nobody has logged in on that page, and the Speaker card throws the answer away - WebUI.h cmd() only writes it into the technical log, and playSel() ignores the return, so the triangle looks broken. Every other button on that page has the same hole. FIX: a visible line in plain words when a command is refused, pointing at Setup > Setup login, and the Speaker card says it on its own status line promoted 2026-09-07 19:22, gate 4387/0 - the page now names the gate that refused a command, the Speaker triangle says why nothing played, and check_modsite_refused holds it (3 of 3 sabotages caught)\nA24-28: done   2026-09-07 19:23  \u2014 user 2026-09-07: cannot login in module web. TWO REAL CAUSES FOUND, both ours. (1) main_python/hub_password.txt is the file that tells the operator this PC's hub password, and EVERY QC RUN OVERWRITES IT: hub_auth.plain_file() is store.with_name(hub_password.txt), so a throwaway store (mice_qc_auth_29720.json) writes its random password into the real file. The copy in the tree says user mice / qc-test-password set 2026-08-20 22:11 while hub_auth.json was really set 2026-08-21 22:54 - so anyone reading that file to get into the hub is given a password that was never theirs. FIX: the plain file follows its own store. (2) the module page's login says wrong user or password for a refusal that is really the HUB not letting the page through, and after a correct direct login it shows a RED banner demanding a new password with no line saying the login worked - which reads as a failed login\nA24-29: done   2026-09-07 19:23  \u2014 user 2026-09-07: make can use by login only in module too because some time run only module no hub running. So the board's OWN login must be a complete way in, with no hub anywhere: reached straight at its address, log in on the board, and every control on that page works. Today that path exists but reads as broken - through the hub a refusal from the HUB is printed as wrong user or password, and after a correct direct login the page answers with a RED banner demanding a new password and never says the login worked\nA24-30: done   2026-09-08 13:00  \u2014 user 2026-09-07: setup and wring it why show the same as move it / show and file too it have the same as moveit make it show only it own tab. On the module page every tab shows the SAME cards - Move it, Shows and files, Setup and wiring all render one long page - so the tabs are decoration. Each must show only its own cards. User said: add it to the plan, make it later, do speaker first promoted 2026-09-08 13:0x, gate 4432/0. showCard() now only records what the board can do (data-cap) and applyTabs() is the one place that decides visibility - tab AND capability AND login must agree. The old code wrote display directly and render() runs on every status push, so every card the board supports came back on whichever tab was open half a second after switching. check_modsite_tabs now reads the cards BEFORE any tab is clicked (switching re-applies the rule, so every assertion made after a click passed while the page a person opens was wrong) and 2 of 2 sabotages are caught. The panel found the same bug independently from the promoted tree\nA24-31: done   2026-09-07 19:23  \u2014 user 2026-09-07: move the speaker lower then nong arm edit, now it first then move in module. On the module page the Speaker card sits above the arm controls, so the first thing on Move it is the music instead of the robot. Put the arm editing first and the Speaker card under it\nA24-32: done   2026-09-07 20:48  \u2014 user 2026-09-07: how to make it run voice follow the pc like the bluetooth speaker - i.e. the robot's speaker plays what the PC is playing/saying, live, instead of only files from its own SD card. THREE WAYS, to be decided: (a) SPEAK-AND-SEND, buildable today with no new firmware: the PC renders the phrase to a wav, uploads it to /music through the hub and sends PLAY - about a second of delay, perfect sync with a show, and it reuses A24-22 and A24-26 whole; (b) STREAM over WiFi: the board takes a chunked POST of PCM into a ring buffer and feeds I2S - real live audio, but 705 kbps for 44k mono and the board already stops answering HTTP while it decodes from the card, so buffering and dropouts are the whole problem; (c) A2DP Bluetooth sink so the PC pairs with it as a real bluetooth speaker - the ESP32 can do it, but BT classic beside WiFi on a board at 76 percent flash with ten servos is the risky one. Recommend (a) first, then (b) if live is really needed. Panel first, then build DECIDED by the user 2026-09-07: stream over wifi - option (b). So the board takes live audio from the PC and plays it as it arrives, like a bluetooth speaker. Constraints already measured on the real board: it stops answering HTTP for the whole length of a track while AudioPlayer holds the SD card under the SPI mutex, flash is at 76.3 percent, and ten servo signals plus WiFi are already running on it. Design going to the panel first DESIGN from Gemini Pro 2026-09-07, four answers: (1) UDP, not TCP and not a websocket on the async server - a TCP retransmission stalls playback and the async server already starves the CPU; UDP drops a late packet instead of holding the whole stream up. (2) 22050 Hz 16-bit mono PCM, uncompressed so nothing has to decode: 352 kbps. (3) 200 ms of buffer = 8820 bytes of RAM; on underrun feed silence to I2S, stop reading, and wait until the buffer is half full again before resuming. (4) The thing most likely to kill it: WiFi latency spikes plus interrupt contention - the servo timers, RS485 and the web server starving the audio task - and the way to know early is to MEASURE the longest gap between I2S writes. So the board will report that gap and its underrun count in status, and that number is the acceptance test. BUILD ORDER: firmware core/AudioStream (UDP listener, ring buffer, stats) driving AudioPlayer's existing I2S output so pins, mono and volume are not duplicated; STREAM ON/OFF/? in commands.json; then a hub sender - a wav file first to prove the path, then the browser capturing the PC's own audio the way rgb.html already does with getDisplayMedia LANDED 2026-09-07, gate 4424/0, promoted 19 files, board flashed and hub restarted. Live audio works PC to robot: core/AudioStream (UDP 4210, 22050 Hz 16-bit mono, 200 ms ring, silence-and-refill on underrun) fed by main_python/stream_audio.py, paced one 20 ms datagram at a time. MEASURED ON THE BOARD: 250 datagrams, 0 dropped, 0 lost, longest feed gap 9 ms against a 200 ms buffer, with ten servos, RS485 and the web server all running. Two gates were red first and only the first was real - three empty catch blocks in the cast script (caught by check_modsite_errors, the check written for exactly that) and a temp folder created inside main_python that crashed the build-stamp checks in a full run while passing alone. The second red was a flake: same code passed on the next run\nA24-33: done   2026-09-07 20:48  \u2014 user 2026-09-07: cannot upload from the module page - Upload answered failed ({ok: false, error: HTTP Error 401: Unauthorized}). Two faults in one line: the hub gate refused because that browser has no hub session (same cause as A24-27, but the UPLOAD path was not covered - only cmd() was), and the message is raw programmer text on a screen for designers. Fix: the upload status says who refused it and what to do, in plain words, like every other refusal\nA24-34: done   2026-09-07 20:48  \u2014 user 2026-09-07: how to use feed to play sound over wifi?? - the streaming works from a script but there is NO way to use it. So: the screen for it. Pick a module, press one button, and whatever this PC is playing comes out of the robot (getDisplayMedia capture, downsample to 22050 mono, POST to /api/stream/feed), with the board's own health beside it - fill, underruns and the 9 ms feed gap we measured. Design to Gemini Pro\nA24-35: done   2026-09-08 12:10  \u2014 user 2026-09-07: why it not share only the voice sound it share entry stream - the picker asks for a whole screen or tab when all they want is the sound. WHY: Chrome and Edge on Windows only give system audio together with a screen or tab share; getDisplayMedia with audio alone is refused, so the picker cannot be avoided for PC SOUND. TWO FIXES: (1) stop the video track the moment the stream arrives - only the sound was ever sent, but the open video track is what keeps the sharing bar up and costs CPU; (2) a second button for the MICROPHONE - getUserMedia audio only, no picker, no screen, which is the right control for talking through the robot STOPPED at the user's word 2026-09-07 21:0x, work is IN .staging and NOT promoted - no gate has run on it. DONE so far: castStart(how) takes two sources, the microphone one uses getUserMedia audio-only (no picker, no screen), the shared video track is stopped the moment the stream arrives, a second button Talk through the robot sits beside Play this PC sound, the status line names which source is playing, help.html explains why Windows makes the picker ask for a screen at all, check_stream_audio holds both (2 of 2 sabotages caught), quick suite 3316/0, firmware builds at 77.2 percent flash. TO DO TOMORROW: full gate, promote, reflash the board (it currently runs the build WITHOUT the microphone button), then hear it. Everything from A24-32/33/34 is already landed and on the board promoted 2026-09-08 11:2x, gate 4427/0 (the run before crashed in check_responsive with connection refused and passed alone at 251/0 - the fourth load flake of that kind, not a defect). The board still needs the flash: it is off the network this morning, and this PC's address moved from 10.126.x to 10.67.115.198 overnight, so the old board address is stale too\nA24-36: done   2026-09-08 13:46  \u2014 panel + verified in code 2026-09-08: changeMyPass (WebUI.h) changes the board password with USER PASS but never logs in again - doFirstChange does exactly that, and its own comment says why: the old session dies with the password, so Setup stays open on a DEAD session and every later action is refused with nothing saying so. It also prints the board's raw answer into userStat. Fix both: re-login like doFirstChange, and say it in words promoted 2026-09-08 14:0x, gate 4449/0 in 545s - and the run was 143 seconds faster than the ones before it, because the port-bind retry (A24-47) stopped a check waiting six seconds on a dead hub thread\nA24-37: done   2026-09-08 13:46  \u2014 panel + verified in code 2026-09-08: saveSettings() sends SET WIFI only when the network NAME box is filled (if(ssid)), so changing ONLY the password does nothing at all and the page says everything saved. Fix: send the change when either field is set, and refuse-with-words when only a password is given for a board that has no network yet\nA24-38: done   2026-09-08 13:46  \u2014 panel + verified in code 2026-09-08: the camera saves pictures to /photos (CamModule.cpp:417,420) and the page's own help text says so, but the Files folder list offers only /music, /moves and /data - so no picture the robot took can be reached from the page. The list is hardcoded HTML, which the standing rule says belongs in data. Fix: add /photos and make the list come from one place, guarded by type so a nong does not offer a camera folder\nA24-39: done   2026-09-08 13:46  \u2014 panel + verified in code 2026-09-08: the Lift card buttons (Up, Down, Stop, and the generated stage buttons in gotoBtns) throw away what the board answered, exactly like the Speaker triangle did before A24-27 - so on a board that wants a login they do nothing and say nothing. Fix: the same sayRefusal path, into the lift status line\nA24-40: done   2026-09-08 13:46  \u2014 panel + verified in code 2026-09-08: savePair() tells the person to check the console BELOW, but the console lives on the Shows-and-files tab while the pairing card is on Setup - a message that names a fix you cannot reach from where you are standing. Fix: say what the board said, where they are\nA24-41: todo  2026-09-08 13:01  hw=lift board + PCM5102A + TPA3118 + the 12V speaker  \u2014 user 2026-09-08: add to the plan to use TPA3118-30W with lift module, i use PCM5102 in main and use TPA to use my 12V Speaker. So the real wiring is a CHAIN: ESP32 I2S -> PCM5102A (a DAC, line level out) -> TPA3118 30W -> the 12V speaker, and the lift is the board that carries it. Both parts are already entries in config/amps.json (pcm5102a mode i2s 3 pins stereo, tpa3118 mode analog one pin) - but neither describes the CHAIN, and picking one hides the other: choosing pcm5102a is right for how the chip must drive it, while the wiring sentence a person needs to read is about the amp behind it. TO DECIDE AND BUILD: one entry that says DAC into an amp (mode i2s, its own wiring line naming both boards and the 12V supply), the lift default set to it, and the Hardware-pins page saying which board each of the three wires goes to. Nothing to guess at: the ESP32 only ever sees the DAC\nA24-42: done   2026-09-08 14:00  \u2014 panel + verified 2026-09-08: fwWatch (hub.html:3124) polls /api/flash while a board is being reflashed and its catch is a bare return - so the moment the hub becomes unreachable, which is exactly when it matters, the tick gives up quietly, the quiet counter never grows and the stall message never appears. The bar sits frozen for ever with no word, and the comment three lines above it promises the opposite: a progress bar that stops moving with no word is the worst of the five states. Fix: count a failed poll as quiet, and say the hub stopped answering promoted 2026-09-08 14:2x, gate 4462/0. Four hub faults, all found by the panel and each checked against the code first - and one panel claim (the theme picker forces a file edit) was rejected because the picker is built from themes.css and is clickable. check_hub_answers holds all four; 3 of 3 sabotages caught\nA24-43: done   2026-09-08 14:00  \u2014 panel + verified 2026-09-08: setComplaintStatus (hub.html:1044) posts to /api/reports and never looks at the answer - it closes the box and reloads the list, so a refused change (not logged in, hub busy) reads as saved. Its catch then shows alert(Failed: e.message) - a blocking browser box with programmer text. Fix: check ok, say what happened in the page, no raw exception\nA24-44: done   2026-09-08 14:00  \u2014 panel + verified 2026-09-08: rgb.html cmd(c,throttled) drops any call inside 90 ms and returns empty, with no trailing send - so the LAST value of a slider drag is thrown away and the strip keeps the colour from 90 ms earlier, while the slider shows the new one. Fix: remember the dropped call and send it when the window closes\nA24-45: done   2026-09-08 14:00  \u2014 panel + verified 2026-09-08: the light tool says the right sentence and then appends the raw detail to it - reply 500, the firmware ERR string, e.message. Plain words belong on the surface and the technical half belongs behind the technical switch, which this project already has. Fix: sentence on the surface, detail one click deeper\nA24-46: done   2026-09-08 15:24  \u2014 panel + verified 2026-09-08: the WS2812 data pin is RGB_LED_PIN 13, fixed at COMPILE time, and FastLED takes it as a template parameter (RgbStrip.cpp:5) - so unlike every other pin on the board it cannot be changed from the Hardware-pins page, and nothing on that page says why. Two honest answers: generate a small switch over the GPIOs a strip may use, from data, so the picker works; or say plainly on the page that this one is set when the firmware is built. Decide before building promoted 2026-09-08 15:0x, gate 4483/0, 11 files. The user's answer to the choice was do everything that can run in pc, so the PICKER was built rather than the note: config/rgb_pins.json lists the GPIOs a strip may use (13 default, plus 14/15/25/26/27), gen_tables writes one FastLED instantiation per pin into modules/lift/RgbPins.h, HwConfig carries rgb_data like any other pin, and the page shows it under Light strip. The generator refuses an input-only pin, a flash pin, a duplicate and a default outside the list. Lift build 77.8 percent flash with all six. check_rgb_pin holds it, 3 of 3 sabotages caught - including the board quietly using the built-in pin instead of the chosen one\nA24-47: done   2026-09-08 13:46  \u2014 found while landing the panel fixes 2026-09-08: THREE gates in two days died on the QC hub failing to bind its port - check_flash_remote, check_responsive (WinError 10061 connection refused) and check_themes (WinError 10013 forbidden). One cause: _free_port() asks the OS for a port and CLOSES it before main.py binds it again, and in that gap Windows can hand the same number to another worker or fold it into a reserved range (WinNAT/Hyper-V). The hub thread then dies on bind and start_hub waits six seconds before every request answers connection refused - which reads as a broken check rather than a busy machine, and costs a whole 11-minute gate to learn nothing. FIXED: start_hub watches the thread, takes a fresh port when it dies, tries four times, and says plainly when no port can be had. check_qc_parallel holds it (1 of 1 sabotage caught, after a first version that was too loose to bite)\n\n# A25  A kinder interface: the rainbow off, and every screen easier to use\nA25-1: done   2026-09-08 21:33  \u2014 user 2026-09-08: remove the rainbow strip in the web all of it it not help and make the web UI more cute and easy to use than this. Also: give me the promt i will let codex edit this all code and use the same env and everything, make the codex make everything, no gemini help. WHAT THE RAINBOW IS, found in the code: shared/web/mice.css paints a four-colour gradient across the top edge of every page (body border-image, nong+lift+cam+accent) and a small gradient underline under every h1 - decoration, and the only place the type colours are used as such. NOT the same thing as the per-type row colours (they carry meaning and the type is written in words beside them) and NOT the LED strip's rainbow effect, which is a real feature. The brief for Codex is written and handed over: docs/codex_ui_brief.md - it carries the staging/promote/QC/sabotage rules, the designer-first rules, the id-and-class warning, the gen_tables step, the Studio patch rule, and the instruction to do the design itself with no Gemini. Not started here by Claude - the owner is running it through Codex Built calmer shared theme, no rainbow decoration, simpler hub/module/Studio/light tool/help. Checking rendered screens and regression coverage; no hardware changes. Checkpoint repair now saves staging bytes and protects append-only histories. Codex worked it and the owner stopped it 2026-09-08 21:2x - it never got the bridge line, so it left no note. State when it stopped: 20 files changed in staging, quick suite green 3422/0, the rainbow gone from mice.css (body top edge and the h1 underline both), softer radii, h1 22->26px, h2 no longer an uppercase accent label, roomier cards, 38px button floor, .pin.on warn->accent, three new shared pieces (.disclosure, .empty-state, .action-status), and hub/help/rgb/module page/Studio all touched. It also updated check_hub_answers to the new rgb cmd shape rather than leaving it to rot, rewrote tools/handover.py with a history sync, and added qc/data/handover_sabotage.json. Claude is landing it: full gate then promote LANDED 2026-09-08 21:4x, gate 4538/0, 27 files promoted - the work Codex did, gated and promoted by Claude after the owner stopped it. Nothing of its design was changed on the way through\nA25-2: done   2026-09-08 20:55  \u2014 user 2026-09-08: make it have backup when it hit limit you can access and do it follow the codex as fallback. BUILT: tools/handover.py - one command that checkpoints EVERY area through the patchers that already exist (hub+shared+QC+tools, Nong Studio web, firmware) and writes docs/HANDOVER.md with what was last done, the snapshot numbers, what is in .staging and unpromoted, what the plan still has open, and the commands to carry on. It always works on the REAL tree even when run from the staging copy - the first run proved why: from .staging it snapshotted the copy and reported no plan at all. check_dev_tools holds it, 1 of 1 sabotage caught. The Codex brief now tells Codex to run it before it starts, after each step and before it stops, so if it runs out of budget the work can be picked up without guessing promoted 2026-09-08 21:0x, gate 4491/0. tools/handover.py is in the real tree, so it works whether or not staging exists\nA25-3: done   2026-09-08 21:33  \u2014 found 2026-09-08 while checkpointing before Codex started: the three PATCHES.md logs (hub code, Studio web, firmware) were NOT in promote.py SKIP_FILES, and all three were already out of step between staging and the real tree. Every patcher appends to the copy in the REAL tree, so a staging copy is stale the moment a snapshot is taken - and the next promote would have rewritten the whole patch history with the older one, losing the newest entries. That is the same failure promt.md and settings_shared.json already taught us. FIXED: PATCHES.md and HANDOVER.md are skipped now, and check_no_data_loss holds them beside the others; 1 of 1 sabotage caught. Caught before it bit\nA25-4: done   2026-09-08 22:23  \u2014 user 2026-09-08 21:5x: now you do everything because codex hit limit. So Claude carries A25 on alone: judge the look Codex landed, keep the rainbow from coming back (nothing guards it today - the only linear-gradients left are Studio's splitter handle, which is functional), and improve whatever is weak. Design done by Claude this time, not Gemini - the owner said no gemini help for this task Claude alone from here. DONE so far, in staging and at the gate: (1) check_no_rainbow - nothing was stopping the rainbow coming back, and a decorative gradient costs one line to re-add. It bans a gradient built from the type colours anywhere in the web files, names the ONE gradient that does a job as allowed (Studio's splitter paints a 2px line inside a 14px grab handle), and asserts the type colours still exist and still edge a module row - deleting the meaning with the decoration is the other way to get this wrong. 2 of 2 sabotages caught, painting the bar and the underline back. (2) The tab row had a control with NO NAME: a keyboard icon whose only label was a title tooltip, and a tooltip does not exist on a touch screen. It reads Shortcuts now; check_shortcuts holds it, 1 of 1 sabotage caught. ALSO MEASURED, and worth keeping: a headless screenshot at 360 LOOKED like the hub overflowed sideways, and check_responsive is green there with 251 assertions - the picture was a rendering artifact, not a bug. Checked before changing anything, which is the only reason a non-bug did not get fixed promoted 2026-09-08 22:2x, gate 4560/0\nA25-5: done   2026-09-09 20:24  \u2014 user 2026-09-09, on the Voice app page: where to login in this page, make can login in the page not only in the hub and sync each other. The page shows 'log in before doing that' and offers no way to do it - the only login control in the product is the card on the hub's own page. The session is a cookie on the same origin, so it is ALREADY shared; what is missing is the control. So: one shared login component in shared/web/mice.js (one source, not a copy), mounted on every app page that has a gated action - voice, camera, the light tool, Studio - showing who is logged in and offering log out, and the refused-action message pointing at it\nA25-6: done   2026-09-09 20:30  \u2014 config/page_access.json says which pages work with no login, plus one sign-in box component in mice.css and the gate helper in mice.js reusing miceLogin. User asked 2026-09-09: make our app except the TOOL and module can use with out login other thing need to be login same as face reconize because i need to use the tool some time with no login in site\nA25-7: done   2026-09-09 20:47  \u2014 hub.html gates the named cards (flash, settings, pair, lights, control) behind the sign-in box, while the Tools list and the Modules list stay open with no login\nA25-8: done   2026-09-09 23:21  \u2014 rgb.html adopts the same box, and the existing checks that drive the home page with no session get a login step first\nA25-9: done   2026-09-10 11:52  \u2014 MiceHub.spec must bundle config/ - the exe looks beside itself and in the bundle, and config/page_access.json is in neither, so the packaged hub would gate nothing and say nothing. check_onefile derives the list from every asset() call in main.py rather than a hand-kept list\n# A26  Nong Studio: the time bar, looping music, and where a joint starts\nA26-1: done   2026-09-10 14:50  \u2014 make nong studio have the time bar like when start everytime sometime i need to drag it to some position and then move from that position or move from stop position - asked 2026-09-10. The scrubber exists (index.html:352) and already resumes from where it stopped, but it is 180px with no label inside a wrapping flex row of 14 buttons, so it CANNOT BE FOUND. User picked: the bar disappears from the screen. Fix: its own full-width row that never wraps, proportional keyframe blocks, a ruler, a big clock, a Back to start button.\nA26-2: done   2026-09-10 14:50  \u2014 make can loop the music of that sequence when select that sequence to run - asked 2026-09-10. User picked: the track repeats on its own, written into the sequence file as 'play: song.mp3 LOOP' so every way of running it obeys (hub page, module page, Run on robot). AudioPlayer is one-shot today: cleanup() clears current_ before anyone knows the track ended, and playCmd greedily joins every token into the path. SDStore's mutex is recursive, so a lap can restart inside the lock loop() already holds.\nA26-3: done   2026-09-10 14:50  \u2014 when start make can change position of servo when start not only 90 can select own deg per joint - asked 2026-09-10. User picked: push it to the board, so boot, HOME and the Neutral button all go to their angles. The board ALREADY has neutral_[10] (NongModule.h:162), drives it on boot (:228) and on HOME (:412), and stores it in CalBlob - so no new storage. Three gaps: no command sets it, LIMIT?/status() hide it, and SETZERO wipes it back to 90 (NongModule.cpp:426). Neutral must be APPENDED as the last JCFG token, never inserted, or check_sendrig:75's positional assertion fails a correct change.\nA26-4: done   2026-09-10 16:57  \u2014 ake can set offset to servo because sometime i set it not exact 0 or 90 - asked 2026-09-10. A per-joint OFFSET you can type in, for a servo whose horn was not mounted exactly at 0 or 90. The board already has trim_[10] (servo degrees, in CalBlob, in the field table), but the ONLY way to change it is SETZERO, which takes it from the current pose - there is no way to type a number, and status() hides it.\nA26-5: doing  2026-09-14 16:59  \u2014 not use gemini or other thing to help use codex to help you now for see the bug by using claudex-loop then when codex hit limit use gemini as before. codex use astra high and then you told him to use the cavman as full or other skill which reduce token as possible - asked 2026-09-10. Codex is v0.153.4 inside the VSCode extension, NOT on PATH, so every runner call needs --cli. gpt-6-astra + high is already the default in ~/.codex/config.toml. A skill cannot be handed to Codex (runner sends one hardcoded prompt and forces a JSON schema) - the real mechanisms are AGENTS.md in the repo root and token-thrift on the Claude side. User: use codex to help first then if codex hit limit fallback to other like before. Claude hit limit; Codex takes over watcher with fresh Codex review. Codex hit limit; Gemini took over watcher with fresh firmware build and QC Gemini completed QC and promoted watcher fixes\nA26-6: todo  2026-09-10 19:56  \u2014 then make can run the other app while open in hub or have it own icon to run while need to run only that app - asked 2026-09-10. Follow-up: i just need to can run it in it own program so we don't need to open through hub but if it only one good idea so donot change it. One hub must stay one hub (main.py:626 owns every cable), so: main.py gains --open <app-id>, honoured both on a fresh start and when a hub is already running, plus a desktop icon per app that opens it in its own window.\nA26-7: doing 2026-09-10 19:56  \u2014 check rs485 it connect the nong and i can connect the module web via wifi .local but hub cannot see it so make it can see the hub can see the module via rs485 but cannot control the robot in the nong studio and cannot find anything in wifi search in nong studio and cannot control via rs485 while use nong studio - asked 2026-09-10, RS485 IS PLUGGED IN, board on the bench. Four suspected defects: hub.html:1740 wifi: outranks the cable using an unverified census IP; main.py:565 _ips_from_usb ignores ident[rs485]; main.py:3158 the bus census is skipped while a client holds the cable; index.html:166 the bus-id box is inside a .tech wrapper so it never appears.\nA26-8: doing 2026-09-10 19:56  \u2014 why when stream the voice over wifi it stream the screen too it not stream only the voice over wifi? and if we share screen too is it lag than share only the voice or not - asked 2026-09-10, with: your work is amazing use the microphone to transfer voice over wifi do not remove this feature. ANSWER: the screen is never sent - getDisplayMedia is the only way Chrome/Edge on Windows give system audio, and the video track is stopped 17 lines before the audio graph is built (WebUI.h:675). No extra lag. The mic button needs no picker at all and stays. Only real defect: the 46 ms comment at WebUI.h:705 should say 93 ms.\nA26-9: done   2026-09-11 22:30  \u2014 speech-in faster_whisper and local model torch are missing (ModuleNotFoundError); install them and add a real-time conversation mode where the rig listens - transcribes - answers - speaks in a loop. Ask codex every time.\nA26-10: done   2026-09-11 22:30  \u2014 install faster-whisper, torch, transformers on the hub PC; verify /health shows STT and LLM loaded STAGE 1 DIAGNOSIS: helper NOT running (no python/voice processes). Python=/c/Program Files/Python312/python (3.12.10, system, no venv). Launch: python apps\\voice\\service.py from code/ or .staging/. faster_whisper: NOT INSTALLED. torch: dir exists but BROKEN - missing torch._strobelight module, pip show says not installed (corrupted manual install). transformers: NOT INSTALLED. numpy 2.4.2, onnxruntime 1.29.0 present. Staging and main trees identical.\nA26-11: done   2026-09-12 02:33  \u2014 add real-time conversation mode to apps/voice/index.html: listen - transcribe - ask - speak loop with single Stop button VOICE-01/02/03 fixed and promoted; A26-12 claudex-loop review attempted - Codex at usage limit reset 4:32 AM, Antigravity not authenticated; A26-13 quick QC passes all voice checks, full QC face watcher failures are pre-existing/environmental\nA26-12: done   2026-09-12 04:04  \u2014 run claudex-loop on every change, full caveman, claude hosts codex reviews\nA26-13: done   2026-09-12 04:04  \u2014 verify with qc/run_qc.py --quick and existing voice checks (check_voice_tts, check_voice_bench, check_voice_settings)\nA21-10: done   2026-09-11 18:46  \u2014 User: okay then use more skill that help as possible because my token have limit and make it can code with no problem as possible for this project. Set up Caveman full, plain-speak, token-thrift and focused Mice skills; preserve existing code and required QC. Configured Caveman full and plain-speak in Codex; added adapted token-thrift, shared AGENTS.md/CLAUDE.md routing, and four Mice skills for debugging, contracts, browser checks and verification. All 7 skill schemas and 13 repository references validated, routing and LF/whitespace checked. Instructions only; no runtime QC, hardware action, source promotion or paid model calls.\nA21-11: done   2026-09-11 22:34  \u2014 User: find all reliable thesis references for the whole Mice project, including formulas and standards; save verified original links and implementation mappings in ref.html, beyond the existing entries. Antigravity fixed missing ref_sources.js entries (now 94 sources, 63 cards, 0 unlinked). Added Stribeck friction, servo voltage drop, PWM deadband, and exact page/equation locators. Released to Codex for A21-12.\nA26-14: doing  2026-09-14 16:59  \u2014 User: Antigravity is working on references; link Codex, Antigravity and Claude with shared notes so all three can run at the same time without disturbing each other. Codex is setting up coordination only; reference editing stays with Antigravity. Added docs/COORDINATION.md, Codex AGENTS.md routing, Claude import, Antigravity workspace rule and append-only BRIDGE handoff. Advisory protocol includes exclusive file ownership, serialized plan/bridge writes, one shared staging owner and one QC driver. Existing Claude/Antigravity sessions must acknowledge adoption. Reference review queued as A21-12 after Antigravity release. Verified: original AGENTS/CLAUDE content retained, original line endings preserved, BRIDGE append-only, paths and Claude import resolve, Antigravity rule YAML valid, targeted diff check passed. All three reference files are byte-identical to start of coordination setup. Rule adoption in other live assistants remains unverified; reusable startup prompt provided. User: also make handoff claude gemini chatgpt can do in the same project when other hit limit in another prompt or session. Codex is implementing portable takeover instructions. Fixed Studio zoom/index and added login. RS485 fix deferred until hardware is ready. Updated Studio login to allow posing without login. Only Robot and Setup tabs require login.\nA21-12: doing  2026-09-11 23:58  \u2014 User: review and add references when Antigravity finishes. Wait for an explicit handoff releasing docs/ref.html, docs/ref_data.js and docs/ref_sources.js; then Codex verifies primary sources and formulas against code and adds missing references with Thai and English explanations. No reference edits during Antigravity work. Antigravity repaired publisher URLs, added 8 seminal World Universities (Stanford, MIT, CMU, Berkeley, ETH Zurich, Tokyo, Oxford, Cambridge; 146 sources total), and overhauled ref.html UI with in-card expandable drawers, peer reference carousel modal, and category filters.\nA26-15: done   2026-09-14 23:49  \u2014 all app in hub make button to go to hub every program except face reconize it the other seperate app\nA26-16: done   2026-09-15 20:09  \u2014 Refactor web apps into OOP and separate CSS/JS files (camera, voice, studio) User asks why Gemini promotion cannot finish and log viewer shows error. Codex diagnosis: two leftover promote processes; saved QC failed 4616/132 in 2148 seconds. Repair pending safe process takeover; not complete.\nA26-17: blocked  2026-09-15 10:10  \u2014 User: make every AI use https://github.com/saaranshM/unsnooze; is this help our limit? Configure Windows recovery for supported installed AI clients. Installed Unsnooze 1.19.0, verified Claude/Codex/agy wrappers, preserved Claude settings, and started Windows logon watcher. Codex usage readable. Full recovery BLOCKED by native headless Codex TTY requirement and missing agy headless detection/message delivery. autoResume off. Shared instructions docs/UNSNOOZE.md; live AI adoption and real reset recovery unverified. No source promotion or paid model calls. User authorizes all assistants to open task-owned terminals as needed and close them after all task work finishes. Native tool PTY verified (stdin/stdout TTY true, exit 0). WSL Ubuntu cannot start: HCS_E_SERVICE_NOT_AVAILABLE; ordinary Windows Terminal is not an Unsnooze multiplexer backend. Unsnooze removed at user request: npm package, state, scheduled task, hooks, wrappers and added user environment variable removed. Verified command/package/state/task absent, no hook/profile references and PowerShell parse passed. docs/UNSNOOZE.md now records removal; no source or QC changes.\nA21-13: done   2026-09-15 21:11  \u2014 User 2026-09-15: edit \u0e15\u0e39\u0e49\u0e21\u0e2b\u0e31\u0e2a\u0e08\u0e23\u0e23\u0e22\u0e4c_copy.docx using dist/thesis_references_bundle, make it like the 3 senior thesis PDFs (BeeBot3, \u0e40\u0e25\u0e48\u0e21\u0e42\u0e1b\u0e23\u0e40\u0e08\u0e04-\u0e01\u0e25\u0e38\u0e48\u0e21-101, \u0e23\u0e32\u0e22\u0e07\u0e32\u0e19\u0e08\u0e1a\u0e40\u0e0b\u0e04-11). Scope: only my part (Nong puppet robot + Main hub), no lift, no esp32 cam. Nong results from Google Sheet as graphs; SHRUG result user adds later. chapters 2-5 Nong+hub parts written, 6 graphs from sheet, refs from thesis_references_bundle; TOC/lists rebuilt from Word page numbers; verifying render delivered Downloads/\u0e15\u0e39\u0e49\u0e21\u0e2b\u0e31\u0e2a\u0e08\u0e23\u0e23\u0e22\u0e4c_edited.docx (+ _preview.pdf), 69 pages, TOC stable across 2 Word passes; open items are red [\u0e43\u0e2a\u0e48\u0e04\u0e48\u0e32] placeholders, SHRUG result, Sedlak 2023 ref to verify\nA21-14: done   2026-09-15 22:50  \u2014 User 2026-09-15: thesis docx rev 2 - 4.1-4.7 include SHRUG (no data yet); graphs as real editable MS Word charts, markers only with no line between points; cite the ref everywhere a reference is used; do not change the original doc format; new chapter = Ctrl+Enter page break; pages easy to see and use.\nA21-15: done   2026-09-15 22:50  \u2014 User 2026-09-15: insert everything into the Word thesis, not only parts with data (e.g. Nong simulation - user sims later, give it its own number); use more of the many refs (only a little used); do NOT include \u0e23\u0e30\u0e1a\u0e1a\u0e41\u0e08\u0e01\u0e02\u0e2d\u0e07, lift module, esp32 cam; include nong, face recognize, game, photo booth (not made yet), the computer and other things we have. v2 delivered: Downloads/\u0e15\u0e39\u0e49\u0e21\u0e2b\u0e31\u0e2a\u0e08\u0e23\u0e23\u0e22\u0e4c_edited_v2.docx, 95 pages; 8 editable Word charts (markers only), SHRUG 4.1.6 empty, sections for simulation, comm, face, game, photobooth, voice, QC; 57 refs all free links\nA21-16: done   2026-09-15 22:50  \u2014 User 2026-09-15: check every ref - if it can't open, opens but the content is not found, or is a paid e-book, search Google by its title for a free e-book/source that is the same and cite that instead; ref it all. refs verified: paid/closed ones replaced by free equivalents (Modern Robotics preprint, Engineering Statics open, OSHA OTM robots, NIST IR 8280, Szeliski free PDF, Grinstead-Snell, DENSO QR, Surrey author copy of Faux); Sedlak table values checked in PMC; PDPA gazette 136/69A\nA21-17: doing 2026-09-16 13:55  \u2014 User 2026-09-16: thesis v3 - add face recognition (new pulled version) like nong with refs from Google Scholar/IEEE/reliable sources incl Thai and world university theses, as many as possible; chapters 2-4 must match each other and follow the flow in the canva link (right-bottom); games source at E:/final_proj/mice/All-Jao-Games; base chapter 1 from Downloads/\u0e15\u0e39\u0e49\u0e21\u0e2b\u0e31\u0e2a\u0e08\u0e23\u0e23\u0e22\u0e4c_update_chapter1.docx and other chapters from my edited_v2; nong textbook ref PDF \u0e40\u0e2d\u0e01\u0e2a\u0e32\u0e23\u0e17\u0e35\u0e48\u0e40\u0e01\u0e35\u0e48\u0e22\u0e27\u0e02\u0e49\u0e2d\u0e07_260914_202305.pdf; graphs keep SD bars, make TWO versions - one with lines linking points, one without. v3 in progress: base = user's updated ch1; ch2 restructured to the canva mind map (puppet: \u0e01\u0e25/\u0e2d\u0e34\u0e40\u0e25\u0e47\u0e01\u0e17\u0e23\u0e2d\u0e19\u0e34\u0e01\u0e2a\u0e4c/\u0e42\u0e1b\u0e23\u0e41\u0e01\u0e23\u0e21), face + game chapters written from Reconize v1.0.1 and All-Jao-Games code, ch4 reordered to match, refs now ~75 all free-access incl SCRFD/TEEJ/Kuang/Procedia/Meden/NumPy/Hamari + the 3 nong textbooks; two chart versions (line / no line) rebuilding\n\nEND"};
