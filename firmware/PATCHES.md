# Firmware patches

Every change to the firmware is saved here as a numbered patch. Old patches are never removed — each row is a snapshot you can restore with `python save_fw_patch.py --restore <n>`.

| # | when | change |
|---|---|---|
| 0001 | 2026-08-04 02:38 | baseline before Phase A firmware work (env rename, batch command, SETZERO guard) |
| 0002 | 2026-08-04 03:28 | Extensible firmware: servo presets moved to config/servos.json and generated into ServoPresets.h by tools/gen_tables.py at build time (zero runtime cost, flash unchanged at 65.1%); the SERVO command now looks presets up in that table instead of a hardcoded if-chain, and its help text lists whatever the table holds. |
| 0003 | 2026-08-04 03:39 | Commands declared once in config/commands.json, generated into CommandHelp.h at build time. New HELP / HELP <cmd> answers from that table so the board cannot advertise a command it lacks. QC checks both directions - declared-without-handler and handler-without-declaration - which found PINS and PINS? had existed undocumented (they are aliases of PIN?). |
| 0004 | 2026-08-04 03:48 | Module capabilities: each module declares what it can do (addCapabilities), status carries a caps list, and the module website shows each card by capability instead of switching on the module type - so a new type shows the right controls with no site edit. Old firmware without caps still renders via a type fallback. |
| 0005 | 2026-08-04 09:03 | JCFG batch command: one joint's whole setup (gear, pulse, dps, travel, hz, limits) in a single line, validated exactly as the five individual commands are, re-attaching only that joint. Native logic tests now RUN (MinGW installed): NongMath.h no longer depends on the POSIX M_PI, ESP32-only sketches moved to test/esp32_sketches so the native env stops compiling them, and lib_ignore keeps the Arduino-only project libraries out. |
| 0006 | 2026-08-04 10:29 | WiFi can no longer stall the servo loop: applyHostname deferred its STA restart instead of delay(150), which ran from loop() and would stop a joint mid-move. The connect path was already a polled state machine; a QC check now keeps it that way. |
| 0007 | 2026-08-04 10:44 | before module website restructure (Phase C) - snapshot so the current single-column layout can be restored with --restore |
| 0008 | 2026-08-04 10:50 | Module website restructured into three tabs (Control / Sequences & files / Setup) instead of thirteen cards in one column. Nothing moved in the DOM - each card carries data-tab and one applyTabs() decides visibility from BOTH the open tab and the login, so the setup gate cannot disagree with the tabs. Every id and handler unchanged. |
| 0009 | 2026-08-04 13:50 | TWO bugs found on real hardware: (1) HELP answered with 54 lines but every channel here is one-command-one-line, so its extra lines arrived after the next command's buffer flush and CAL came back as a fragment of HELP - bare HELP now lists names on one line and HELP <name> explains one. (2) SET WIFI took the SSID as a single token, so a real network name with a space (MSI 3058) stored only 'MSI' - it now accepts a quoted SSID and echoes back what it stored. |
| 0010 | 2026-08-04 14:28 | The robot can run a sequence with NO SD card: FBEGIN/FDATA/FEND fall back to an 8KB memory buffer when no card is fitted, and MOVE plays that. SequencePlayer.startText + SDStore.parseYaml parse YAML straight from memory, touching no SD hardware. Studio needs no change - it already uploads with these commands, and the BOARD decides where the sequence lands. |
| 0021 | 2026-08-09 01:03 | One binary per module type: pio run -e mice_nong / mice_lift / mice_blank. Module classes, pin entries, commands and web cards are chosen at BUILD time (-D MICE_TYPE_*, src/core/BuildTypes.h, <!--#type--> markers in the master src/web/WebUI.h), and gen_tables.py writes this env's page and tables into .pio/build/<env>/generated/. Nong 75.0% -> 65.5% flash, and a lift binary no longer contains id=jointRows at all. |
| 0022 | 2026-08-09 01:19 | One player: a motion command from outside stops the sequence the board is playing on its own clock, so Nong Studio and the module's own sequence can never drive the servos at the same time. Which commands move is declared once in config/commands.json (motion: true) and compiled into the table HELP reads; the player's own steps go through CommandRouter::handleFromSequence so a sequence cannot stop itself; settings (SPEED/LIMIT/SERVO) deliberately do not interrupt a show. |
| 0023 | 2026-08-09 06:37 | OTA over WiFi: POST /api/ota writes new firmware into the app slot that is NOT running (min_spiffs.csv has app0+app1, 1.875MB each), so a failed or half-sent update leaves the board booting what it had. Refused while the module is moving or a sequence is playing; force=1 overrides. New OTA command reports running/target slot and free room. +6KB flash. |
| 0024 | 2026-08-09 07:13 | Camera module: cam is a third module type (ESP32-CAM, board esp32cam) with SNAP/CAM commands, a /api/cam.jpg frame endpoint and its own web card, at 68.0% flash and costing nong/lift nothing. Module types became a registry (config/modules.json): BuildTypes.h and the ModuleTable factory are generated from it, so ModuleFactory.cpp is no longer a hand-kept list and the hub reads the same file for what it can flash. |
| 0025 | 2026-08-09 13:01 | Bench fixes from the first real-hardware run: blank fell out of the generated module table (tokens after #endif on one line are discarded); preemption stopped a show for a command the module could not run (now after the command, only when taken); SDStore claimed the ESP32-CAM's camera data pins as SPI (now refuses -1 pins before touching the bus, and a cam board declares no SPI card); the camera module no longer copies frames at all, because a plain malloc(16KB) panics on that board while captures are fine. |
| 0026 | 2026-08-09 15:08 | Second bench session, over WiFi: /api/cam.jpg sent the driver's frame buffer and truncated the picture to 353 of 13000 bytes (now a small private copy, frame returned before the send is queued); PSRAM disabled on the cam build because with it ANY large HTTP response killed the board, including the 31KB module page, while the same core serves it fine on a non-PSRAM board. OTA over WiFi proven twice on the camera board; real 320x240 JPEGs fetched. |
| 0027 | 2026-08-09 16:49 | The camera card blamed the ribbon cable for a working camera: opened from the hub, its <img src=/api/cam.jpg> bypassed the hub's fetch shim and asked the hub instead of the board. The page now fetches the frame (so it follows the page's own transport) and prints the module's real refusal; the hub gained /api/dev/cam.jpg, which proxies over WiFi and refuses with 501 over a cable. |
| 0028 | 2026-08-09 17:34 | Lag fixes measured on real boards: main loop now yields (delay(1)) so the web task is not starved behind the router mutex (USB POSE 30ms -> 13ms); WiFi.setSleep(false) so the radio does not doze between beacons (ping 73ms -> 17ms); the camera live view chases the board instead of a 1s timer (1 fps -> 7.3 fps). |
| 0029 | 2026-08-09 19:24 | Camera live view is now an MJPEG stream (/api/cam.stream): many frames down one connection instead of one request per picture, 1 -> 7.3 -> 18.7 fps direct and 22.2 through the hub, because capture costs 25ms while a separate request cost 120ms on a server that closes every connection. One viewer at a time; the hub pipes it through for a module opened from there. |
| 0030 | 2026-08-09 20:52 | Camera picture quality, measured: the orange/blue lines were corrupted scanlines, not noise. Sensor clock 20 -> 10 MHz (VGA: ~37 corrupted rows per frame -> 0-4), the OV2640's bpc/wpc/lenc/raw-gamma corrections enabled, and the sensor starts on SVGA because corruption tracks frame size (qqvga 0.8, qvga 26.4, vga 12.8, svga 1.6). SVGA cannot be the INIT size without PSRAM (init fails 0x105), so the buffer is allocated at QVGA and the sensor moved up after. Also: a dead live viewer no longer latches the camera forever. |
| 0031 | 2026-08-09 21:16 | Camera picture quality, second pass: reverted the GRAB_WHEN_EMPTY experiment (the driver refuses it with one frame buffer, init 0x105) and re-tested PSRAM with every later fix in place - it still kills every large HTTP response, so it stays off. Live view now measures 80% completely clean frames at qqvga, and 1.4 bad rows out of 600 at svga. |
