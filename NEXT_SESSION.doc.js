window.LIVEDOC={"stamp": "2026-08-20 23:44:50", "html": "<h1>Plan: split the firmware per module type \u2014 ALL FIVE STEPS DONE 2026-08-09</h1>\n<p>Every step below is finished and landed. The tree is green at <strong>QC 1290</strong></p>\n<p>(it was 1021 when this plan was written). Each step&#x27;s own section says what it</p>\n<p>turned into, and the CHANGELOG has the user-facing version.</p>\n<table>\n<tr><th>step</th><th>what</th><th>result</th></tr>\n<tr><td>1</td><td>one binary per module type</td><td>nong 65.9%, lift 72.8%, cam 68.0%, blank 62.5%</td></tr>\n<tr><td>2</td><td>the hub as the show clock</td><td><code>POST /api/play</code> \u2014 a rehearsal survives the browser</td></tr>\n<tr><td>3</td><td>hub flashes over USB</td><td>\u26a1 flash \u2026 as, on any port, cable handed to esptool</td></tr>\n<tr><td>4</td><td>OTA over WiFi</td><td>two app slots existed all along; a failed update cannot brick</td></tr>\n<tr><td>5</td><td>camera module</td><td><code>cam</code> type + <code>config/modules.json</code> registry</td></tr>\n</table>\n<p><strong>All of it has now run on real hardware</strong> (2026-08-09, three boards):</p>\n<table>\n<tr><th>board</th><th>as</th><th>proven</th></tr>\n<tr><td>COM9</td><td><code>nong</code></td><td>flashed from the hub, boots as nong, type gate refuses <code>lift</code>, sequence/preemption and the hub clock drive it</td></tr>\n<tr><td>COM16</td><td><code>lift</code></td><td>flashed from the hub, <code>types:[&quot;lift&quot;,&quot;blank&quot;]</code>, <code>STAGE?</code>/<code>RGB</code> answer</td></tr>\n<tr><td>COM17</td><td><code>cam</code></td><td>flashed from the hub, <strong>updated over WiFi twice</strong>, serves real 320\u00d7240 JPEGs</td></tr>\n</table>\n<p>Six faults came out of those sessions that no PC-side check could have found \u2014</p>\n<p>see the CHANGELOG entries for 2026-08-09. Each has a QC check now.</p>\n<p>Still open on hardware:</p>\n<ol>\n<li>A <strong>nong board with servos actually attached</strong> \u2014 COM9 is lift hardware, so</li>\n</ol>\n<p>   the joints were never driven against a real arm.</p>\n<ol>\n<li><strong>RS485</strong> between two modules has not been exercised this session.</li>\n<li>The camera&#x27;s <strong>SD card</strong> path (<code>SNAP &lt;name&gt;</code>) \u2014 that board has no card, and</li>\n</ol>\n<p>   its socket is on the SDMMC bus the camera also needs.</p>\n<p>Also still open, and needing you rather than code: the SHRUG measurements below,</p>\n<p>and the two boards that drop off USB (cable or power).</p>\n<p>Read first: <code>code/qc/README.md</code>, <code>code/firmware/CLAUDE.md</code>,</p>\n<p><code>code/nong/main_python_set_nong/CLAUDE.md</code>. Work in <code>code/.staging/</code> and land</p>\n<p>through <code>python promote.py</code> (full QC gate).</p>\n<hr>\n<h2>Why this is being done</h2>\n<p>One binary today contains <strong>every</strong> module type. A nong carries the lift code</p>\n<p>*and* the lift&#x27;s web cards, and vice versa. Measured on 2026-08-07:</p>\n<table>\n<tr><th></th><th></th></tr>\n<tr><td>flash used</td><td><strong>74.9%</strong> \u2014 1,472,545 of 1,966,080 bytes</td></tr>\n<tr><td>free</td><td>~493 KB</td></tr>\n<tr><td>module website served by every board</td><td><strong>43.9 KB</strong></td></tr>\n<tr><td>a lift&#x27;s page contains <code>id=&quot;jointRows&quot;</code> (nong markup)</td><td><strong>yes</strong> \u2014 hidden at runtime, not absent</td></tr>\n</table>\n<p>Three more module types are planned plus a camera (esp32-camera is ~60\u2013120 KB).</p>\n<p>Adding any of it to the shared binary costs <strong>every</strong> module.</p>\n<p>The user&#x27;s own insight resolved it: **if the hub flashes boards, each board only</p>\n<p>needs its own type.** Per-type builds then cost the other modules nothing.</p>\n<p>It also closes a safety hole. Only a runtime capability check (<code>has(&#x27;joints&#x27;)</code>)</p>\n<p>stops a lift showing joint sliders \u2014 and that check has already failed once:</p>\n<p><code>isNong</code> was used and never defined, which threw mid-handler and made the *right*</p>\n<p>controls vanish. The mirror of that bug shows the *wrong* controls. With per-type</p>\n<p>builds the wrong markup is <strong>absent</strong>, so no JavaScript bug can show it.</p>\n<hr>\n<h2>Step 1 \u2014 split the build per module type \u2014 <strong>DONE 2026-08-09</strong></h2>\n<p>Landed as firmware patch 0021. What it turned into, and the numbers:</p>\n<table>\n<tr><th>build</th><th>flash</th><th>page</th></tr>\n<tr><td>all types (<code>mice_module_firmware</code>, kept)</td><td>75.0%</td><td>43.1 KB</td></tr>\n<tr><td><code>mice_nong</code></td><td>65.5%</td><td>38.5 KB</td></tr>\n<tr><td><code>mice_lift</code></td><td>72.5%</td><td>31.9 KB</td></tr>\n<tr><td><code>mice_blank</code> (core only)</td><td>62.5%</td><td>27.2 KB</td></tr>\n</table>\n<ul>\n<li>Type chosen by <code>-D MICE_TYPE_*</code> per env; <code>src/core/BuildTypes.h</code> turns it into</li>\n</ul>\n<p>  <code>MICE_HAS_LIFT</code> / <code>MICE_HAS_NONG</code>, and no flag means &quot;carry everything&quot;.</p>\n<ul>\n<li><code>src/web/WebUI.h</code> is now the <strong>master</strong> page and is not compiled.</li>\n</ul>\n<p>  <code>tools/gen_tables.py</code> writes this env&#x27;s page, command table and servo table</p>\n<p>  into <code>.pio/build/&lt;env&gt;/generated/</code>; per-type regions are marked</p>\n<p>  <code>&lt;!--#type nong--&gt;</code> \u2026 <code>&lt;!--#end--&gt;</code> (and <code>//#type</code> inside <code>&lt;script&gt;</code>).</p>\n<ul>\n<li>Verified in the BINARY, not the source: <code>check_build_split.py</code> searches</li>\n</ul>\n<p>  <code>firmware.bin</code> for the other type&#x27;s markup, and was proven by breaking the</p>\n<p>  split and watching those six assertions fail.</p>\n<ul>\n<li>Also fixed on the way: the ArduinoJson <code>-I</code> in platformio.ini named one env&#x27;s</li>\n</ul>\n<p>  libdeps folder and would have been wrong for every other env.</p>\n<p><strong>One assumption below turned out wrong.</strong> Step 4 says each binary would be</p>\n<p>&quot;well under 1 MB&quot; after the split. It is not: <strong>62.5% is the floor</strong> \u2014 WiFi, the</p>\n<p>web server, SD and YAML, before any module exists. The nong module adds ~58 KB,</p>\n<p>the lift ~196 KB (it owns FastLED and the MP3 decoder). So OTA has to be judged</p>\n<p>against ~1.29 MB (nong) and ~1.42 MB (lift), not ~0.9 MB. A dual-app 4 MB layout</p>\n<p>gives roughly 1.5 MB per slot: the nong fits, the lift is tight, and dropping</p>\n<p>FastLED/ESP8266Audio from the lift (or a 8 MB module) is the lever if it does</p>\n<p>not.</p>\n<p>Original goal, for reference: <code>mice_nong</code>, <code>mice_lift</code>, <code>mice_blank</code> (names</p>\n<p>open), each sharing the same core and containing <strong>only its own</strong> module class</p>\n<p>and web cards.</p>\n<p>Shared core, unchanged: <code>Identity</code>, <code>WebPortal</code>, <code>CommandRouter</code>, <code>RS485Bus</code>,</p>\n<p><code>SequencePlayer</code>, <code>SDStore</code>, <code>PeerDiscovery</code>, groups/WiFi.</p>\n<p>Where the per-type content lives today:</p>\n<ul>\n<li><code>src/modules/ModuleFactory.cpp</code> \u2014 registers every type</li>\n<li><code>src/web/WebUI.h</code> \u2014 ONE page, all cards, C++ raw string literal</li>\n<li><code>config/commands.json</code> \u2014 entries already carry <code>scope: core|lift|nong</code></li>\n<li><code>firmware/tools/gen_tables.py</code> \u2014 <strong>already generates headers at build time</strong></li>\n</ul>\n<p>  (<code>ServoPresets.h</code>, <code>CommandHelp.h</code>) via a PlatformIO <code>extra_scripts</code> pre-hook</p>\n<p><strong>Use the generator that already exists.</strong> <code>gen_tables.py</code> is the established</p>\n<p>pattern \u2014 extend it to emit a per-type <code>WebUI.h</code> and a per-type command table</p>\n<p>from a build flag (e.g. <code>-D MICE_TYPE_NONG</code>). Do not hand-maintain three copies</p>\n<p>of the web page.</p>\n<p>Suggested shape in <code>platformio.ini</code>:</p>\n<pre><code>\n[env:mice_nong]\nextends = env:mice_module_firmware\nbuild_flags = ${env:mice_module_firmware.build_flags} -D MICE_TYPE_NONG\n</code></pre>\n<p>Keep <code>mice_module_firmware</code> working (all types) until the split is proven \u2014</p>\n<p><code>check_firmware_build.py</code> builds it and QC depends on it.</p>\n<p><strong>Verify:</strong> each env builds; flash % drops for each; a lift binary contains no</p>\n<p><code>jointRows</code>; <code>pio test -e native</code> still passes (20 tests).</p>\n<hr>\n<h2>Step 2 \u2014 hub becomes the show clock \u2014 <strong>DONE 2026-08-09</strong></h2>\n<p><code>ShowPlayer</code> in <code>main_python/main.py</code> + <code>POST /api/play</code>, <code>GET /api/play</code>,</p>\n<p><code>POST /api/play/stop</code>. Studio hands the timeline over whenever the link goes</p>\n<p>through the hub (<code>hubDriven()</code> = live link and not Web Serial direct); the page</p>\n<p>then draws only, and <code>playTick</code> sends nothing.</p>\n<ul>\n<li>Same wire traffic as before \u2014 <code>POSE \u2026 T &lt;ms&gt;</code>, one whole move at a time. The</li>\n</ul>\n<p>  hub decides only WHEN a move starts, so the motion matches a module-played</p>\n<p>  show exactly.</p>\n<ul>\n<li>One clock still: <code>MOVE STOP</code> before it starts, and a live motion command for</li>\n</ul>\n<p>  the same device stops the hub player \u2014 the motion list is read from</p>\n<p>  <code>firmware/config/commands.json</code>, the file the firmware compiles, so the two</p>\n<p>  cannot disagree.</p>\n<ul>\n<li>Returning to the page re-syncs the play head from <code>GET /api/play</code>; the module</li>\n</ul>\n<p>  hand-off is skipped while the hub drives.</p>\n<ul>\n<li><code>check_hub_clock.py</code>, proven by breaking the wait and the takeover.</li>\n<li>Web Serial direct is unchanged: the browser owns that port, so the hub cannot</li>\n</ul>\n<p>  reach the robot at all.</p>\n<p>Original plan below.</p>\n<h3>Step 2 (original)</h3>\n<p>This is the fix for *&quot;it does not run in the background&quot;*, and it is the biggest</p>\n<p>day-to-day win for the user.</p>\n<p>The browser <strong>cannot</strong> be fixed for this: a hidden tab has <code>requestAnimationFrame</code></p>\n<p>stopped and timers throttled to ~once a minute. That is browser policy, not a</p>\n<p>JavaScript limitation. The user asked directly whether Python could do it \u2014</p>\n<p><strong>yes</strong>, and that is the answer. The hub is a native process that already owns</p>\n<p>the serial ports and nothing throttles it.</p>\n<p>Three tiers, each right for a different job:</p>\n<table>\n<tr><th>Clock</th><th>Survives</th><th>Use for</th></tr>\n<tr><td>Module (<code>MOVE &lt;file&gt;</code>)</td><td>PC off entirely</td><td>the show</td></tr>\n<tr><td><strong>Hub (Python)</strong></td><td>browser closed</td><td><strong>editing / rehearsal \u2014 the missing one</strong></td></tr>\n<tr><td>Browser</td><td>nothing</td><td>preview only</td></tr>\n</table>\n<p>The hub tier is also the only one that helps while *editing*, before a sequence</p>\n<p>has been uploaded.</p>\n<p>Already in place and should be kept: Studio hands off to the module on</p>\n<p><code>visibilitychange</code> (<code>handOffToRobot</code>), and <code>playTick</code> clamps <code>dt</code> to</p>\n<p><code>MAX_TICK_MS</code> so returning to the page cannot teleport the show.</p>\n<hr>\n<h2>Step 3 \u2014 hub flashes boards over USB \u2014 <strong>DONE 2026-08-09</strong></h2>\n<p><code>Flasher</code> in <code>main_python/main.py</code> + <code>GET /api/flash/images</code>,</p>\n<p><code>POST /api/flash?port=&amp;type=</code>, <code>GET /api/flash</code>. The hub page shows</p>\n<p><strong>\u26a1 flash \u2026 as</strong> on every USB port, including ones where nothing answered.</p>\n<ul>\n<li>The cable is given up for the whole job: the show player is stopped if it was</li>\n</ul>\n<p>  on that port, the handle is closed, and <code>_flash_ports</code> makes <code>_usb_get</code> and</p>\n<p>  the probe refuse it until esptool finishes. This is the <code>port is busy</code> fix.</p>\n<ul>\n<li>esptool comes from PlatformIO&#x27;s own copy (`~/.platformio/packages/</li>\n</ul>\n<p>  tool-esptoolpy/esptool.py`), run with PlatformIO&#x27;s python when the hub is</p>\n<p>  frozen. <code>MICE_ESPTOOL</code> overrides it \u2014 QC drives the whole path with a fake.</p>\n<ul>\n<li>Images are <code>firmware/.pio/build/mice_&lt;type&gt;/</code> (bootloader/partitions/</li>\n</ul>\n<p>  boot_app0/firmware). Not built = the page says which command builds it.</p>\n<ul>\n<li><code>check_flash.py</code>, proven by removing the cable guard.</li>\n</ul>\n<p><strong>Still open for a fresh machine:</strong> <code>MiceHub.exe</code> has no esptool bundled, so on</p>\n<p>a PC without PlatformIO the button explains itself instead of working. Vendoring</p>\n<p>esptool (~0.5 MB) into the repo is the fix when that matters.</p>\n<h3>Step 3 (original)</h3>\n<p>The hub already knows which module is on which cable. Pick a type in the Network</p>\n<p>tab \u2192 it flashes the right binary.</p>\n<ul>\n<li>needs esptool; <code>main.py</code> is stdlib-only, but <code>MiceHub.exe</code> is PyInstaller-frozen</li>\n</ul>\n<p>  so bundling is fine</p>\n<ul>\n<li><strong>stop the hub, or close the port first</strong> \u2014 uploads fail <code>port is busy</code></li>\n</ul>\n<p>  otherwise (hit repeatedly on 2026-08-07)</p>\n<hr>\n<h2>Step 4 \u2014 OTA, flashing over WiFi \u2014 <strong>DONE 2026-08-09</strong></h2>\n<p><strong>The blocker below was wrong, and it was wrong from the start.</strong></p>\n<p><code>min_spiffs.csv</code> does not give one app partition \u2014 it gives <strong>two</strong>:</p>\n<pre><code>\napp0, app, ota_0, 0x10000,  0x1E0000     1,966,080 bytes\napp1, app, ota_1, 0x1F0000, 0x1E0000     1,966,080 bytes\n</code></pre>\n<p>That is why the build always reported &quot;used X from 1966080 bytes&quot; \u2014 1966080 is</p>\n<p>one slot, not the flash. So OTA needed no partition change and was never</p>\n<p>blocked by size: nong sits at 66% of a slot, lift at 73%.</p>\n<p>What was actually built:</p>\n<ul>\n<li>Firmware <code>POST /api/ota</code> (multipart <code>firmware.bin</code>) using <code>Update</code>, then</li>\n</ul>\n<p>  reboot. Refused while moving or while a sequence plays (<code>?force=1</code> overrides)</p>\n<p>  \u2014 a flash write stalls the servo loop.</p>\n<ul>\n<li><code>OTA</code> command: <code>running=app0 size=\u2026 target=app1 room=\u2026 free=\u2026</code>.</li>\n<li>Hub <code>POST /api/ota?ip=&amp;type=</code>, sharing the one flashing job with USB</li>\n</ul>\n<p>  flashing, and <strong>\u26a1 update over WiFi</strong> on any module row with an IP.</p>\n<ul>\n<li>Only the app image goes over the air; bootloader and partition table never do.</li>\n<li><code>check_ota.py</code> \u2014 two slots, every build fits one with margin, the board&#x27;s</li>\n</ul>\n<p>  guards, and the image arrives byte for byte. Proven by truncating the upload.</p>\n<ul>\n<li>Cost: 6 KB of flash.</li>\n</ul>\n<p><strong>First update still needs a cable</strong> \u2014 older firmware has no <code>/api/ota</code>.</p>\n<h3>Step 4 (original, with its wrong premise)</h3>\n<p>The user specifically wants this and finds it the most interesting part. It is</p>\n<p>step 4 only because of a hard blocker, not priority:</p>\n<p>OTA needs <strong>two</strong> app partitions so a failed update can boot the old firmware.</p>\n<p>Today <code>board_build.partitions = min_spiffs.csv</code> gives ONE 1.9 MB app partition.</p>\n<p>On 4 MB flash, OTA means ~1.5 MB each \u2014 and the current binary is <strong>1.47 MB</strong>.</p>\n<p>That fits with ~30 KB spare, which is no margin.</p>\n<p><strong>After step 1 each binary is well under 1 MB and OTA fits comfortably.</strong> The</p>\n<p>split is what makes WiFi flashing safe, not a detour before it.</p>\n<hr>\n<h2>Step 5 \u2014 camera module \u2014 <strong>DONE 2026-08-09</strong></h2>\n<p><code>cam</code> is a real module type: <code>src/modules/cam/CamModule.{h,cpp}</code>,</p>\n<p><code>[env:mice_cam]</code> (board <code>esp32cam</code>, min_spiffs so it gets OTA too), <code>SNAP</code> and</p>\n<p><code>CAM</code> commands, a <code>/api/cam.jpg</code> frame endpoint and its own web card. 68.0% of</p>\n<p>flash, and nong/lift are unchanged by it existing.</p>\n<p>On the way it turned module types into a registry \u2014 <code>config/modules.json</code> \u2014</p>\n<p>because &quot;just another type&quot; is only true if adding one is a single declaration.</p>\n<p><code>BuildTypes.h</code> and <code>ModuleTable.h</code> (the factory) are generated from it, and the</p>\n<p>hub&#x27;s flash list reads it, so <code>ModuleFactory.cpp</code> is no longer a list.</p>\n<p><code>check_camera.py</code> walks that chain; <code>check_build_split.py</code> builds the camera too.</p>\n<p><strong>Not run on hardware.</strong> The ESP32-CAM has not been flashed or photographed</p>\n<p>with. Everything here is a real build of the real driver plus source-level</p>\n<p>checks. Flashing it may need BOOT/IO0 held to GND (the CAM-MB shield&#x27;s button).</p>\n<h3>Step 5 (original)</h3>\n<p>ESP32-CAM (AI-Thinker) + CAM-MB shield (CH340, appeared as COM17). By then it is</p>\n<p>just another type, costing the other modules nothing.</p>\n<p><strong>It can never be a servo module</strong> \u2014 the camera occupies GPIO</p>\n<p>0,5,18,19,21\u201323,25\u201327,32,34\u201336,39 and the SD card 2,4,12\u201315. Nothing left for PWM.</p>\n<p>Flashing it may need BOOT/IO0 held to GND.</p>\n<hr>\n<h2>Bench notes that cost time on 2026-08-07</h2>\n<ul>\n<li><strong>COM numbers move constantly.</strong> The same board was COM9/11/12/13/14/16 in one</li>\n</ul>\n<p>  week. List ports and identify by <code>PING</code> (returns id, name, type). Never trust a</p>\n<p>  remembered port number.</p>\n<ul>\n<li><strong>Stop the hub before flashing.</strong> It re-opens ports and uploads then fail.</li>\n<li>Opening a port with pyserial <strong>reboots the board</strong> unless <code>dtr</code>/<code>rts</code> are set</li>\n</ul>\n<p>  false *before* <code>open()</code>.</p>\n<ul>\n<li>Boards drop off USB mid-session (cable/power, not firmware).</li>\n</ul>\n<h2>Open, waiting on the user</h2>\n<ul>\n<li><strong>SHRUG measurements.</strong> The calibration curve is built, tested and landed; it</li>\n</ul>\n<p>  just needs numbers. Their rig has <code>neutral[9] = 93</code>, <code>min 79</code>, <code>max 107</code>.</p>\n<p>  Table: SHRUG angle \u2192 left rise mm, right rise mm (up positive), 3\u20135 rows.</p>\n<ul>\n<li>Two boards dropping off USB \u2014 cable or power.</li>\n</ul>\n<h2>A trap worth not repeating</h2>\n<p><code>check_shrug_curve.py</code> hardcoded <code>93</code> as &quot;a moved SHRUG position&quot;. The user&#x27;s rig</p>\n<p>has neutral <strong>93</strong>, so the test asserted the bar rocks while sitting at rest, and</p>\n<p>blamed the code. <strong>Derive test angles from <code>RIG</code>, never hardcode joint values</strong> \u2014</p>\n<p>the rig is user data and it changes.</p>", "done": 0, "total": 0};
