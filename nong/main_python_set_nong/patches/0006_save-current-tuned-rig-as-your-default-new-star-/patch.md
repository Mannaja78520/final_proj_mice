# Patch 0006

- **when:** 2026-07-26 00:32
- **change:** Save current tuned rig as your default: new star-Save-current-as-default button locks the cached tune into a protected slot; Reset now returns to YOUR default (factory only if none saved). Does NOT overwrite tuned dims.
- **files:** app.js, index.html, style.css

Restore this exact version with:  `python save_patch.py --restore 6`
